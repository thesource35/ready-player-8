import XCTest
@testable import ready_player_8

// Wave 0 stub — implementation lands in Plan 15-04
final class TeamServiceTests: XCTestCase {
    func testTeamMemberFormRejectsEmptyName() throws {
        throw XCTSkip("Wave 0 stub — implemented in Plan 15-04")
    }

    func testCertExpiryAlertRoutesToAssignedTaskCategory() throws {
        throw XCTSkip("Wave 0 stub — covered by Deno tests in 15-02")
    }
}
