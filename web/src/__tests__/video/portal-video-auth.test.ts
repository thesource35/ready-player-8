// Owner: 22-09-PLAN.md Wave 3 — Portal video auth guards (VIDEO-01-M)
// Will test: drone source_type → 403; portal_visible=false → 403; live source with show_cameras=false → 403;
// portal live is head-only (no DVR offset param accepted).
import { describe, it, expect } from "vitest";

describe("Portal video auth", () => {
  it.skip("TODO: implemented in Wave 3 plan 22-09-PLAN.md — drone/portal_visible/show_cameras gates + head-only live", () => {
    expect(true).toBe(true);
  });
});
