// Owner: 22-04-PLAN.md Wave 2 — VOD signed-manifest batch signer (VIDEO-01-H)
// Will test: /api/video/vod/playback-url returns a rewritten m3u8 manifest with every .ts line as a
// signed Supabase Storage URL; Content-Type is application/vnd.apple.mpegurl.
import { describe, it, expect } from "vitest";

describe("VOD manifest batch signer", () => {
  it.skip("TODO: implemented in Wave 2 plan 22-04-PLAN.md — signed URLs for every segment + correct Content-Type", () => {
    expect(true).toBe(true);
  });
});
