import { describe, it, expect } from "vitest";

describe("Section Configuration", () => {
  it.todo("applies executive_summary template defaults");
  it.todo("applies full_progress template defaults");
  it.todo("applies photo_update template defaults");
  it.todo("budget defaults to disabled regardless of template (D-33)");
  it.todo("toggles individual sections independently");
  it.todo("persists date range per section (D-35)");
  it.todo("persists pinned items per section (D-36)");
  it.todo("persists section notes (D-45)");
  it.todo("select all / deselect all toggle (D-34)");
});
