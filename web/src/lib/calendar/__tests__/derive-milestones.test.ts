import { describe, it, expect } from "vitest";

describe("derive-milestones", () => {
  it("merges projects.start_date/end_date, contracts.bid_due_date, schedule_events inspections into one milestones[]", () => {
    // RED — implement in Wave 2 (Plan 17-02)
    // Expected: deriveMilestones({ projects, contracts, events }) returns a flat list
    // containing 4 milestone types: "Project Start", "Project End", "Bid Due", "Inspection".
    throw new Error("RED — not yet implemented: deriveMilestones pure fn (Plan 17-02)");
  });
});
