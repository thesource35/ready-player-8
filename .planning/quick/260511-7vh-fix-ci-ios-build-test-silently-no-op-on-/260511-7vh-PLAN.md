---
phase: quick-260511-7vh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - 999.10
must_haves:
  truths:
    - "CI iOS job actually compiles the Swift project (no silent 20s no-op)"
    - "A real Swift compile error in main causes the job to fail (red status, not green)"
    - "The CI log shows the available iOS Simulator destination inventory before build (sanity assertion against future runtime drift)"
    - "Workflow uses macos-15-default Xcode 16.4 paired with the pre-installed iOS 18.5 simulator runtime (no -downloadPlatform race)"
    - "xcbeautify renders xcodebuild output with GitHub Actions inline annotations under workflow-level pipefail"
    - "web-build and link-health jobs are byte-unchanged (no scope creep)"
    - "Inline TODO marker exists for the deferred xcodebuild test re-enable so the gap is grep-discoverable"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Honest iOS CI that fails loudly on real compile errors"
      contains: "Xcode_16.4.app"
    - path: ".github/workflows/ci.yml"
      provides: "Workflow-level pipefail (Kingfisher pattern)"
      contains: "bash -eo pipefail"
    - path: ".github/workflows/ci.yml"
      provides: "Pinned deterministic destination (no OS=latest fragility)"
      contains: "iPhone 16 Pro,OS=18.5"
    - path: ".github/workflows/ci.yml"
      provides: "Sanity-check destination step that surfaces inventory in log"
      contains: "xcrun simctl list devices"
    - path: ".github/workflows/ci.yml"
      provides: "Modern formatter with GitHub annotations"
      contains: "xcbeautify --renderer github-actions"
    - path: ".github/workflows/ci.yml"
      provides: "Grep-discoverable marker for deferred test re-enable"
      contains: "TODO(999.10): re-enable xcodebuild test"
  key_links:
    - from: ".github/workflows/ci.yml build-and-test job"
      to: "/Applications/Xcode_16.4.app on macos-15 runner"
      via: "DEVELOPER_DIR env var"
      pattern: "DEVELOPER_DIR.*Xcode_16\\.4\\.app"
    - from: ".github/workflows/ci.yml build-and-test job"
      to: "iOS 18.5 simulator runtime (image-pre-installed)"
      via: "-destination platform=iOS Simulator,name=iPhone 16 Pro,OS=18.5"
      pattern: "OS=18\\.5"
    - from: ".github/workflows/ci.yml workflow-level defaults"
      to: "Every step in every job"
      via: "defaults.run.shell: bash -eo pipefail {0}"
      pattern: "bash -eo pipefail"
    - from: "xcodebuild stdout"
      to: "GitHub Actions log + inline PR annotations"
      via: "pipe into xcbeautify --renderer github-actions"
      pattern: "xcbeautify --renderer github-actions"
---

<objective>
Restore honest CI signal for the iOS build job. The current `build-and-test` job in `.github/workflows/ci.yml` silently no-ops in ~20s because (a) it pins `Xcode_16.2.app` whose matching iOS 18.2 simulator runtime was removed from the macos-15 image under GitHub's August 2025 "three runtimes max" policy, so `name=iPhone 16,OS=latest` matches zero destinations, and (b) `| xcpretty || true` swallows the resulting xcodebuild failure and exits 0.

Purpose: Closes backlog item 999.10. After this change, a real Swift compile error in `main` will turn CI red instead of being hidden behind a green checkmark — restoring trust in the CI signal before continuing v2.1 production work.

Output: Single-file edit to `.github/workflows/ci.yml` swapping the iOS `build-and-test` job to Xcode 16.4 + iPhone 16 Pro/iOS 18.5 + xcbeautify + workflow-level pipefail, with a sanity-check destination step and an inline TODO marker for the deferred test re-enable. `web-build` and `link-health` jobs are NOT touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260511-7vh-fix-ci-ios-build-test-silently-no-op-on-/260511-7vh-RESEARCH.md
@.github/workflows/ci.yml

<!--
Why these references:
- RESEARCH.md contains the live-verified macos-15 image inventory, the canonical recommended workflow diff (verbatim), and the rationale for every choice (Xcode 16.4 vs 16.2, iPhone 16 Pro/18.5 vs iPhone 17/26.x, xcbeautify vs xcpretty, build-only vs test). The executor should treat the YAML in the RESEARCH "Recommended Workflow Diff" section as the source of truth.
- ci.yml is the file being edited. The executor MUST preserve the `web-build` (lines 41-59) and `link-health` (lines 61-79) jobs byte-for-byte. Only the `build-and-test` job (lines 10-39) plus the workflow-level `defaults` block (new, top-level) change.
- STATE.md is implicitly referenced via the compile-only verification precedent (Phase 22/29.1/30 all explicitly adopted compile-only because of the same pre-existing ready_player_8Tests.swift async/concurrency errors). Do NOT add a `Run Tests` step.
-->

<interfaces>
<!-- Current ci.yml top-level structure (3 jobs at top level under `jobs:`). -->

```yaml
name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  build-and-test:        # iOS macOS-15 — TO BE REPLACED
    runs-on: macos-15
  web-build:             # Ubuntu Node 20 — DO NOT TOUCH
    runs-on: ubuntu-latest
  link-health:           # Ubuntu link audit — DO NOT TOUCH
    runs-on: ubuntu-latest
    continue-on-error: true
```

<!-- Existing macos-15 runner image facts (from RESEARCH.md, live-verified 2026-04-28 against image 20260428.0039.1): -->
<!-- - Default Xcode: 16.4 (also has 16.3, 16.2, 26.x available under /Applications/) -->
<!-- - Pre-installed iOS Simulator runtimes: 18.5, 18.6, 26.0, 26.1, 26.2 (NOT 18.2) -->
<!-- - Pre-installed log formatters: xcbeautify 3.2.1, xcpretty (Ruby gem, maintenance mode) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace iOS build-and-test job + add workflow-level pipefail in .github/workflows/ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Edit `.github/workflows/ci.yml` to replace the existing `build-and-test` job (lines 10-39) and add a workflow-level `defaults.run.shell` block. The `web-build` (lines 41-59) and `link-health` (lines 61-79) jobs MUST remain byte-identical — verify with `git diff` that only the iOS job and the new top-level `defaults` block changed.

**Make exactly these edits, preserving the rest of the file verbatim:**

1. **Add a top-level `defaults` block** between the `on:` block and the `jobs:` block (so workflow-level pipefail applies to every step in every job — Kingfisher pattern, RESEARCH.md "Reference Workflows" section). This is safe for the Ubuntu jobs because their existing scripts already work under pipefail (no broken pipes today):

```yaml
defaults:
  run:
    shell: bash -eo pipefail {0}
```

2. **Replace the `build-and-test` job entirely** with the YAML below (verbatim from RESEARCH.md "Recommended Workflow Diff" section, plus the inline TODO comment for T-999.10-03 mitigation and the inline runner-images audit comment for T-999.10-02 mitigation):

```yaml
  build-and-test:
    runs-on: macos-15
    timeout-minutes: 30
    # Pin Xcode 16.4 (macos-15 default) — its matching iOS 18.5 simulator runtime
    # is pre-installed in the image. Xcode 16.2 was previously pinned but its
    # matching iOS 18.2 runtime was removed under GitHub's Aug 2025 "three
    # runtimes max" policy, causing OS=latest to match zero destinations and
    # the job to silently no-op (backlog 999.10).
    #
    # Audit periodically against:
    #   https://github.com/actions/runner-images/blob/main/images/macos/macos-15-Readme.md
    # If iOS 18.5 is removed from the image, this pin must move to the next
    # pre-installed runtime (likely 18.6 or 26.x) — failing to do so will
    # surface visibly in the "Show available iOS Simulator destinations" step
    # below, NOT silently as before. (T-999.10-02)
    env:
      DEVELOPER_DIR: /Applications/Xcode_16.4.app/Contents/Developer
    steps:
      - uses: actions/checkout@v4

      # Sanity assertion: surface destination availability in the log so future
      # runtime-removal events are caught the FIRST CI run, not weeks later.
      # (T-999.10-01 — silent failure mitigation)
      - name: Show available iOS Simulator destinations
        run: |
          xcodebuild -version
          echo "--- iOS Simulator destinations ---"
          xcrun simctl list devices available | grep -E "iOS|iPhone" || true

      # Compile-only verification per Phase 22 / 29.1 / 30 precedent (STATE.md):
      # ready_player_8Tests.swift has 30+ pre-existing async/concurrency errors
      # that block `xcodebuild test`. Re-enable the test step ONLY after a
      # focused phase repairs the test target.
      # TODO(999.10): re-enable xcodebuild test step once
      # ready_player_8Tests.swift async errors are fixed (tracked in
      # Phase 22 deferred-items). Grep marker: TODO(999.10) (T-999.10-03)
      - name: Build iOS (compile-only — see TODO above)
        run: |
          xcodebuild build \
            -project "ready player 8.xcodeproj" \
            -scheme "ready player 8" \
            -destination "platform=iOS Simulator,name=iPhone 16 Pro,OS=18.5" \
            -configuration Debug \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            | xcbeautify --renderer github-actions
```

**Things the executor MUST NOT do:**

- Do NOT add a `Run Tests` / `xcodebuild test` step. The existing test target has pre-existing async errors documented since Phase 22 — adopting compile-only verification is the explicit precedent (STATE.md mentions it ~10 times across Phase 22/29.1/30-02/30-04/30-07).
- Do NOT use `|| true` anywhere in the YAML. That mask was the root cause of the silent failure being fixed.
- Do NOT use `sudo xcode-select -s ...`. Setting `DEVELOPER_DIR` env var achieves the same Xcode selection without sudo (Alamofire/Kingfisher pattern).
- Do NOT touch the `web-build` job (lines 41-59) or the `link-health` job (lines 61-79). The `defaults.run.shell` addition will inherit into them, which is safe (their commands already work under pipefail).
- Do NOT use `OS=latest`. Pin `OS=18.5` explicitly — the whole point of this fix is that `latest` is fragile across monthly image refreshes.
- Do NOT change `runs-on: macos-15` to `macos-26`. macos-26 is GA but adopting it requires also bumping to Xcode 26.x which would surface new Swift 6.2/6.3 strict-concurrency diagnostics — out of scope for this quick task. Stay on macos-15 + Xcode 16.4.

**Why these specific choices** (cross-referenced to RESEARCH.md):
- `Xcode_16.4.app` — RESEARCH "Standard Stack" + "Why NOT Xcode 16.2" tables. macos-15 default; iOS 18.5 sim pre-installed; zero `-downloadPlatform` race.
- `iPhone 16 Pro,OS=18.5` — RESEARCH "Open Question 2". Uses image-pre-installed runtime; deterministic across image refreshes (until iOS 18.5 itself is deprecated).
- `xcbeautify --renderer github-actions` — RESEARCH "Standard Stack" + Pitfall 2. Pre-installed at v3.2.1; native Swift binary; emits `::error::` and `::warning::` GitHub annotations.
- Workflow-level `bash -eo pipefail` — RESEARCH "Reference Workflows" #2 (Kingfisher). Single source of pipefail truth.
- `Show available iOS Simulator destinations` step — RESEARCH "Recommended Workflow Diff" + Pitfall 5. Loud diagnostic so the next runtime-removal is visible immediately.
  </action>
  <verify>
<verify>
  <automated>python3 -c 'import yaml; doc = yaml.safe_load(open(".github/workflows/ci.yml")); jobs = doc.get("jobs", {}); assert "build-and-test" in jobs, "build-and-test job missing"; assert "web-build" in jobs, "web-build job missing (regression)"; assert "link-health" in jobs, "link-health job missing (regression)"; bat = jobs["build-and-test"]; assert bat.get("runs-on") == "macos-15", f"wrong runner: {bat.get(\"runs-on\")}"; env = bat.get("env", {}); assert env.get("DEVELOPER_DIR") == "/Applications/Xcode_16.4.app/Contents/Developer", f"wrong DEVELOPER_DIR: {env.get(\"DEVELOPER_DIR\")}"; defaults_run = doc.get("defaults", {}).get("run", {}); assert defaults_run.get("shell") == "bash -eo pipefail {0}", f"missing/wrong workflow-level pipefail: {defaults_run.get(\"shell\")}"; steps = bat.get("steps", []); step_names = [s.get("name", "") for s in steps]; assert any("Show available iOS Simulator destinations" in n for n in step_names), f"missing destinations sanity step: {step_names}"; assert not any("Run Tests" in n or "xcodebuild test" in n for n in step_names), f"forbidden test step present: {step_names}"; full_yaml = open(".github/workflows/ci.yml").read(); assert "|| true" not in full_yaml, "|| true must NOT appear anywhere"; assert "xcpretty" not in full_yaml, "xcpretty must be replaced by xcbeautify"; assert "xcbeautify --renderer github-actions" in full_yaml, "xcbeautify --renderer github-actions missing"; assert "iPhone 16 Pro,OS=18.5" in full_yaml, "destination not pinned to iPhone 16 Pro/iOS 18.5"; assert "OS=latest" not in full_yaml, "OS=latest must be removed (fragile)"; assert "TODO(999.10)" in full_yaml, "TODO(999.10) marker missing for deferred test re-enable"; assert "sudo xcode-select" not in full_yaml, "sudo xcode-select must be replaced by DEVELOPER_DIR env"; print("OK: ci.yml passes all 999.10 invariants")'</automated>
</verify>
  </verify>
  <done>
1. `.github/workflows/ci.yml` parses as valid YAML.
2. Top-level `defaults.run.shell` is `bash -eo pipefail {0}`.
3. `build-and-test` job uses `runs-on: macos-15`, `env.DEVELOPER_DIR: /Applications/Xcode_16.4.app/Contents/Developer`.
4. `build-and-test` job has a `Show available iOS Simulator destinations` step before the build step.
5. `build-and-test` job has NO `Run Tests` step (compile-only per precedent).
6. The string `|| true` does NOT appear anywhere in the file.
7. The string `xcpretty` does NOT appear anywhere; `xcbeautify --renderer github-actions` does.
8. Destination is pinned to `iPhone 16 Pro,OS=18.5` (not `OS=latest`).
9. `TODO(999.10)` marker is present for the deferred test re-enable (grep-discoverable).
10. `web-build` and `link-health` jobs still exist in the `jobs:` map (regression check).
11. The verify command above prints `OK: ci.yml passes all 999.10 invariants` and exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final defense-in-depth grep audit + record real-CI verification deferral</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Run a final round of explicit grep checks against `.github/workflows/ci.yml` to belt-and-suspenders the YAML-parse-based asserts in Task 1 (a YAML parser may tolerate semantically-equivalent reorderings that text-grep would catch). Then surface the unavoidable verification gap: GitHub-hosted CI cannot be exercised from this local machine — the only real proof that the silent-failure trap is closed comes from observing the next CI run on GitHub after the commit is pushed.

**No file edits in this task.** This is a pure verification + reporting step. The executor MUST:

1. Run the grep audit (the verify block below). If any check fails, return to Task 1 and fix.

2. Optionally — if `actionlint` is available locally (`command -v actionlint`) — run `actionlint .github/workflows/ci.yml` for additional schema validation. If `actionlint` is NOT installed, skip silently (this is bonus, not required — the YAML parse + grep checks already cover the load-bearing invariants).

3. In the SUMMARY.md the executor will write at end-of-quick-task, include a clearly-marked "Real-CI verification deferred" section noting:
   - **What this PR proves locally:** YAML parses; `xcpretty` and `|| true` are gone; Xcode 16.4 + iPhone 16 Pro/18.5 are pinned; sanity-check step is present; TODO(999.10) marker is grep-discoverable.
   - **What this PR does NOT prove locally:** That `Xcode_16.4.app` actually exists at that path on the macos-15 runner image at the moment the next CI run executes (image-inventory drift is the very class of bug this PR fixes — the `Show available iOS Simulator destinations` step makes any such drift visible in the next run's log).
   - **How real verification happens:** Push the change to `main` (or a branch + PR), then either watch the Actions tab manually or run `gh run watch --exit-status` against the resulting run. Expected outcomes:
     - Green check + log shows `iPhone 16 Pro (18.5)` in destinations + `BUILD SUCCEEDED` in xcbeautify output → silent-failure trap closed, Task 1 done correctly.
     - Red X with a real Swift compile error → ALSO a win: CI is now telling the truth (this is the desired post-condition, not a Task 1 regression).
     - Red X with `Xcode_16.4.app: No such file or directory` → image inventory has drifted; bump the pin to whatever the destinations diagnostic step shows (likely Xcode 26.x + macos-26 runner — see RESEARCH "Alternatives Considered").
     - Green check + log shows ZERO destinations + ZERO compile output → previous silent-failure mode somehow recurred; investigate immediately (this would mean a new mask was introduced, e.g., `set +e` somewhere).
  </action>
  <verify>
<verify>
  <automated>set -e; F=.github/workflows/ci.yml; grep -q "DEVELOPER_DIR: /Applications/Xcode_16.4.app/Contents/Developer" "$F" && grep -q "platform=iOS Simulator,name=iPhone 16 Pro,OS=18.5" "$F" && grep -q "xcbeautify --renderer github-actions" "$F" && grep -q "shell: bash -eo pipefail {0}" "$F" && grep -q "Show available iOS Simulator destinations" "$F" && grep -q "TODO(999.10)" "$F" && ! grep -q "|| true" "$F" && ! grep -q "xcpretty" "$F" && ! grep -q "OS=latest" "$F" && ! grep -q "sudo xcode-select" "$F" && grep -q "^  web-build:" "$F" && grep -q "^  link-health:" "$F" && grep -q "continue-on-error: true" "$F" && echo "OK: all 13 grep invariants satisfied"</automated>
</verify>
  </verify>
  <done>
1. The grep audit (verify block) prints `OK: all 13 grep invariants satisfied` and exits 0.
2. SUMMARY.md authored by the executor at end-of-quick-task contains a "Real-CI verification deferred" section with the 4 expected-outcome scenarios from the action above.
3. The user understands that the next push to a branch is the actual proof, and knows what to look for in the GitHub Actions log (the `Show available iOS Simulator destinations` step output + the `BUILD SUCCEEDED`/error lines from xcbeautify).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer commit -> GitHub Actions runner | YAML interpreted by GitHub's Actions parser on a fresh macos-15 VM; runner image inventory may drift between commit time and run time |
| xcodebuild stdout -> CI exit code | Pipe-chain semantics determine whether a real build failure produces a non-zero exit code (this is the load-bearing boundary the original bug violated) |
| pinned Xcode/iOS-runtime version -> macos-15 image inventory | GitHub's "three runtimes max" policy means this pin must be re-validated periodically |
| executor (this task) -> deferred-items.md / future test re-enable phase | The compile-only verification stance must remain grep-discoverable so the deferred test re-enable is not lost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.10-01 | Tampering / Denial of evidence | xcodebuild pipe chain in build step | mitigate | (a) workflow-level `defaults.run.shell: bash -eo pipefail {0}` so EVERY step preserves the leftmost non-zero exit code in pipes; (b) drop `\| xcpretty \|\| true` entirely (replace with `\| xcbeautify --renderer github-actions` which has no exit-mask); (c) prepend a `Show available iOS Simulator destinations` step that prints `xcodebuild -version` and `xcrun simctl list devices available` so any zero-destination state is visible in the FIRST log line, not silently masked. Verify in Task 1 with `! grep -q "\|\| true"` and `! grep -q "xcpretty"`. |
| T-999.10-02 | Information disclosure / Stale config | Xcode + iOS-runtime pin (Xcode 16.4 + iOS 18.5) | mitigate | Inline comment block above `env:` linking to the canonical `actions/runner-images/main/images/macos/macos-15-Readme.md` and explaining that the destinations diagnostic step is the early-warning mechanism for image drift. Pin is deterministic (specific Xcode major.minor + specific iOS major.minor) so any drift produces a loud, actionable failure rather than silent recurrence. Verify in Task 1 with `grep -q "Xcode_16.4.app"` and `grep -q "OS=18.5"` and `! grep -q "OS=latest"`. |
| T-999.10-03 | Denial of test discipline | Deferred `xcodebuild test` re-enable | mitigate | Inline `TODO(999.10): re-enable xcodebuild test step once ready_player_8Tests.swift async errors are fixed` comment immediately above the build step — uses the exact backlog ID as the grep marker so a future `grep -r "TODO(999.10)"` from the test-repair phase finds the correct re-enable site. Adopts the explicit Phase 22/29.1/30 compile-only verification precedent rather than expanding scope to also fix the test target. Verify in Task 1 with `grep -q "TODO(999.10)"`. |
| T-999.10-04 | Spoofing / Local verification false-positive | The local verify commands cannot exercise the actual macos-15 runner | accept | Local verification (YAML parse + grep) proves the YAML is structurally correct but CANNOT prove that the runner image inventory at next-CI-run-time still has Xcode 16.4 + iOS 18.5. Disposition: ACCEPT — the only honest test is to push and observe the next run. Task 2 explicitly surfaces this gap in SUMMARY.md with the 4 expected-outcome scenarios so the user knows what to look for in the actual CI log. The mitigation for T-999.10-01 (the destinations diagnostic step) ensures that if this acceptance turns into a real problem, the next failure will be loud, not silent. |
| T-999.10-05 | Elevation of privilege via workflow scope creep | `web-build` and `link-health` jobs in same file | mitigate | Task 1 action explicitly forbids touching lines 41-79 (web-build + link-health). Task 2 verify includes regression greps `grep -q "^  web-build:"`, `grep -q "^  link-health:"`, and `grep -q "continue-on-error: true"` so any accidental edit to those jobs is caught before commit. The new top-level `defaults.run.shell: bash -eo pipefail {0}` does inherit into the Ubuntu jobs but their existing commands (`npm ci`, `npm run lint`, `node scripts/...`) are already pipefail-safe (single commands, no broken pipes). |
</threat_model>

<verification>
After Task 1 + Task 2 complete, the executor MUST run the combined invariant check:

```bash
F=.github/workflows/ci.yml && \
python3 -c "import yaml; yaml.safe_load(open('$F'))" && \
grep -q "DEVELOPER_DIR: /Applications/Xcode_16.4.app/Contents/Developer" "$F" && \
grep -q "platform=iOS Simulator,name=iPhone 16 Pro,OS=18.5" "$F" && \
grep -q "xcbeautify --renderer github-actions" "$F" && \
grep -q "shell: bash -eo pipefail {0}" "$F" && \
grep -q "Show available iOS Simulator destinations" "$F" && \
grep -q "TODO(999.10)" "$F" && \
! grep -q "|| true" "$F" && \
! grep -q "xcpretty" "$F" && \
! grep -q "OS=latest" "$F" && \
! grep -q "sudo xcode-select" "$F" && \
grep -q "^  web-build:" "$F" && \
grep -q "^  link-health:" "$F" && \
echo "PHASE VERIFIED: 999.10 invariants all green"
```

Exit code must be 0 and the `PHASE VERIFIED` message must print.

The real, end-to-end verification (a CI run on actual GitHub-hosted macos-15) is intentionally deferred to the next push — see Task 2's "Real-CI verification deferred" section in SUMMARY.md.
</verification>

<success_criteria>
1. `.github/workflows/ci.yml` is valid YAML and parses cleanly under `python3 -c "import yaml; yaml.safe_load(open(...))"`.
2. The iOS `build-and-test` job pins `Xcode_16.4.app` via `DEVELOPER_DIR` env var (no sudo xcode-select).
3. The destination is pinned to `iPhone 16 Pro,OS=18.5` (no `OS=latest`).
4. xcodebuild output is piped through `xcbeautify --renderer github-actions` (no xcpretty, no `|| true`).
5. Workflow-level `defaults.run.shell: bash -eo pipefail {0}` is set.
6. A `Show available iOS Simulator destinations` step runs BEFORE the build step.
7. An inline `TODO(999.10): re-enable xcodebuild test ...` marker exists for grep-discoverability.
8. The `Run Tests` / `xcodebuild test` step is REMOVED (compile-only per Phase 22/29.1/30 precedent).
9. The `web-build` and `link-health` jobs are byte-unchanged (no scope creep into Ubuntu jobs).
10. SUMMARY.md surfaces the unavoidable real-CI verification gap with 4 named expected-outcome scenarios so the user knows what to watch for in the next CI run.
11. Backlog item 999.10 is referenced by ID in both the inline TODO marker and the SUMMARY so STATE.md cross-linking is straightforward.
</success_criteria>

<output>
After completion, create `.planning/quick/260511-7vh-fix-ci-ios-build-test-silently-no-op-on-/260511-7vh-SUMMARY.md` with:
- Standard quick-task summary frontmatter (status, files_modified, requirements: [999.10]).
- Closure of backlog 999.10 with the commit SHA.
- The "Real-CI verification deferred" section described in Task 2 — explicitly listing the 4 expected-outcome scenarios for the next CI run (green+evidence / red+real-error / red+missing-Xcode / green+silent-recurrence).
- Forward pointer: a follow-up quick task or phase to repair `ready_player_8Tests.swift` async/concurrency errors so the deferred `xcodebuild test` step can be re-enabled (grep `TODO(999.10)` to find the re-enable site).
- Update `.planning/STATE.md` Session Continuity to reflect that 999.10 is closed (or closed-pending-CI-observation) with a one-line cross-link to the commit.
</output>
