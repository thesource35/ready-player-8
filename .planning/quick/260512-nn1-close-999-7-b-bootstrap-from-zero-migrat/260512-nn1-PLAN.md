---
phase: quick-260512-nn1
plan: 01
type: execute
wave: 1
files_modified:
  - ".planning/ROADMAP.md"
autonomous: true
must_haves:
  truths:
    - "ROADMAP.md 999.7 row reflects that (b) is substantially closed (the baseline file already addresses the superuser concern)"
    - "The remaining 'staging E2E test' is documented as a user-initiated verification, not blocking ongoing work"
    - "If staging push reveals a real gap, recovery path is to open 999.7(c) at that point"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "999.7 row updated with (b) closure annotation + staging-verify note"
      contains: "(b) substantially closed"
---

<objective>
Update ROADMAP.md 999.7 row to reflect today's finding: the baseline file already addresses the (b) superuser-statement concern. Document the single remaining unknown (real-staging E2E test) as a user-initiated verification step.

NOT writing new migration SQL. The baseline file already exists and the research findings show it's structurally complete.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Annotate ROADMAP.md 999.7 row with (b) closure + staging-verify pending</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (line 312 — the 999.7 row, including the existing (a) CLOSED 2026-05-11 annotation)
    - .planning/quick/260512-nn1-close-999-7-b-bootstrap-from-zero-migrat/260512-nn1-RESEARCH.md (inline findings)
  </read_first>
  <action>
    Find the 999.7 row in ROADMAP.md (line 312-ish) and append to its Notes column AFTER the existing "(a) CLOSED 2026-05-11" annotation, BEFORE the "(b) Schema dump from prod..." paragraph:

    Insert this annotation block (between the (a) closure and the (b) section that's currently being updated):

    ```
    **(b) SUBSTANTIALLY CLOSED 2026-05-12** via quick task 260512-nn1: `00000000000_baseline_pre_migration_tables.sql` (372 lines, 19 CREATE TABLEs with `IF NOT EXISTS`) already addresses the core (b) concern. The file header (lines 21-27) explicitly documents that GRANT / ALTER OWNER / COMMENT ON statements were stripped during authoring as deferred-to-baseline-cleanup-phase work; zero such statements exist in the file body. Cross-reference confirms 24 cs_* tables have CREATE TABLE migrations (19 in baseline + 5 in phase migrations) — no missing creates. **Remaining unknown**: real-staging end-to-end test — confirming `supabase db push --include-all` succeeds against a genuinely empty Supabase project requires user-initiated provisioning (cost + cleanup overhead). Recovery path if staging push reveals a gap: open 999.7(c) at that point.
    ```

    Then strike-through or precede the existing "(b) Schema dump from prod still includes..." paragraph with "**Historical (pre-2026-05-12 framing):**" so the original concern is preserved as context but clearly marked as outdated.

    Do NOT touch any other backlog row. Do NOT touch the (a) closure annotation (that's correct as-is).
  </action>
  <verify>
    <automated>
      grep -c "(b) SUBSTANTIALLY CLOSED 2026-05-12" .planning/ROADMAP.md   # expect 1
      grep -c "999.7" .planning/ROADMAP.md   # expect ≥1 (row identifier preserved)
      grep -c "999.7(c)" .planning/ROADMAP.md   # expect ≥1 (recovery path documented)
      grep -c "260512-nn1" .planning/ROADMAP.md   # expect 1 (cross-link)
      [ "$(git diff --name-only HEAD | wc -l | tr -d ' ')" -eq 1 ] && echo "scope: PASS"
      [ "$(git diff --name-only HEAD)" = ".planning/ROADMAP.md" ] && echo "correct-file: PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 6 grep invariants PASS
    - Single file modified (.planning/ROADMAP.md)
    - No code changes
    - No other backlog rows touched
  </acceptance_criteria>
  <done>
    ROADMAP.md 999.7 row reflects substantial closure + staging-verify pending; recovery path documented.
  </done>
</task>

</tasks>

<verification>
Single-task plan. Verify cascade in Task 1 covers all invariants. No code changes. Single .md edit.
</verification>

<success_criteria>
- 999.7(b) marked as substantially closed with rationale
- Staging E2E test documented as the only remaining unknown
- Recovery path (999.7(c)) documented for the worst case
- ROADMAP.md is the single file changed
</success_criteria>
