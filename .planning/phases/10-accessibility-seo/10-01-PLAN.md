---
phase: 10-accessibility-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/analytics/layout.tsx
  - web/src/app/clients/layout.tsx
  - web/src/app/compliance/layout.tsx
  - web/src/app/contractors/layout.tsx
  - web/src/app/cos-network/layout.tsx
  - web/src/app/electrical/layout.tsx
  - web/src/app/empire/layout.tsx
  - web/src/app/field/layout.tsx
  - web/src/app/finance/layout.tsx
  - web/src/app/hub/layout.tsx
  - web/src/app/market/layout.tsx
  - web/src/app/pricing/layout.tsx
  - web/src/app/privacy/layout.tsx
  - web/src/app/roofing/layout.tsx
  - web/src/app/scanner/layout.tsx
  - web/src/app/schedule/layout.tsx
  - web/src/app/security/layout.tsx
  - web/src/app/settings/layout.tsx
  - web/src/app/smart-build/layout.tsx
  - web/src/app/support/layout.tsx
  - web/src/app/tax/layout.tsx
  - web/src/app/tech/layout.tsx
  - web/src/app/terms/layout.tsx
  - web/src/app/training/layout.tsx
  - web/src/app/wealth/layout.tsx
  - web/src/lib/seo.ts
  - web/src/app/layout.tsx
  - web/src/app/sitemap.ts
  - web/src/app/robots.ts
  - web/public/manifest.json
  - web/public/og-image.png
  - web/public/icon-192.png
autonomous: true
requirements: [SEO-01, SEO-02, SEO-03, SEO-04, SEO-05, INFRA-04]

must_haves:
  truths:
    - "Every page route has a metadata export with page-specific title and description"
    - "Social shares show ConstructionOS OG image with correct dimensions"
    - "Search engines can discover sitemap.xml and robots.txt"
    - "PWA Add to Home Screen works with 192x192 icon"
  artifacts:
    - path: "web/src/app/sitemap.ts"
      provides: "Dynamic sitemap generation"
      exports: ["default"]
    - path: "web/src/app/robots.ts"
      provides: "Robots.txt generation"
      exports: ["default"]
    - path: "web/public/og-image.png"
      provides: "Shared OpenGraph image for social sharing"
    - path: "web/public/icon-192.png"
      provides: "192x192 PWA icon"
    - path: "web/src/app/analytics/layout.tsx"
      provides: "Metadata for analytics page"
      exports: ["metadata"]
  key_links:
    - from: "web/src/app/*/layout.tsx"
      to: "web/src/lib/seo.ts"
      via: "getPageMetadata import"
      pattern: "getPageMetadata"
    - from: "web/src/app/layout.tsx"
      to: "web/public/og-image.png"
      via: "openGraph.images reference"
      pattern: "og-image.png"
    - from: "web/public/manifest.json"
      to: "web/public/icon-192.png"
      via: "icons array entry"
      pattern: "icon-192"
---

<objective>
Add SEO metadata to all 25 pages missing it, create sitemap.ts and robots.ts route handlers, add OG image for social sharing, and add 192x192 PWA icon to manifest.

Purpose: Make the web app discoverable by search engines and shareable on social platforms, and enable PWA installation with proper icon.
Output: 25 layout.tsx files, sitemap.ts, robots.ts, OG image, PWA icon, updated manifest.json and root layout.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-accessibility-seo/10-RESEARCH.md

@web/src/lib/seo.ts
@web/src/app/layout.tsx
@web/src/app/ai/layout.tsx
@web/public/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create metadata layout.tsx for all 25 missing pages</name>
  <files>
    web/src/app/analytics/layout.tsx
    web/src/app/clients/layout.tsx
    web/src/app/compliance/layout.tsx
    web/src/app/contractors/layout.tsx
    web/src/app/cos-network/layout.tsx
    web/src/app/electrical/layout.tsx
    web/src/app/empire/layout.tsx
    web/src/app/field/layout.tsx
    web/src/app/finance/layout.tsx
    web/src/app/hub/layout.tsx
    web/src/app/market/layout.tsx
    web/src/app/pricing/layout.tsx
    web/src/app/privacy/layout.tsx
    web/src/app/roofing/layout.tsx
    web/src/app/scanner/layout.tsx
    web/src/app/schedule/layout.tsx
    web/src/app/security/layout.tsx
    web/src/app/settings/layout.tsx
    web/src/app/smart-build/layout.tsx
    web/src/app/support/layout.tsx
    web/src/app/tax/layout.tsx
    web/src/app/tech/layout.tsx
    web/src/app/terms/layout.tsx
    web/src/app/training/layout.tsx
    web/src/app/wealth/layout.tsx
  </files>
  <read_first>
    web/src/app/ai/layout.tsx
    web/src/lib/seo.ts
  </read_first>
  <action>
Create a layout.tsx file for each of the 25 pages listed below, following the exact established pattern from web/src/app/ai/layout.tsx (per SEO-01). Each file has identical structure -- only the slug string changes:

```typescript
import { getPageMetadata } from "@/lib/seo";

export const metadata = getPageMetadata("SLUG");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
```

The 25 slugs to create (each maps to the directory name):
analytics, clients, compliance, contractors, cos-network, electrical, empire, field, finance, hub, market, pricing, privacy, roofing, scanner, schedule, security, settings, smart-build, support, tax, tech, terms, training, wealth

All these slugs already have entries in web/src/lib/seo.ts -- no changes to seo.ts needed for this task.

IMPORTANT: These are SERVER components. Do NOT add "use client" directive. The metadata export only works in server components.
  </action>
  <verify>
    <automated>cd /Users/beverlyhunter/Desktop/ready\ player\ 8/web && count=$(find src/app -maxdepth 2 -name layout.tsx | wc -l) && echo "Layout files: $count" && test "$count" -ge 39</automated>
  </verify>
  <acceptance_criteria>
    - All 25 new layout.tsx files exist in their respective directories
    - Each file imports getPageMetadata from "@/lib/seo"
    - Each file exports const metadata with correct slug
    - Each file exports default Layout function that returns children
    - No "use client" directive in any file
  </acceptance_criteria>
  <done>25 layout.tsx files created, each exporting page-specific metadata from seo.ts. Total layout.tsx count is 40 (25 new + 15 existing).</done>
</task>

<task type="auto">
  <name>Task 2: Add sitemap.ts, robots.ts, OG image reference, and OG image file</name>
  <files>
    web/src/app/sitemap.ts
    web/src/app/robots.ts
    web/src/app/layout.tsx
    web/src/lib/seo.ts
    web/public/og-image.png
  </files>
  <read_first>
    web/src/app/layout.tsx
    web/src/lib/seo.ts
    .planning/phases/10-accessibility-seo/10-RESEARCH.md
  </read_first>
  <action>
1. Create `web/src/app/sitemap.ts` (per SEO-04) using Next.js MetadataRoute.Sitemap convention:
   - Import type `MetadataRoute` from "next"
   - Export default function `sitemap()` returning `MetadataRoute.Sitemap`
   - Base URL: "https://constructionos.world"
   - Include ONLY public-facing routes (NOT /api/, /auth/, /login, /checkout, /preview, /settings, /profile):
     "", "projects", "contracts", "market", "maps", "feed", "jobs", "ops", "ai", "pricing", "rentals", "punch", "tasks", "wealth", "trust", "verify", "hub", "security", "field", "finance", "compliance", "clients", "analytics", "schedule", "training", "scanner", "electrical", "tax", "roofing", "smart-build", "contractors", "tech", "cos-network", "empire", "terms", "privacy", "support"
   - Home ("") gets priority: 1, all others priority: 0.8
   - changeFrequency: "weekly" for all
   - lastModified: new Date()

2. Create `web/src/app/robots.ts` (per SEO-03) using Next.js MetadataRoute.Robots convention:
   - Import type `MetadataRoute` from "next"
   - Export default function `robots()` returning `MetadataRoute.Robots`
   - Rules: userAgent "*", allow "/", disallow ["/api/", "/auth/", "/preview/"]
   - Sitemap: "https://constructionos.world/sitemap.xml"

3. Update `web/src/lib/seo.ts` (per SEO-02) -- add OG images to the base object:
   - Change `const base` to include `images: [{ url: "/og-image.png", width: 1200, height: 630, alt: "ConstructionOS" }]`
   - This propagates OG image to ALL pages that use `...base` in their openGraph

4. Update `web/src/app/layout.tsx` (per SEO-05) -- add OG images to root metadata:
   - Add `images: [{ url: "/og-image.png", width: 1200, height: 630, alt: "ConstructionOS - The Construction Command Center" }]` to the existing openGraph object
   - Also add the same images array to the existing twitter object

5. Create `web/public/og-image.png` (per SEO-02) -- generate a 1200x630 OG image:
   - Use a Node.js script with Canvas or generate via CLI. The image should show "COS" initials in bold white text on dark background (#080E12) with amber accent (#F29E3D) underline. Tagline "The Construction Command Center" below.
   - If canvas dependency not available, create a simple SVG and convert, or use a placeholder approach: create a minimal HTML file and screenshot, or use ImageMagick if available.
   - Fallback: copy existing logo.png as og-image.png temporarily -- the image must exist.
  </action>
  <verify>
    <automated>cd /Users/beverlyhunter/Desktop/ready\ player\ 8/web && test -f src/app/sitemap.ts && test -f src/app/robots.ts && test -f public/og-image.png && grep -q "og-image.png" src/app/layout.tsx && grep -q "og-image.png" src/lib/seo.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - web/src/app/sitemap.ts exists and exports default function returning MetadataRoute.Sitemap
    - web/src/app/robots.ts exists and exports default function returning MetadataRoute.Robots
    - sitemap excludes /api/, /auth/, /login, /checkout, /preview, /settings, /profile routes
    - robots.ts disallows /api/ and /auth/
    - web/src/lib/seo.ts base object includes images array with og-image.png, width 1200, height 630
    - web/src/app/layout.tsx openGraph includes images array with og-image.png
    - web/public/og-image.png exists as a valid image file
  </acceptance_criteria>
  <done>sitemap.ts generates XML with all public routes, robots.ts blocks crawlers from API/auth paths, OG image referenced in root layout and seo.ts base config, og-image.png file exists in public/.</done>
</task>

<task type="auto">
  <name>Task 3: Add 192x192 PWA icon to manifest and generate icon file</name>
  <files>
    web/public/manifest.json
    web/public/icon-192.png
  </files>
  <read_first>
    web/public/manifest.json
  </read_first>
  <action>
1. Generate `web/public/icon-192.png` (per INFRA-04) -- a 192x192 PNG icon:
   - Resize existing `web/public/logo.png` (512x512) down to 192x192. Use `sips` (macOS built-in): `sips -z 192 192 web/public/logo.png --out web/public/icon-192.png`
   - If sips fails, try ImageMagick: `convert web/public/logo.png -resize 192x192 web/public/icon-192.png`
   - Verify the output is 192x192 with `sips -g pixelWidth -g pixelHeight web/public/icon-192.png`

2. Update `web/public/manifest.json` -- add the 192x192 icon entry to the icons array:
   ```json
   {
     "name": "ConstructionOS",
     "short_name": "ConstructOS",
     "description": "The operating system for the $13 trillion construction industry",
     "start_url": "/",
     "display": "standalone",
     "background_color": "#080E12",
     "theme_color": "#F29E3D",
     "icons": [
       { "src": "/logo-sm.png", "sizes": "128x128", "type": "image/png" },
       { "src": "/icon-192.png", "sizes": "192x192", "type": "image/png" },
       { "src": "/logo.png", "sizes": "512x512", "type": "image/png" }
     ]
   }
   ```
   Keep existing entries, insert the 192x192 entry between 128x128 and 512x512 (ordered by size).
  </action>
  <verify>
    <automated>cd /Users/beverlyhunter/Desktop/ready\ player\ 8 && test -f web/public/icon-192.png && grep -q "192x192" web/public/manifest.json && sips -g pixelWidth web/public/icon-192.png 2>/dev/null | grep -q "192" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - web/public/icon-192.png exists and is exactly 192x192 pixels
    - web/public/manifest.json contains an icon entry with "sizes": "192x192" and "src": "/icon-192.png"
    - manifest.json icons array has 3 entries: 128x128, 192x192, 512x512 (ordered by size)
    - manifest.json is valid JSON
  </acceptance_criteria>
  <done>192x192 PWA icon generated from existing logo, manifest.json updated with new icon entry. "Add to Home Screen" now has required icon size.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public web -> sitemap/robots | Crawlers access auto-generated XML/text -- no user input processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | I (Information Disclosure) | sitemap.ts | mitigate | Exclude auth-protected routes (/api/, /auth/, /login, /checkout, /preview, /settings, /profile) from sitemap |
| T-10-02 | I (Information Disclosure) | robots.ts | mitigate | Disallow /api/ and /auth/ paths in robots.txt |
| T-10-03 | S (Spoofing) | og-image.png | accept | Static asset, no user input, low risk |
</threat_model>

<verification>
1. `cd web && npx next build` completes without errors
2. All 40 layout.tsx files export metadata
3. `curl localhost:3000/sitemap.xml` returns valid XML with public routes only
4. `curl localhost:3000/robots.txt` shows allow / and disallow /api/, /auth/
5. manifest.json has 3 icon entries including 192x192
</verification>

<success_criteria>
- All 25 pages have metadata exports (titles visible in browser tabs)
- sitemap.xml accessible and lists all public routes
- robots.txt accessible and blocks crawlers from API/auth
- OG image shows in social share previews
- PWA manifest has 192x192 icon for Add to Home Screen
</success_criteria>

<output>
After completion, create `.planning/phases/10-accessibility-seo/10-01-SUMMARY.md`
</output>
