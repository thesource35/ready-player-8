---
phase: 21-live-satellite-traffic-maps
plan: 11
type: execute
wave: 4
depends_on:
  - 21-07
  - 21-08
  - 21-09
  - 21-10
files_modified:
  - .planning/phases/21-live-satellite-traffic-maps/21-UAT.md
  - .planning/phases/21-live-satellite-traffic-maps/21-VERIFICATION.md
autonomous: false
gap_closure: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04

must_haves:
  truths:
    - "All 16 UAT tests re-walked after Plans 21-07 through 21-10 have landed; fresh result/reported values captured per test"
    - "Each test outcome is one of: pass (observable behavior matches expected) | issue (still failing, requires new gap) | skipped-with-reason (user deferred, e.g. no device)"
    - "Tests that pass on re-walk flip the root_cause/debug_session references in 21-UAT.md Gaps block to status: closed"
    - "21-VERIFICATION.md status updates from human_needed to passed if ≥ 14/16 PASS (mirrors Phase 29 re-walk precedent — 16/16 pass flipped to closed)"
    - "Any remaining failures produce explicit new gap entries with updated root_cause hypotheses so a future --gaps run can close them"
    - "Plan 21-07 Task 5 vitest suite (PortalMapClient.test.tsx) is green BEFORE Test 15 can be marked pass on re-walk — regression coverage gates Test 15 closure"
  artifacts:
    - path: ".planning/phases/21-live-satellite-traffic-maps/21-UAT.md"
      provides: "Updated test results + Gaps block closed-vs-failed reconciliation"
      contains: "re-walk complete"
    - path: ".planning/phases/21-live-satellite-traffic-maps/21-VERIFICATION.md"
      provides: "Verification status flipped to passed or remaining human_needed items enumerated"
      contains: "status: passed"
  key_links:
    - from: "21-11 re-walk session"
      to: "21-07 token + UAT reconciliation + PortalMapClient regression test"
      via: "Tests 1, 2, 4, 15, 16 outcomes"
      pattern: "Test 1|Test 2|Test 4|Test 15|Test 16"
    - from: "21-11 re-walk session"
      to: "21-08 web code fixes + seed"
      via: "Tests 3, 5, 6 outcomes"
      pattern: "Test 3|Test 5|Test 6"
    - from: "21-11 re-walk session"
      to: "21-09 iOS data/error fixes"
      via: "Tests 8, 9, 11 outcomes"
      pattern: "Test 8|Test 9|Test 11"
    - from: "21-11 re-walk session"
      to: "21-10 iOS persistence + permission UX"
      via: "Tests 10, 12 outcomes"
      pattern: "Test 10|Test 12"
    - from: "21-11 Task 1 pre-check"
      to: "web/src/app/portal/[slug]/[project]/map/PortalMapClient.test.tsx"
      via: "npx vitest run green before Test 15 can be marked pass"
      pattern: "PortalMapClient.test.tsx"
---

<objective>
Gate the gap-closure milestone with a re-walk of all 16 UAT tests after Plans 21-07 through 21-10 have landed. This is the inverse of the original 16/16-issue UAT session — with root causes addressed, the expected outcome is most tests flip to pass. Any remaining failures get fresh root_cause hypotheses so future --gaps sessions start from evidence, not speculation.

Purpose: Close the loop. Plans 07-10 make claims ("tests 8, 9, 11 now pass"); this plan proves them by observation. Mirrors Phase 29 UAT re-walk precedent (commit 70fece6) where the same kind of 16/16-issue session flipped to 16/16 pass after a clean rebuild. Additionally enforces that Plan 21-07 Task 5's regression vitest (covering PortalMapClient's unconfigured-token fallback) is green before Test 15 can be marked pass — this pins the Test 15 gap bullet "Add unit test for unconfigured-token path" (21-UAT.md:381).
Output: Re-walked 21-UAT.md with per-test result/reported; 21-VERIFICATION.md status line updated; any new gaps explicitly recorded so the next planning cycle has clean inputs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-live-satellite-traffic-maps/21-UAT.md
@.planning/phases/21-live-satellite-traffic-maps/21-VERIFICATION.md
@.planning/phases/21-live-satellite-traffic-maps/21-07-PLAN.md
@.planning/phases/21-live-satellite-traffic-maps/21-08-PLAN.md
@.planning/phases/21-live-satellite-traffic-maps/21-09-PLAN.md
@.planning/phases/21-live-satellite-traffic-maps/21-10-PLAN.md

<interfaces>
Test-to-plan mapping for re-walk verification:

| Test | Expected Closer(s) | Primary Acceptance Observation |
|------|--------------------|--------------------------------|
| 1  | 21-07 Task 2 + Task 3 | 7-toggle strip visible + no "Maps Unavailable" card + reconciled expected text |
| 2  | 21-07 Task 2 | TRAFFIC toggle renders live traffic-v1 lines + legend |
| 3  | 21-08 Task 1 | 5 seeded equipment markers visible with typed shapes + status colors; click opens popup |
| 4  | 21-07 Task 2 | PHOTOS toggle renders/hides purple #8A8FCC markers cleanly (or shows empty-state if no GPS photos yet) |
| 5  | 21-08 Task 2 | Get Directions renders road-following polyline; error state visible if token missing |
| 6  | 21-08 Task 2 | Panned camera + toggle selections restore on refresh; geolocation does not clobber |
| 7  | 21-07 Task 4 | iOS TRAFFIC toggle shows MapKit traffic on .hybrid and .standard |
| 8  | 21-09 Tasks 1+2 | iOS equipment annotations visible near NYC-Midtown with SF Symbols + status colors + callouts |
| 9  | 21-09 Task 3 | iOS PHOTOS toggle shows purple camera.fill annotations OR "0 PHOTOS WITH GPS" chip |
| 10 | 21-10 Task 1 | All 7 overlays + camera persist across app background/quit/relaunch |
| 11 | 21-09 Task 1 | CHECK IN EQUIPMENT end-to-end write succeeds (no "Invalid table name"); visible success toast |
| 12 | 21-10 Task 2 | Location denial renders "Enable location in Settings" + Open Settings button |
| 13 | 21-07 Task 4 | MKDirections renders gold polyline + distance/ETA on success; "Route unavailable" fallback on error |
| 14 | 21-07 Task 2 (cache reset) | PortalCreateDialog MAP SETTINGS section renders with master + 4 sub-toggles |
| 15 | 21-07 Task 1 + Task 2 + Task 5 | /portal/{slug}/{project}/map renders Mapbox canvas with locked overlays (no toggle strip visible) AND vitest regression suite green |
| 16 | 21-07 Task 3 | Reconciled expected text: legacy portal home shows NO Map link; direct /map URL renders DEFAULT_MAP_OVERLAYS |
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Re-walk UAT Tests 1-6 + 14-16 (web /maps, portal /map, portal create) — gated by regression test green</name>
  <what-built>
Plans 21-07 and 21-08 closed the web-layer failures. This checkpoint asks you to re-walk Tests 1 through 6 and Tests 14 + 15 + 16 (all web-surface tests) and record outcomes.

Prerequisites verified before starting:
- Plan 21-07 Task 2 checkpoint "token landed" was approved.
- Plan 21-07 Task 3 UAT text is reconciled (Test 1 + Test 16 expected reflect shipped behavior).
- Plan 21-07 Task 5 vitest regression suite (`web/src/app/portal/[slug]/[project]/map/PortalMapClient.test.tsx`) passes — Test 15 CANNOT be marked pass unless this suite is green.
- Plan 21-08 seed migration is applied (`select count(*) from cs_equipment_latest_positions` ≥ 5).
- `npm run dev` is running on localhost:3000 with the populated token.

**Pre-check (executor action, no user input needed):** Before presenting this checkpoint to the user, the executor MUST run the regression vitest suite:

```
cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx vitest run src/app/portal/[slug]/[project]/map/PortalMapClient.test.tsx
```

If the suite exits non-zero, STOP and surface the failure — Plan 21-07 Task 5 did not land cleanly and Test 15 cannot be re-walked to pass. If the suite exits 0, proceed to present the manual re-walk checkpoint to the user below.
  </what-built>
  <how-to-verify>
**First: executor runs the automated pre-check.** Confirm the line below in the terminal output before presenting the manual checklist:
```
cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx vitest run src/app/portal/[slug]/[project]/map/PortalMapClient.test.tsx
```
Expected: 4 tests pass, 0 fail. If any fail, abort and surface the failure back to the user before proceeding.

**Then: user re-walks the 9 web tests.**

Open a fresh browser tab at http://localhost:3000/maps. Hard-refresh (Cmd+Shift+R). For each of Tests 1-6, 14, 15, 16 below, record outcome as **pass** | **issue** | **skip** with a one-line note. Paste the results back in this format:

```
Test 1 (cold start smoke): pass | issue | skip — <note>
Test 2 (TRAFFIC overlay): pass | issue | skip — <note>
...
```

**Test 1 — Cold start smoke test:**
- Expected: Mapbox satellite canvas renders. 7-toggle strip visible above: SATELLITE, THERMAL, CREWS, WEATHER, AUTO TRACK, TRAFFIC, PHOTOS. EQUIPMENT sidebar with filter chips. DELIVERY ROUTES cards with "Get Directions" buttons.
- Observe: browser console has no red errors. No "Maps Unavailable" placeholder.

**Test 2 — TRAFFIC overlay toggle:**
- Click TRAFFIC button (the 6th toggle). Observe: colored traffic lines appear over roads (green/yellow/orange/red congestion colors). Click again → lines disappear.

**Test 3 — Equipment markers with typed shapes:**
- Expected: 5 seeded markers visible clustered near Houston default center (29.7604, -95.3698). Shapes: circles for type='equipment', rounded-squares for 'vehicle', diamond for 'material'. Colors: green/gold/red by status. Click one → popup shows sanitized details.
- If map default center is elsewhere for /maps (e.g., user's geolocation), pan to 29.76, -95.37 to find the seeded cluster.

**Test 4 — GPS photo markers:**
- Click PHOTOS toggle. With no GPS photos in DB, no purple markers appear AND no console errors. Click again to hide. (If Phase 16 field-photo flow has captured photos, expect them to render as #8A8FCC circles.)

**Test 5 — Delivery route directions:**
- In DELIVERY ROUTES section, click "Get Directions" on any route. Observe: road-following polyline renders between start/end (not straight line). Distance + ETA display on the card. Click hide/show cleanly.
- Regression sub-test: temporarily blank NEXT_PUBLIC_MAPBOX_TOKEN in .env.local and restart dev server. Re-click "Get Directions". Card shows "Directions unavailable — Mapbox token not configured." (then restore the token).

**Test 6 — Overlay + camera persistence:**
- Toggle TRAFFIC on. Pan the map to a new area. Zoom in. Refresh the page (Cmd+R). Observe: TRAFFIC still on, camera position restored to where you panned. Do NOT confuse with geolocation — verify the camera matches your prior pan, not your browser's current location.

**Test 14 — Portal Create Dialog MAP SETTINGS:**
- From any page with the "Create Portal Link" dialog trigger, open the dialog. Scroll to MAP SETTINGS section. Observe: master "Show Map" checkbox + 4 sub-toggles (satellite, traffic, equipment, photos). Uncheck "Show Map" — sub-toggles hide. Create a test portal link with specific selections; re-open the link in DB or via GET to confirm sections_config.map_overlays stored.

**Test 15 — Portal public map page:**
- PREREQUISITE (executor-enforced): the PortalMapClient vitest suite above must be green. If it is, proceed.
- Visit the portal URL created in Test 14 at /portal/{slug}/{project}/map. Observe: Mapbox canvas renders with ONLY the configured overlays. No toggle strip visible. No admin controls. Pan/zoom work. Try to toggle overlays — nothing happens (expected: locked).
- Note: if the vitest suite was NOT green, mark Test 15 as **issue** regardless of browser observation — the regression coverage gate is part of the acceptance contract per 21-UAT.md:381.

**Test 16 — Portal backward compatibility (D-09 aware):**
- Find a portal link created before 2026-04-13 (Phase 21 ship date) or any link whose sections_config has no map_overlays field. Visit /portal/{slug}/{project}. Observe: NO Map link in desktop header nav, NO MapPin icon in mobile bottom nav (per D-09).
- Now visit /portal/{slug}/{project}/map directly by URL. Observe: map renders with DEFAULT_MAP_OVERLAYS (satellite on, traffic off, equipment off, photos on). No crash, no missing-field errors.

**If Test 14 fails because you don't have a test portal to create from:** mark as "skip — no test portal creation access in this session". That's fine — the code was verified by Phase 21-06 VERIFICATION.md.
  </how-to-verify>
  <resume-signal>Paste the 9 test outcomes (Test 1-6, 14-16) in the "Test N (name): pass|issue|skip — note" format. Also include a one-line note confirming the PortalMapClient vitest suite ran green ("vitest: 4/4 pass") or failed ("vitest: N/4 — abort"). Executor will patch 21-UAT.md with your results and proceed to Task 2.</resume-signal>
  <files>checkpoint-only — no files edited by executor; human-in-the-loop action (executor does run an automated vitest pre-check, but the outcome is used to gate the checkpoint, not to modify files)</files>
  <action>See &lt;what-built&gt; and &lt;how-to-verify&gt; above. Executor: FIRST run the PortalMapClient vitest regression suite as the automated pre-check. If it fails, surface the failure and stop. If it passes, THEN present the manual checkpoint to the user and pause for the resume-signal. Do not attempt to automate the manual UI-observation steps.</action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx vitest run src/app/portal/[slug]/[project]/map/PortalMapClient.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>PortalMapClient.test.tsx vitest suite exits 0 with 4/4 passing. User has provided the resume-signal with 9 web-test outcomes AND the vitest confirmation line. No file edits performed by this checkpoint beyond the user-provided outcomes being captured for Task 3's patches.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-walk UAT Tests 7-13 (iOS on simulator/device)</name>
  <what-built>
Plans 21-07 Task 4 (clean iOS rebuild), 21-09, and 21-10 closed the iOS-layer failures. This checkpoint re-walks Tests 7 through 13 on a running iOS simulator or device.

Prerequisites verified before starting:
- Plan 21-07 Task 4 checkpoint "ios clean build verified" was approved.
- The iOS app is installed from a fresh build of HEAD post-21-10 (every plan committed).
- For Tests 11/12: Supabase credentials configured in the app via Integration Hub (for a real CHECK IN flow) OR accept "unconfigured" mode behavior (mocks visible).
  </what-built>
  <how-to-verify>
Open the Construction Command app on your iPhone simulator or device. Navigate to the MAPS tab. Paste outcomes back as:
```
Test 7 (iOS TRAFFIC): pass | issue | skip — <note>
Test 8 (iOS equipment): pass | issue | skip — <note>
...
```

**Test 7 — iOS TRAFFIC toggle:**
- Tap TRAFFIC in the toggle strip. Observe: MapKit traffic lines appear on roads. Tap SATELLITE off/on — traffic colors render on both .standard and .hybrid styles. Tap TRAFFIC off — lines disappear.

**Test 8 — iOS equipment annotations:**
- With the app in unconfigured-Supabase mode: 4 mock equipment pins visible clustered near NYC-Midtown (40.758, -73.9855). Shapes: gearshape.fill (equipment), truck.box.fill (vehicle), shippingbox.fill (material), colored circles by status. Tap a pin → callout shows name + status.
- If the default map center is elsewhere in MapsView, pan to NYC-Midtown to find the cluster. If Supabase IS configured and has real cs_equipment_locations rows, those render instead (which is the intended production behavior).

**Test 9 — iOS PHOTOS toggle:**
- Tap PHOTOS. In unconfigured mode: 3 mock photo pins visible (purple camera.fill). In configured-empty mode: "0 PHOTOS WITH GPS" chip renders above/near the map. Tap a pin (if present) → callout shows filename + date.

**Test 10 — iOS persistence across launches:**
- Toggle SATELLITE off, TRAFFIC on, AUTO TRACK off. Pan the map to a specific region; zoom in. Press Home (swipe up from bottom) to background the app. Force-quit from the app-switcher (swipe up on the card). Reopen the app → MAPS tab. Observe: SATELLITE still off, TRAFFIC still on, AUTO TRACK still off, camera position matches your prior pan. Repeat with different toggle combos to confirm all 7 persist.

**Test 11 — iOS CHECK IN EQUIPMENT end-to-end:**
- Requires Supabase credentials configured in Integration Hub for a real write test.
- Tap CHECK IN EQUIPMENT button on MAPS. Sheet opens. Equipment picker populates. Live GPS coordinates + accuracy display. First time: iOS prompts for "When In Use" location permission — tap Allow.
- Select an equipment row. Tap Confirm Location.
- Observe: sheet dismisses, top-of-screen green toast "Location updated for {name}" appears and auto-dismisses after ~3 seconds. Map refreshes showing the new position.
- If Supabase unconfigured: attempt the flow; the submit should surface a visible AppError-shaped message in the sheet (not silently fail).

**Test 12 — iOS location permission denial:**
- Open Settings → Construction Command → Location → Never.
- Return to app → MAPS → CHECK IN EQUIPMENT.
- Observe: sheet shows "Enable location in Settings to check in equipment." + "Open Settings" button (in the LOCATION section). NO ambiguous "GPS signal unavailable" copy. Confirm Location button is disabled.
- Tap Open Settings → iOS Settings opens to Construction Command's Location pane. Flip to "While Using" → return to app. Denial branch disappears, GPS locks, Confirm Location enables.

**Test 13 — iOS DELIVERY ROUTES MKDirections:**
- Scroll to DELIVERY ROUTES section in the sidebar.
- Tap "Get Directions" on any route card. Observe: card shows "Computing route..." briefly, then a gold polyline renders on the map and the card shows distance (miles) + ETA (minutes).
- For a route with unreachable coordinates (if available), observe "Route unavailable. Showing straight-line connection." fallback.
  </how-to-verify>
  <resume-signal>Paste the 7 test outcomes (Tests 7-13) in the "Test N (name): pass|issue|skip — note" format. If any test fails, include a one-paragraph description of the observed behavior vs expected so a future gap-closure session has evidence.</resume-signal>
  <files>checkpoint-only — no files edited by executor; human-in-the-loop action</files>
  <action>See &lt;what-built&gt; and &lt;how-to-verify&gt; above. Executor: present the checkpoint to the user and pause for the resume-signal. Do not attempt to automate steps marked as human-only.</action>
  <verify>
    <automated>MISSING — checkpoint task verified by human resume-signal, not automated command</automated>
  </verify>
  <done>User has provided the resume-signal described in &lt;resume-signal&gt; above. No code artifacts produced by this checkpoint task.</done>
</task>

<task type="auto">
  <name>Task 3: Patch 21-UAT.md + 21-VERIFICATION.md with re-walk outcomes</name>
  <files>.planning/phases/21-live-satellite-traffic-maps/21-UAT.md, .planning/phases/21-live-satellite-traffic-maps/21-VERIFICATION.md</files>
  <read_first>
    .planning/phases/21-live-satellite-traffic-maps/21-UAT.md,
    .planning/phases/21-live-satellite-traffic-maps/21-VERIFICATION.md
  </read_first>
  <action>
**Consume the outcome lists** from Tasks 1 and 2 (16 outcomes total — Tests 1-16, noting that Task 1 covers 9 tests and Task 2 covers 7).

**1. Update 21-UAT.md Tests section:**

For each test reported as **pass**:
- Change `result:` from `issue` (or `pending` for Tests 1, 16) to `pass`.
- Change `reported:` to `passed on re-walk`.
- Append to `note:`: ` | Closed by Plan 21-XX on 2026-04-21.` (fill XX with the plan that closed it per the test-to-plan mapping in interfaces).
- For Test 15 specifically: the closer annotation MUST include both the code coercion AND the regression test — format: ` | Closed by Plan 21-07 Task 1 (coercion) + Task 5 (vitest regression) on 2026-04-21.`

For each test reported as **issue**:
- Keep `result: issue`.
- Update `reported:` to the user's note from the re-walk.
- Append to `note:`: ` | Re-walk 2026-04-21 STILL failing — regenerate root_cause in next --gaps session.`

For each test reported as **skip**:
- Change `result:` to `skipped`.
- Update `reported:` to the user's skip reason.

**2. Update 21-UAT.md Summary block (lines 117-124):**

Replace:
```
total: 16
passed: 0
issues: 16
pending: 0
skipped: 0
blocked: 0
```
with actual counts from the outcome lists. Confirm `total = passed + issues + pending + skipped + blocked` equals 16.

**3. Update 21-UAT.md Gaps block:**

For every test that flipped to **pass**, locate the corresponding `truth:` entry in the Gaps block and change `status: failed` → `status: closed`. Add a `closed_by:` line below `status:`:
```
closed_by: "21-07 + 21-08 (token + camera race + empty-state chip)"
```

For Test 15's gap entry specifically, also confirm the `missing:` bullet "Add unit test for unconfigured-token path" (line 381) has been addressed. Append to `closed_by:` the note that the bullet is satisfied by `web/src/app/portal/[slug]/[project]/map/PortalMapClient.test.tsx`. Format:
```
closed_by: "21-07 Task 1 (server coercion) + 21-07 Task 5 (PortalMapClient.test.tsx — closes the 'Add unit test for unconfigured-token path' bullet at line 381)"
```

For tests still **issue**, keep `status: failed` but update `reason:` with the user's new observation and null out the `root_cause:` (set to `"TBD — re-walk 2026-04-21 requires fresh diagnosis"`) so the next /gsd-debug or /gsd-plan-phase --gaps run starts from observation, not stale hypothesis.

**4. Update 21-UAT.md frontmatter:**
- `status:` → `closed` if all 16 are pass/skipped, else keep `diagnosed`.
- `updated:` → today's ISO timestamp.

**5. Update 21-VERIFICATION.md frontmatter (lines 2-6):**
- If ≥ 14/16 pass (matching Phase 29 precedent threshold where a re-walk of the same scope flipped human_needed → passed), change `status: human_needed` → `status: passed` and add `re_verified: 2026-04-21T{now}Z` on a new line.
- If < 14/16 pass, keep `status: human_needed` and append a "## Re-walk Results (2026-04-21)" section at the bottom of the file enumerating remaining items.

**6. Append a "## Re-walk Summary (2026-04-21)" section at the bottom of 21-UAT.md:**

```markdown
## Re-walk Summary (2026-04-21)

Re-walked all 16 tests after Plans 21-07 through 21-10 landed.

| Test | Before (2026-04-21 diagnosis) | After (2026-04-21 re-walk) | Closer |
|------|-------------------------------|----------------------------|--------|
| 1    | issue (token empty + UAT mismatch) | {pass/issue/skip}     | 21-07 |
| 2    | issue (cascade of Test 1)          | {pass/issue/skip}     | 21-07 |
| ...  | ...                                | ...                    | ...   |
| 15   | issue (empty token + no unit test)  | {pass/issue/skip}     | 21-07 Task 1 + Task 5 |

Remaining open gaps: {list Test IDs that are still `issue`, or "none".}

Regression coverage: `web/src/app/portal/[slug]/[project]/map/PortalMapClient.test.tsx` (4 cases, green) pins the PortalMapClient fallback-card behavior for empty/whitespace/undefined `mapboxToken` props. This closes the Test 15 gap bullet at 21-UAT.md:381 "Add unit test for unconfigured-token path".

This re-walk mirrors Phase 29's 16/16-issue → 16/16-pass flip pattern (commit 70fece6) where the root cause was environmental/build rather than code, and resolved after rebuilds + token landing.
```

Fill in the table rows from the actual Task 1 + Task 2 outcomes.

**Acceptance:** grep in the updated file must show `result: pass` count equal to the number of passes reported by the user, and every `status: closed` gap truth in the Gaps block must reference a `closed_by:` plan number. Test 15's `closed_by:` must explicitly name `21-07 Task 5` (or equivalent reference to `PortalMapClient.test.tsx`).
  </action>
  <verify>
    <automated>grep -c "result: pass\|result: passed" "/Users/beverlyhunter/Desktop/ready player 8/.planning/phases/21-live-satellite-traffic-maps/21-UAT.md" && grep -c "Re-walk Summary" "/Users/beverlyhunter/Desktop/ready player 8/.planning/phases/21-live-satellite-traffic-maps/21-UAT.md" && grep -c "PortalMapClient.test.tsx\|Task 5" "/Users/beverlyhunter/Desktop/ready player 8/.planning/phases/21-live-satellite-traffic-maps/21-UAT.md" && grep -c "closed_by\|re_verified\|status: passed" "/Users/beverlyhunter/Desktop/ready player 8/.planning/phases/21-live-satellite-traffic-maps/21-VERIFICATION.md"</automated>
  </verify>
  <done>21-UAT.md reflects 16 fresh test outcomes. Summary counts updated. Gaps block reconciled (closed vs still-failed). Test 15's closed_by references Task 5 + PortalMapClient.test.tsx explicitly (closing the line-381 bullet). 21-VERIFICATION.md status flipped to `passed` with re_verified timestamp if ≥ 14/16 pass; otherwise remaining human_needed items enumerated. "Re-walk Summary (2026-04-21)" table appended to 21-UAT.md with regression-coverage note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human observation → planning artifact | Single source of truth for test outcomes; asymmetric — user has direct observation, Claude has only reports |
| Re-walk timestamp → audit trail | Timestamp + plan closer reference creates a reproducible chain of cause and closure |
| Vitest regression suite → Test 15 acceptance | Automated pre-check gates manual re-walk; a failing suite blocks Test 15 from being marked pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-31 | Repudiation | Re-walk outcome recording | mitigate | Every closed gap references the specific Plan number (21-07/08/09/10) that closed it; fresh "issue" outcomes carry fresh root_cause=TBD forcing the next session to re-diagnose from observation; Test 15's closed_by additionally cites PortalMapClient.test.tsx to establish regression-coverage chain of custody |
| T-21-32 | Tampering | Manual outcome transcription | accept | Single-user, single-session workflow; user has incentive to record truth because lingering false passes block milestone progression |
| T-21-34 | Bypass | Test 15 marked pass without regression coverage | mitigate | Task 1's automated pre-check runs PortalMapClient.test.tsx and will surface failure BEFORE presenting the manual checkpoint; user must include vitest-confirmation line in resume-signal so Task 3 patch cannot mark Test 15 pass without the green suite |
</threat_model>

<verification>
- Task 1 automated pre-check exits 0 (PortalMapClient.test.tsx green) BEFORE Test 15 can be evaluated.
- Task 1 outcome count + Task 2 outcome count = 16 (all tests covered).
- Frontmatter `updated:` in 21-UAT.md is today's date.
- Summary block total count matches sum across passed/issues/skipped/pending/blocked.
- Every test marked `pass` has a `closed_by:` line in its Gaps block truth entry.
- Test 15's `closed_by:` line explicitly references `21-07 Task 5` / `PortalMapClient.test.tsx` to document that 21-UAT.md:381's "Add unit test for unconfigured-token path" bullet is closed.
- Every test still `issue` has a fresh `reason:` + `root_cause: TBD` reflecting observation from re-walk.
</verification>

<success_criteria>
- Fresh outcomes captured for all 16 tests via the two checkpoint tasks.
- PortalMapClient regression vitest suite green before Test 15 can be recorded as pass.
- 21-UAT.md fully reconciled: results updated, Gaps block flipped to closed where appropriate (Test 15 citing Task 5 explicitly), Summary block re-counted, "Re-walk Summary" table appended with regression-coverage note.
- 21-VERIFICATION.md status line either flipped to `passed` with `re_verified:` timestamp (≥ 14/16 pass, matching Phase 29 threshold) OR keeps `human_needed` with a new "Re-walk Results" section listing the residual items.
- Any remaining failures carry `root_cause: TBD — re-walk 2026-04-21 requires fresh diagnosis` so the next planning cycle starts from observation, not stale hypothesis.
</success_criteria>

<output>
After completion, create `.planning/phases/21-live-satellite-traffic-maps/21-11-SUMMARY.md` documenting the re-walk counts, which plans closed which tests (with Test 15's regression-coverage chain noted), and any residual work.
</output>
