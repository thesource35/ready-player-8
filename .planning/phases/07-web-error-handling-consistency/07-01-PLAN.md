---
phase: 07-web-error-handling-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/api/chat/route.ts
  - web/src/app/api/export/route.ts
  - web/src/app/api/jobs/route.ts
  - web/src/instrumentation.ts
autonomous: true
requirements: [WERR-01, WERR-02, WERR-03, WERR-05, WERR-07, CONSIST-01, CONSIST-04]

must_haves:
  truths:
    - "Chat route uses AI SDK streamText instead of raw fetch to Anthropic API"
    - "Chat route uses shared rate limiter from @/lib/rate-limit instead of inline duplicate"
    - "Chat route returns distinct error messages for API-unavailable vs temporary-error"
    - "Export route returns 503 with error JSON when Supabase is not configured"
    - "Jobs GET validates response shape before using data"
    - "Missing env vars are logged at server startup via Next.js instrumentation hook"
  artifacts:
    - path: "web/src/app/api/chat/route.ts"
      provides: "AI SDK chat streaming endpoint"
      contains: "streamText"
    - path: "web/src/app/api/export/route.ts"
      provides: "Data export with proper 503 on missing config"
      contains: "status: 503"
    - path: "web/src/instrumentation.ts"
      provides: "Startup env var validation"
      contains: "validateRequiredEnvVars"
  key_links:
    - from: "web/src/app/api/chat/route.ts"
      to: "@ai-sdk/anthropic"
      via: "import createAnthropic"
      pattern: "import.*@ai-sdk/anthropic"
    - from: "web/src/app/api/chat/route.ts"
      to: "web/src/lib/rate-limit.ts"
      via: "import checkRateLimit"
      pattern: "import.*checkRateLimit.*rate-limit"
---

<objective>
Overhaul the chat route to use AI SDK, fix export route demo-data-as-200 bug, add Jobs response validation, and wire env var startup logging via Next.js instrumentation.

Purpose: Eliminates the duplicate rate limiter in chat, replaces fragile raw-fetch streaming with the AI SDK already in package.json, ensures export API returns proper 503 instead of fake data, and validates env vars at startup.
Output: 4 files modified/created covering 7 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@web/src/app/api/chat/route.ts
@web/src/app/api/export/route.ts
@web/src/app/api/jobs/route.ts
@web/src/lib/rate-limit.ts
@web/src/lib/supabase/env.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From web/src/lib/rate-limit.ts:
```typescript
export function checkRateLimit(ip: string, limit?: number, windowMs?: number): boolean;
export function getRateLimitHeaders(ip: string, limit?: number): Record<string, string>;
```

From web/src/lib/supabase/env.ts:
```typescript
export function validateRequiredEnvVars(): void;
export function isSupabaseConfigured(): boolean;
```

AI SDK packages already in package.json:
- "@ai-sdk/anthropic": "^3.0.64"
- "ai": "^6.0.142"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite chat route with AI SDK and shared rate limiter</name>
  <files>web/src/app/api/chat/route.ts</files>
  <read_first>web/src/app/api/chat/route.ts, web/src/lib/rate-limit.ts</read_first>
  <action>
Rewrite web/src/app/api/chat/route.ts to:

1. CONSIST-01: Remove the inline duplicate rate limiter (lines 4-19). Import `checkRateLimit` and `getRateLimitHeaders` from `@/lib/rate-limit` instead.

2. CONSIST-04: Replace raw `fetch("https://api.anthropic.com/v1/messages", ...)` with AI SDK. Use:
   ```typescript
   import { createAnthropic } from "@ai-sdk/anthropic";
   import { streamText } from "ai";
   ```
   Create the provider with `createAnthropic({ apiKey })`. Call `streamText({ model: anthropic("claude-haiku-4-5-20251001"), system: systemPrompt, messages })`. The model ID "claude-haiku-4-5-20251001" is the Anthropic model identifier -- keep it exactly as-is (the SDK passes it through).

   Return `result.toTextStreamResponse()` -- this endpoint streams raw text chunks to the client (not a chat UI protocol), so use `toTextStreamResponse()` which is the correct AI SDK v6 method for plain text streaming.

3. WERR-02: In the catch block, distinguish errors:
   - If error has status 401 or 403 or message contains "auth" or "key": return `{ error: "AI service configuration error — check API key" }` with status 503
   - If error has status 429: return `{ error: "AI service temporarily overloaded — please retry in a moment" }` with status 429
   - If error has status >= 500 or is a network error: return `{ error: "AI service temporarily unavailable — please retry" }` with status 502
   - Default: return `{ error: "Failed to process AI request" }` with status 500

4. WERR-01: All error responses must be structured JSON: `{ error: string }`.

5. WERR-06: Wrap `await req.json()` in try/catch. On parse failure return `{ error: "Invalid request body" }` with status 400.

6. Keep the existing systemPrompt string as-is. Keep CSRF check. Keep rate limit at 20/min.

7. Include rate limit headers on responses using getRateLimitHeaders().
  </action>
  <verify>
    <automated>cd /Users/beverlyhunter/Desktop/ready\ player\ 8/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "import.*checkRateLimit.*rate-limit" web/src/app/api/chat/route.ts returns match
    - grep "import.*streamText.*from.*ai" web/src/app/api/chat/route.ts returns match
    - grep "import.*createAnthropic.*@ai-sdk/anthropic" web/src/app/api/chat/route.ts returns match
    - grep -c "new Map" web/src/app/api/chat/route.ts returns 0 (no inline rate limiter)
    - grep "AI service temporarily unavailable" web/src/app/api/chat/route.ts returns match (WERR-02 distinction)
    - grep "AI service configuration error" web/src/app/api/chat/route.ts returns match (WERR-02 distinction)
    - grep "Invalid request body" web/src/app/api/chat/route.ts returns match (WERR-06)
  </acceptance_criteria>
  <done>Chat route uses AI SDK streamText, shared rate limiter, try/catch on req.json(), and distinct error messages for unavailable vs temporary errors</done>
</task>

<task type="auto">
  <name>Task 2: Fix export 503, Jobs response validation, and env startup instrumentation</name>
  <files>web/src/app/api/export/route.ts, web/src/app/api/jobs/route.ts, web/src/instrumentation.ts</files>
  <read_first>web/src/app/api/export/route.ts, web/src/app/api/jobs/route.ts, web/src/lib/supabase/env.ts</read_first>
  <action>
1. WERR-07 -- Export route (web/src/app/api/export/route.ts):
   Replace the `if (!url || !key)` block that currently returns demo export data with 200. Instead return:
   ```typescript
   return NextResponse.json(
     { error: "Export unavailable -- database not configured" },
     { status: 503 }
   );
   ```
   Also add auth check: import `getAuthenticatedClient` from `@/lib/supabase/fetch`. Before the Supabase config check, verify user is authenticated. If not, return 401. This prevents unauthenticated data export. Wrap the entire export logic in try/catch returning 500 on unexpected errors.

2. WERR-05 -- Jobs route (web/src/app/api/jobs/route.ts):
   In the GET handler, after the Supabase query returns `data`, validate the response shape before passing to `parseJobListing`. Add a guard:
   ```typescript
   if (!Array.isArray(data)) {
     console.error("[Jobs] Unexpected response shape:", typeof data);
     return NextResponse.json({ jobs: getFallbackJobs(), source: "sample" });
   }
   ```
   This goes right after `if (error)` check, before the `const liveJobs = ...` line.

3. WERR-03 -- Create web/src/instrumentation.ts:
   ```typescript
   export async function register() {
     if (process.env.NEXT_RUNTIME === "nodejs") {
       const { validateRequiredEnvVars } = await import("@/lib/supabase/env");
       validateRequiredEnvVars();
     }
   }
   ```
   This is the Next.js instrumentation hook -- it runs once at server startup. The `validateRequiredEnvVars` function in env.ts already logs missing required and optional vars.
  </action>
  <verify>
    <automated>cd /Users/beverlyhunter/Desktop/ready\ player\ 8/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "status: 503" web/src/app/api/export/route.ts returns match
    - grep -v "Demo export" web/src/app/api/export/route.ts confirms demo data removed (grep returns all lines, none say "Demo export")
    - grep "Array.isArray" web/src/app/api/jobs/route.ts returns match
    - test -f web/src/instrumentation.ts confirms file exists
    - grep "validateRequiredEnvVars" web/src/instrumentation.ts returns match
  </acceptance_criteria>
  <done>Export returns 503 when Supabase not configured (not fake data), Jobs validates response shape, env vars validated at startup via instrumentation hook</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> chat API | Untrusted user messages sent to AI |
| chat API -> Anthropic | API key used for external service call |
| client -> export API | Authenticated user requesting bulk data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | I (Info Disclosure) | chat/route.ts | mitigate | AI SDK errors logged server-side only; client gets generic "temporarily unavailable" |
| T-07-02 | I (Info Disclosure) | export/route.ts | mitigate | Auth required before export; 503 if unconfigured instead of leaking demo schema |
| T-07-03 | D (Denial of Service) | chat/route.ts | mitigate | Shared rate limiter (20/min/IP) applied before AI SDK call |
</threat_model>

<verification>
1. `cd web && npx tsc --noEmit` passes with no errors
2. `grep -r "new Map.*rateLimit\|const rateLimit = new Map" web/src/app/api/chat/route.ts` returns no matches (duplicate removed)
3. `grep "streamText" web/src/app/api/chat/route.ts` returns match
4. `grep "503" web/src/app/api/export/route.ts` returns match
</verification>

<success_criteria>
- Chat route uses AI SDK (@ai-sdk/anthropic + ai streamText) instead of raw fetch
- Chat route uses shared rate limiter from @/lib/rate-limit (no duplicate)
- Chat route distinguishes API-unavailable vs temporary-error in responses
- Export route returns 503 when Supabase not configured
- Jobs API validates response body shape
- Env vars validated at server startup via instrumentation.ts
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-error-handling-consistency/07-01-SUMMARY.md`
</output>
