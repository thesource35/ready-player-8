---
phase: 07-web-error-handling-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/api/projects/route.ts
  - web/src/app/api/contracts/route.ts
  - web/src/app/api/punch/route.ts
  - web/src/app/api/feed/route.ts
  - web/src/app/api/tasks/route.ts
autonomous: true
requirements: [WERR-01, WERR-06, WERR-08, WERR-09, CONSIST-02]

must_haves:
  truths:
    - "All POST routes in projects, contracts, punch, feed, and tasks return 201 on successful creation"
    - "All req.json() calls in projects, contracts, punch, feed, and tasks are wrapped in try/catch"
    - "DELETE routes return proper error status (not 200 with success:false) when deletion fails"
    - "All routes use getAuthenticatedClient() for auth (no ad-hoc patterns)"
  artifacts:
    - path: "web/src/app/api/projects/route.ts"
      provides: "Projects CRUD with 201, req.json try/catch, DELETE error status"
      contains: "status: 201"
    - path: "web/src/app/api/contracts/route.ts"
      provides: "Contracts CRUD with 201 and req.json try/catch"
      contains: "status: 201"
    - path: "web/src/app/api/punch/route.ts"
      provides: "Punch CRUD with 201 and req.json try/catch"
      contains: "status: 201"
    - path: "web/src/app/api/feed/route.ts"
      provides: "Feed CRUD with 201 and req.json try/catch"
      contains: "status: 201"
    - path: "web/src/app/api/tasks/route.ts"
      provides: "Tasks CRUD with 201, req.json try/catch, DELETE error status"
      contains: "status: 201"
  key_links:
    - from: "web/src/app/api/projects/route.ts"
      to: "web/src/lib/supabase/fetch.ts"
      via: "import getAuthenticatedClient"
      pattern: "getAuthenticatedClient"
---

<objective>
Harden all CRUD API routes with req.json() try/catch wrappers, correct HTTP status codes (201 for creation, proper error codes for DELETE failures), and consistent auth patterns.

Purpose: Ensures malformed JSON bodies never crash routes, POST creates return correct 201 status, and DELETE failures return error status codes instead of misleading 200 with success:false.
Output: 5 route files modified covering 5 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@web/src/app/api/projects/route.ts
@web/src/app/api/contracts/route.ts
@web/src/app/api/punch/route.ts
@web/src/app/api/feed/route.ts
@web/src/app/api/tasks/route.ts
@web/src/lib/supabase/fetch.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From web/src/lib/supabase/fetch.ts:
```typescript
export async function getAuthenticatedClient(): Promise<{ supabase: SupabaseClient | null; user: User | null }>;
export async function fetchTable<T>(table: string, options?: { ... }): Promise<T[]>;
export async function insertRow<T>(table: string, row: Partial<T>): Promise<T | null>;
export async function updateOwnedRow<T>(table: string, id: string, userId: string, updates: Partial<T>): Promise<T | null>;
export async function deleteOwnedRow(table: string, id: string, userId: string): Promise<boolean>;
```

From web/src/lib/rate-limit.ts:
```typescript
export function checkRateLimit(ip: string, limit?: number, windowMs?: number): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap req.json() in try/catch and fix POST 201 status on projects, contracts, and feed routes</name>
  <files>web/src/app/api/projects/route.ts, web/src/app/api/contracts/route.ts, web/src/app/api/feed/route.ts</files>
  <read_first>web/src/app/api/projects/route.ts, web/src/app/api/contracts/route.ts, web/src/app/api/feed/route.ts</read_first>
  <action>
For each of these three route files, apply the same pattern:

**WERR-06 -- Wrap req.json() in try/catch:**
Every `await req.json()` call that is NOT already in a try/catch must be wrapped. The pattern:
```typescript
let body: Record<string, unknown>;
try {
  body = await req.json();
} catch {
  return NextResponse.json({ error: "Invalid JSON payload" }, { status: 400 });
}
```

Specific locations:
- projects/route.ts POST (line 34): `const body = await req.json()` -- wrap in try/catch
- projects/route.ts DELETE (line 69): `const { id } = await req.json()` -- wrap in try/catch, destructure after
- contracts/route.ts POST (line 34): `const body = await req.json()` -- wrap in try/catch
- feed/route.ts POST (line 35): `const body = await req.json()` -- wrap in try/catch

**WERR-08 -- POST returns 201:**
- projects/route.ts POST: Change `return NextResponse.json(project)` to `return NextResponse.json(project, { status: 201 })`
- contracts/route.ts POST: Change `return NextResponse.json(contract)` to `return NextResponse.json(contract, { status: 201 })`
- feed/route.ts POST: Change `return NextResponse.json(post)` to `return NextResponse.json(post, { status: 201 })`

**WERR-09 -- DELETE error status (projects/route.ts only):**
The projects DELETE currently returns `{ error: "Not found or not owned" }` with 404 when `deleteOwnedRow` returns false. This is already correct (404 is proper). But verify the route does NOT return `{ success: false }` with 200 anywhere -- if it does, change to proper error status.

**WERR-01 -- Verify all error paths return structured JSON:**
All error responses must use `{ error: string }` format. The existing routes mostly do this already -- confirm and fix any that don't.
  </action>
  <verify>
    <automated>cd /Users/beverlyhunter/Desktop/ready\ player\ 8/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "status: 201" web/src/app/api/projects/route.ts returns match
    - grep "status: 201" web/src/app/api/contracts/route.ts returns match
    - grep "status: 201" web/src/app/api/feed/route.ts returns match
    - grep "Invalid JSON payload" web/src/app/api/projects/route.ts returns match
    - grep "Invalid JSON payload" web/src/app/api/contracts/route.ts returns match
    - grep "Invalid JSON payload" web/src/app/api/feed/route.ts returns match
  </acceptance_criteria>
  <done>Projects, contracts, and feed POST routes return 201 on creation; all req.json() calls wrapped in try/catch; all errors are structured JSON</done>
</task>

<task type="auto">
  <name>Task 2: Wrap req.json() and fix status codes on punch and tasks routes</name>
  <files>web/src/app/api/punch/route.ts, web/src/app/api/tasks/route.ts</files>
  <read_first>web/src/app/api/punch/route.ts, web/src/app/api/tasks/route.ts</read_first>
  <action>
**WERR-06 -- Wrap req.json() in try/catch:**

punch/route.ts has 2 unwrapped calls:
- POST (line 34): `const body = await req.json()` -- wrap in try/catch
- PATCH (line 66): `const { id, ...updates } = await req.json()` -- wrap in try/catch, destructure after

tasks/route.ts has 3 unwrapped calls:
- POST (line 38): `const body = await req.json()` -- wrap in try/catch
- PATCH (line 63): `const body = await req.json()` -- wrap in try/catch
- DELETE (line 91): `const { table, id } = await req.json()` -- wrap in try/catch, destructure after

Same pattern as Task 1:
```typescript
let body: Record<string, unknown>;
try {
  body = await req.json();
} catch {
  return NextResponse.json({ error: "Invalid JSON payload" }, { status: 400 });
}
```

**WERR-08 -- POST returns 201:**
- punch/route.ts POST: Change `return NextResponse.json(item)` to `return NextResponse.json(item, { status: 201 })`
- tasks/route.ts POST: Change `return NextResponse.json(row)` to `return NextResponse.json(row, { status: 201 })`

**WERR-09 -- DELETE error status (tasks/route.ts):**
tasks/route.ts DELETE currently returns `{ success: true }` with 200 on success and `{ error: "Not found or not owned" }` with 404 on failure. This is already correct. Verify no path returns `{ success: false }` with 200.

**CONSIST-02 -- Auth pattern consistency:**
All routes already use `getAuthenticatedClient()` from `@/lib/supabase/fetch`. Verify this is consistent across all handlers (POST, PATCH, DELETE) in both files. No changes expected here -- just verification.
  </action>
  <verify>
    <automated>cd /Users/beverlyhunter/Desktop/ready\ player\ 8/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "status: 201" web/src/app/api/punch/route.ts returns match
    - grep "status: 201" web/src/app/api/tasks/route.ts returns match
    - grep "Invalid JSON payload" web/src/app/api/punch/route.ts returns match
    - grep "Invalid JSON payload" web/src/app/api/tasks/route.ts returns match
    - grep -c "getAuthenticatedClient" web/src/app/api/tasks/route.ts returns 3 (POST, PATCH, DELETE)
    - grep -c "getAuthenticatedClient" web/src/app/api/punch/route.ts returns 2 (POST, PATCH)
  </acceptance_criteria>
  <done>Punch and tasks routes have req.json() try/catch, POST returns 201, DELETE has proper error status, auth pattern is consistent</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> CRUD APIs | Untrusted JSON bodies from client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | T (Tampering) | all CRUD routes | mitigate | req.json() wrapped in try/catch -- malformed bodies return 400, not unhandled exception |
| T-07-05 | I (Info Disclosure) | all CRUD routes | mitigate | Error responses use structured JSON with generic messages, no stack traces |
</threat_model>

<verification>
1. `cd web && npx tsc --noEmit` passes with no errors
2. `grep -r "status: 201" web/src/app/api/{projects,contracts,punch,feed,tasks}/route.ts` returns 5 matches (one per POST)
3. `grep -r "Invalid JSON payload" web/src/app/api/{projects,contracts,punch,feed,tasks}/route.ts` returns matches in all 5 files
</verification>

<success_criteria>
- All 10 unwrapped req.json() calls across 5 route files are now in try/catch blocks
- All 5 POST handlers return 201 for successful creation
- DELETE handlers return proper error status codes (never 200 with success:false)
- All routes use consistent getAuthenticatedClient() pattern
- All error responses are structured JSON
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-error-handling-consistency/07-02-SUMMARY.md`
</output>
