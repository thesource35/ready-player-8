---
phase: 01-secrets-infrastructure-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - web/package.json
  - web/package-lock.json
  - ready player 8.xcodeproj/project.pbxproj
autonomous: false
requirements:
  - SEC-08
  - SEC-09
  - SEC-10
  - SEC-11
  - INFRA-01

user_setup:
  - service: supabase
    why: "Supabase secret key was committed to git history and must be rotated"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role key -> Generate new key (or copy after reset)"
    dashboard_config:
      - task: "Rotate the service_role API key"
        location: "Supabase Dashboard -> Project Settings -> API -> service_role (secret) -> Click 'Generate new key' or regenerate via Supabase CLI"

must_haves:
  truths:
    - "User has rotated Supabase secret key in dashboard and updated .env.local with new value"
    - ".gitignore covers .env*, .DS_Store, xcuserdata/, DerivedData/, *.pem"
    - "@next/swc-darwin-arm64 is not in package.json dependencies"
    - "Orphaned 'untitled folder/' and 'web/src/.next/' are removed from disk"
    - "iOS deployment target is 18.2 (not invalid 26.2) in all build configurations"
  artifacts:
    - path: ".gitignore"
      provides: "Comprehensive ignore rules"
      contains: ".env*"
    - path: "web/package.json"
      provides: "Clean dependencies without platform-specific SWC"
    - path: "ready player 8.xcodeproj/project.pbxproj"
      provides: "Valid iOS deployment target"
      contains: "IPHONEOS_DEPLOYMENT_TARGET = 18.2"
  key_links:
    - from: ".gitignore"
      to: "git status"
      via: "Prevents secrets and build artifacts from being tracked"
      pattern: "\\.env\\*"
    - from: "ready player 8.xcodeproj/project.pbxproj"
      to: "Xcode build"
      via: "IPHONEOS_DEPLOYMENT_TARGET setting"
      pattern: "IPHONEOS_DEPLOYMENT_TARGET = 18"
---

<objective>
Rotate exposed Supabase key, fix .gitignore, remove repo debris, clean dependencies, and fix iOS deployment target.

Purpose: The Supabase secret key was committed in .env.local (exposed in git history). The .gitignore only covers .planning/. Orphaned folders and a platform-specific SWC dependency cause issues. The iOS deployment target of 26.2 does not exist.
Output: Rotated key, comprehensive .gitignore, clean repo, valid build target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.gitignore
@web/package.json
@ready player 8.xcodeproj/project.pbxproj

<interfaces>
From .gitignore (current -- only 1 line):
```
.planning/
```

From web/package.json (relevant line):
```json
"@next/swc-darwin-arm64": "^16.2.2",
```

From project.pbxproj (3 occurrences):
```
IPHONEOS_DEPLOYMENT_TARGET = 26.2;  // lines 309, 354, 497
MACOSX_DEPLOYMENT_TARGET = 15.6;    // lines 312, 357, 498
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Rotate Supabase secret key in dashboard</name>
  <files>web/.env.local</files>
  <read_first>
    web/src/lib/supabase/env.ts (to confirm env var names: SUPABASE_SERVICE_ROLE_KEY or SUPABASE_SECRET_KEY)
  </read_first>
  <action>
This task requires the human to rotate the Supabase secret key because the key was committed in .env.local and is exposed in git history. Claude cannot access the Supabase dashboard to regenerate keys.

Steps for the human:
1. Go to Supabase Dashboard: https://supabase.com/dashboard
2. Select your project
3. Go to Project Settings -> API
4. Under "service_role" (secret), regenerate/rotate the key
5. Copy the NEW service_role key
6. Update web/.env.local with: SUPABASE_SERVICE_ROLE_KEY=your_new_key_here
7. If deployed on Vercel, update the env var in Vercel Dashboard -> Project -> Settings -> Environment Variables
  </action>
  <verify>
    <automated>test -f "web/.env.local" && echo "env.local exists: PASS" || echo "env.local missing: CHECK"</automated>
  </verify>
  <done>User has confirmed the Supabase secret key is rotated, .env.local updated, and deployment env vars updated. The old compromised key no longer works.</done>
</task>

<task type="auto">
  <name>Task 2: Fix .gitignore, remove @next/swc-darwin-arm64, remove orphaned folders</name>
  <files>
    .gitignore
    web/package.json
    web/package-lock.json
  </files>
  <read_first>
    .gitignore (full file -- currently 1 line)
    web/package.json (full file)
  </read_first>
  <action>
**Fix .gitignore (SEC-09):**

1. Replace the current .gitignore (which only has `.planning/`) with a comprehensive one. KEEP the `.planning/` entry and ADD:
   ```gitignore
   # Planning artifacts
   .planning/

   # Environment files (secrets)
   .env
   .env.*
   .env.local
   .env.*.local

   # macOS
   .DS_Store
   **/.DS_Store

   # Xcode
   xcuserdata/
   DerivedData/
   *.xcuserstate
   *.xcworkspace/xcuserdata/

   # Build artifacts
   build/
   *.ipa

   # Certificates and keys
   *.pem
   *.p12
   *.key

   # Node
   node_modules/
   .next/

   # IDE
   .vscode/
   .idea/

   # OS
   Thumbs.db
   ```

**Remove @next/swc-darwin-arm64 (SEC-10):**

2. Remove `@next/swc-darwin-arm64` from `web/package.json` dependencies. This is a platform-specific optional dependency that Next.js auto-resolves at install time. Having it explicit breaks Linux CI builds.

   Run: `cd web && npm uninstall @next/swc-darwin-arm64`

   If npm uninstall fails, manually remove the line from package.json and run `npm install` to regenerate the lockfile.

**Remove orphaned folders (SEC-11):**

3. Remove the empty `untitled folder/` directory from the repo root:
   ```bash
   rm -rf "untitled folder/"
   ```

4. Remove the orphaned `web/src/.next/` directory (build cache that should never be in source):
   ```bash
   rm -rf web/src/.next/
   ```

5. Verify both are gone:
   ```bash
   ls -la "untitled folder" 2>&1  # should say "No such file"
   ls -la "web/src/.next" 2>&1    # should say "No such file"
   ```
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8" && grep -q "\.env\.\*\|\.env\.local" .gitignore && echo "env covered: PASS" && grep -q "DS_Store" .gitignore && echo "DS_Store: PASS" && grep -q "xcuserdata" .gitignore && echo "xcuserdata: PASS" && grep -q "DerivedData" .gitignore && echo "DerivedData: PASS" && grep -q "\*\.pem" .gitignore && echo "pem: PASS" && ! grep -q "swc-darwin-arm64" web/package.json && echo "swc removed: PASS" && ! test -d "untitled folder" && echo "untitled folder removed: PASS" && ! test -d "web/src/.next" && echo ".next removed: PASS"</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains entries for: .env*, .DS_Store, xcuserdata/, DerivedData/, *.pem, node_modules/, .next/
    - .gitignore still contains .planning/ entry
    - grep "@next/swc-darwin-arm64" web/package.json returns NO matches
    - Directory "untitled folder/" does not exist
    - Directory "web/src/.next/" does not exist
    - npm install in web/ succeeds without errors
  </acceptance_criteria>
  <done>
    .gitignore comprehensively covers secrets and build artifacts. Platform-specific SWC dependency removed. Orphaned folders cleaned up.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix iOS deployment target from 26.2 to 18.2</name>
  <files>
    ready player 8.xcodeproj/project.pbxproj
  </files>
  <read_first>
    ready player 8.xcodeproj/project.pbxproj (search for IPHONEOS_DEPLOYMENT_TARGET)
  </read_first>
  <action>
**Fix deployment target (INFRA-01):**

iOS 26.2 does not exist. The app description says "iOS 18.2+" and the STACK.md confirms iOS 18.2+. The MACOSX_DEPLOYMENT_TARGET of 15.6 is correct and should NOT be changed.

1. In `ready player 8.xcodeproj/project.pbxproj`, find ALL occurrences of:
   ```
   IPHONEOS_DEPLOYMENT_TARGET = 26.2;
   ```
   There are 3 occurrences (approximately lines 309, 354, 497 -- in Debug, Release, and Test build settings).

2. Replace ALL 3 with:
   ```
   IPHONEOS_DEPLOYMENT_TARGET = 18.2;
   ```

3. Use a precise sed or targeted edit. Do NOT modify any other line in the .pbxproj file -- it is a fragile format. Specifically:
   - Do NOT change MACOSX_DEPLOYMENT_TARGET (15.6 is correct)
   - Do NOT change any other build settings
   - Do NOT reformat or reorder any content

4. Verify the count of replacements is exactly 3.
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8" && grep -c "IPHONEOS_DEPLOYMENT_TARGET = 18.2" "ready player 8.xcodeproj/project.pbxproj" | grep -q "3" && echo "3 occurrences set to 18.2: PASS" && ! grep -q "IPHONEOS_DEPLOYMENT_TARGET = 26.2" "ready player 8.xcodeproj/project.pbxproj" && echo "no 26.2 remaining: PASS" && grep -c "MACOSX_DEPLOYMENT_TARGET = 15.6" "ready player 8.xcodeproj/project.pbxproj" | grep -q "[23]" && echo "macOS target unchanged: PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "IPHONEOS_DEPLOYMENT_TARGET = 18.2" project.pbxproj returns exactly 3 matches
    - grep "IPHONEOS_DEPLOYMENT_TARGET = 26.2" project.pbxproj returns 0 matches
    - grep "MACOSX_DEPLOYMENT_TARGET = 15.6" project.pbxproj returns 2-3 matches (unchanged)
    - No other lines in project.pbxproj are modified
  </acceptance_criteria>
  <done>
    iOS deployment target changed from invalid 26.2 to valid 18.2 across all build configurations. macOS target unchanged at 15.6.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> public | Committed secrets exposed to anyone with repo access |
| CI environment -> build | Platform-specific deps break cross-platform builds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Information Disclosure | .env.local in git history | mitigate | Rotate Supabase secret key so old key is invalid; add .env* to .gitignore |
| T-01-09 | Information Disclosure | .gitignore incomplete | mitigate | Add comprehensive ignore rules for secrets, build artifacts, IDE files |
| T-01-10 | Denial of Service | @next/swc-darwin-arm64 | mitigate | Remove platform-specific dep that breaks Linux CI |
| T-01-11 | Denial of Service | IPHONEOS_DEPLOYMENT_TARGET 26.2 | mitigate | Fix to valid 18.2 so Xcode can build |
</threat_model>

<verification>
- .gitignore covers all secret patterns
- No platform-specific SWC in package.json
- No orphaned folders on disk
- iOS deployment target is 18.2 in all 3 build configs
- Supabase secret key has been rotated (user confirmed)
</verification>

<success_criteria>
- Supabase secret key rotated (old exposed key invalidated)
- .gitignore covers .env*, .DS_Store, xcuserdata/, DerivedData/, *.pem
- @next/swc-darwin-arm64 removed from web/package.json
- Orphaned folders (untitled folder, web/src/.next) deleted
- iOS deployment target is 18.2 in all build configurations
</success_criteria>

<output>
After completion, create `.planning/phases/01-secrets-infrastructure-cleanup/01-03-SUMMARY.md`
</output>
