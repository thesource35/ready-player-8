---
phase: 08-web-ux-loading-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/checkout/error.tsx
  - web/src/app/contractors/error.tsx
  - web/src/app/cos-network/error.tsx
  - web/src/app/empire/error.tsx
  - web/src/app/hub/error.tsx
  - web/src/app/login/error.tsx
  - web/src/app/maps/error.tsx
  - web/src/app/market/error.tsx
  - web/src/app/pricing/error.tsx
  - web/src/app/privacy/error.tsx
  - web/src/app/profile/error.tsx
  - web/src/app/roofing/error.tsx
  - web/src/app/scanner/error.tsx
  - web/src/app/security/error.tsx
  - web/src/app/settings/error.tsx
  - web/src/app/smart-build/error.tsx
  - web/src/app/support/error.tsx
  - web/src/app/terms/error.tsx
  - web/src/app/tech/error.tsx
  - web/src/app/trust/error.tsx
  - web/src/app/verify/error.tsx
  - web/src/app/wealth/error.tsx
  - web/src/app/preview/[feature]/error.tsx
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "Every web page has an error.tsx boundary that catches runtime errors and shows a user-friendly message with a retry button"
    - "No technical error digest is exposed to users — only generic messages"
    - "All 23 new error.tsx files follow the exact same template as existing per-route boundaries"
  artifacts:
    - path: "web/src/app/checkout/error.tsx"
      provides: "Error boundary for checkout route"
      contains: "use client"
    - path: "web/src/app/trust/error.tsx"
      provides: "Error boundary for trust route"
      contains: "use client"
    - path: "web/src/app/preview/[feature]/error.tsx"
      provides: "Error boundary for dynamic preview route"
      contains: "use client"
  key_links:
    - from: "each error.tsx"
      to: "Next.js App Router"
      via: "file convention — error.tsx in route directory"
      pattern: '"use client"'
---

<objective>
Add error.tsx boundaries to all 23 web pages that currently lack them, so every route catches runtime errors and shows a user-friendly retry UI instead of a blank page or raw error.

Purpose: UX-01 requires all unprotected pages to have error boundaries. Currently 18 routes have them and 23 do not.
Output: 23 new error.tsx files, one per route directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-web-ux-loading-states/08-CONTEXT.md
@.planning/phases/08-web-ux-loading-states/08-RESEARCH.md
@.planning/phases/08-web-ux-loading-states/08-UI-SPEC.md

<interfaces>
<!-- Existing per-route error.tsx template (from web/src/app/projects/error.tsx): -->
```typescript
"use client";
export default function Error({ error, reset }: { error: Error; reset: () => void }) {
  return (
    <div style={{ padding: 40, textAlign: "center", maxWidth: 500, margin: "80px auto" }}>
      <div style={{ fontSize: 40, marginBottom: 16 }}>EMOJI</div>
      <h2 style={{ fontSize: 18, fontWeight: 800, marginBottom: 8, color: "var(--text)" }}>Something went wrong</h2>
      <p style={{ fontSize: 13, color: "var(--muted)", marginBottom: 20 }}>{error.message || "An unexpected error occurred."}</p>
      <button onClick={reset} style={{ padding: "10px 24px", borderRadius: 8, border: "none", cursor: "pointer", fontWeight: 700, fontSize: 13, color: "#080E12", background: "linear-gradient(90deg, #F29E3D, #FCC757)" }}>Try Again</button>
    </div>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create error.tsx for first 12 routes</name>
  <files>
    web/src/app/checkout/error.tsx
    web/src/app/contractors/error.tsx
    web/src/app/cos-network/error.tsx
    web/src/app/empire/error.tsx
    web/src/app/hub/error.tsx
    web/src/app/login/error.tsx
    web/src/app/maps/error.tsx
    web/src/app/market/error.tsx
    web/src/app/pricing/error.tsx
    web/src/app/privacy/error.tsx
    web/src/app/profile/error.tsx
    web/src/app/roofing/error.tsx
  </files>
  <read_first>
    web/src/app/projects/error.tsx
  </read_first>
  <action>
Create an error.tsx file in each of these 12 route directories. Use the EXACT template from web/src/app/projects/error.tsx (the simpler per-route pattern, NOT the root error.tsx). Each file is identical except for the emoji, which can vary slightly by context:

- checkout: use emoji character (warning sign or credit card related)
- contractors: use emoji character (construction related)
- cos-network: use emoji character (network related)
- empire: use emoji character (building related)
- hub: use emoji character (gear or hub related)
- login: use emoji character (lock related)
- maps: use emoji character (map related)
- market: use emoji character (chart related)
- pricing: use emoji character (price tag related)
- privacy: use emoji character (shield related)
- profile: use emoji character (person related)
- roofing: use emoji character (house related)

The exact template for each file:

```typescript
"use client";
export default function Error({ error, reset }: { error: Error; reset: () => void }) {
  return (
    <div style={{ padding: 40, textAlign: "center", maxWidth: 500, margin: "80px auto" }}>
      <div style={{ fontSize: 40, marginBottom: 16 }}>EMOJI_HERE</div>
      <h2 style={{ fontSize: 18, fontWeight: 800, marginBottom: 8, color: "var(--text)" }}>Something went wrong</h2>
      <p style={{ fontSize: 13, color: "var(--muted)", marginBottom: 20 }}>{error.message || "An unexpected error occurred."}</p>
      <button onClick={reset} style={{ padding: "10px 24px", borderRadius: 8, border: "none", cursor: "pointer", fontWeight: 700, fontSize: 13, color: "#080E12", background: "linear-gradient(90deg, #F29E3D, #FCC757)" }}>Try Again</button>
    </div>
  );
}
```

CRITICAL: Every file MUST start with "use client" as the first line. Do NOT expose error.digest anywhere. The error.message is shown as-is (it is the user-facing message from Next.js, not a stack trace).
  </action>
  <verify>
    <automated>cd /Users/beverlyhunter/Desktop/ready\ player\ 8/web && for route in checkout contractors cos-network empire hub login maps market pricing privacy profile roofing; do test -f "src/app/$route/error.tsx" && grep -q '"use client"' "src/app/$route/error.tsx" && echo "$route: OK" || echo "$route: MISSING"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 12 files exist in their respective route directories
    - Each file contains "use client" as the first line
    - Each file contains "Something went wrong" heading text
    - Each file contains the amber gradient button: background: "linear-gradient(90deg, #F29E3D, #FCC757)"
    - Each file calls reset on button click
    - No file contains "error.digest"
  </acceptance_criteria>
  <done>12 error.tsx files created, each with "use client", generic error message, and amber retry button matching the existing per-route pattern</done>
</task>

<task type="auto">
  <name>Task 2: Create error.tsx for remaining 11 routes</name>
  <files>
    web/src/app/scanner/error.tsx
    web/src/app/security/error.tsx
    web/src/app/settings/error.tsx
    web/src/app/smart-build/error.tsx
    web/src/app/support/error.tsx
    web/src/app/terms/error.tsx
    web/src/app/tech/error.tsx
    web/src/app/trust/error.tsx
    web/src/app/verify/error.tsx
    web/src/app/wealth/error.tsx
    web/src/app/preview/[feature]/error.tsx
  </files>
  <read_first>
    web/src/app/projects/error.tsx
  </read_first>
  <action>
Create an error.tsx file in each of these 11 route directories. Same template as Task 1. Emoji suggestions:

- scanner: scan-related emoji
- security: lock/shield emoji
- settings: gear emoji
- smart-build: construction/hammer emoji
- support: help/life-ring emoji
- terms: document emoji
- tech: computer/tech emoji
- trust: handshake/star emoji
- verify: checkmark emoji
- wealth: money/chart emoji
- preview/[feature]: eye/preview emoji

Use the EXACT same template structure as Task 1. The preview/[feature] directory already exists at web/src/app/preview/[feature]/ — place error.tsx inside it.

After creating all files, run `npm run build` to verify no build errors from the new error boundaries.
  </action>
  <verify>
    <automated>cd /Users/beverlyhunter/Desktop/ready\ player\ 8/web && for route in scanner security settings smart-build support terms tech trust verify wealth "preview/[feature]"; do test -f "src/app/$route/error.tsx" && grep -q '"use client"' "src/app/$route/error.tsx" && echo "$route: OK" || echo "$route: MISSING"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 11 files exist in their respective route directories
    - Each file contains "use client" as the first line
    - Each file contains "Something went wrong" heading text
    - Each file contains the amber gradient button: background: "linear-gradient(90deg, #F29E3D, #FCC757)"
    - Each file calls reset on button click
    - No file contains "error.digest"
    - `npm run build` succeeds with zero errors
  </acceptance_criteria>
  <done>All 23 error.tsx files exist across every route; build passes cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| error.message display | Error messages from runtime errors are shown to users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | error.tsx | mitigate | Show error.message only (user-facing), never expose error.digest or stack traces; per CONTEXT.md locked decision |
</threat_model>

<verification>
- All 23 routes now have error.tsx files
- Every error.tsx is a client component ("use client" first line)
- No error.digest or stack trace exposed in any boundary
- `npm run build` passes
- Total error.tsx count: 18 (existing) + 23 (new) = 41 per-route + 1 root = 42
</verification>

<success_criteria>
- 23 new error.tsx files created in their respective route directories
- All follow the established per-route template pattern (projects/error.tsx style)
- Build succeeds with no errors
- Grep for "error.digest" across all new files returns zero matches
</success_criteria>

<output>
After completion, create `.planning/phases/08-web-ux-loading-states/08-01-SUMMARY.md`
</output>
