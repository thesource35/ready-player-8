---
phase: 19-reporting-dashboards
plan: 05
type: execute
wave: 3
depends_on: ["19-01", "19-02", "19-04"]
files_modified:
  - web/src/app/reports/page.tsx
  - web/src/app/reports/layout.tsx
  - web/src/app/reports/project/[id]/page.tsx
  - web/src/app/reports/components/ReportHeader.tsx
  - web/src/app/reports/components/BudgetSection.tsx
  - web/src/app/reports/components/ScheduleSection.tsx
  - web/src/app/reports/components/SafetySection.tsx
  - web/src/app/reports/components/TeamSection.tsx
  - web/src/app/reports/components/AIInsightsSection.tsx
  - web/src/app/layout.tsx
autonomous: true
requirements:
  - REPORT-01
  - REPORT-04
must_haves:
  truths:
    - "User can navigate to /reports and see project list"
    - "User can click a project to view its full report"
    - "Report shows budget, schedule, issues, team, safety sections"
    - "Charts render in each section"
    - "Empty sections show 'None recorded' message"
    - "Reports link appears in web sidebar navigation"
  artifacts:
    - path: "web/src/app/reports/page.tsx"
      provides: "Reports landing page with project list"
    - path: "web/src/app/reports/project/[id]/page.tsx"
      provides: "Single project report page"
    - path: "web/src/app/reports/components/BudgetSection.tsx"
      provides: "Budget section with pie chart"
  key_links:
    - from: "web/src/app/reports/project/[id]/page.tsx"
      to: "/api/reports/project/[id]"
      via: "fetch in useEffect or server component"
      pattern: "fetch.*api/reports/project"
    - from: "web/src/app/layout.tsx"
      to: "/reports"
      via: "nav link in sidebar"
      pattern: "reports"
---

<objective>
Build the web report pages: landing page with project list, single-project report view with all sections and charts, and nav integration.

Purpose: Deliver REPORT-01 single-project report UI and REPORT-04 chart visualization on web.
Output: Report pages, section components, nav link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-UI-SPEC.md
@.planning/phases/19-reporting-dashboards/19-01-SUMMARY.md
@.planning/phases/19-reporting-dashboards/19-02-SUMMARY.md
@.planning/phases/19-reporting-dashboards/19-04-SUMMARY.md
@web/src/app/finance/page.tsx
@web/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reports landing page, layout with tabs, and nav integration</name>
  <files>web/src/app/reports/page.tsx, web/src/app/reports/layout.tsx, web/src/app/layout.tsx</files>
  <read_first>
    - web/src/app/layout.tsx (current nav structure -- find navGroups or sidebar links)
    - web/src/app/finance/page.tsx (layout pattern: inline styles, stat cards, CSS custom properties)
    - .planning/phases/19-reporting-dashboards/19-UI-SPEC.md (Report Page Shell, Tab Bar specs)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-03, D-05, D-65, D-66, D-66b, D-66c, D-66d)
  </read_first>
  <action>
1. Create `web/src/app/reports/layout.tsx`:
   - Per UI-SPEC Report Page Shell: container maxWidth 1200px, margin 0 auto, padding 20px
   - Per UI-SPEC Tab Bar: three tabs "PROJECT REPORT" / "PORTFOLIO ROLLUP" / "SCHEDULES" per D-50b
   - Active tab: background var(--accent), color var(--bg). Inactive: background var(--surface), color var(--muted)
   - Tab styling: flex 1, textAlign center, padding 8px 0, fontSize 8, fontWeight 800, letterSpacing 1, textTransform uppercase
   - Tabs link to /reports (project), /reports/rollup (portfolio), /reports/schedules
   - Use Next.js Link component with usePathname for active state detection

2. Create `web/src/app/reports/page.tsx`:
   - Per D-03: dedicated Reports landing page
   - Fetch project list from Supabase (server component or "use client" with useFetch)
   - Per D-05: stat cards at top showing portfolio-level KPIs (total projects, avg health, total contract value)
   - Per UI-SPEC page header: label "REPORTS" in green 12px 800 uppercase, title 24px 800
   - Project list: each project as a row card (background var(--surface), borderRadius 10, padding 12)
   - Per D-42: show all projects with status badges, active by default, toggle for completed
   - Each row shows: project name, health badge (HealthBadge component), contract value, status
   - Per D-40: click row navigates to /reports/project/{id}
   - Per D-66c: if no projects, show demo report with sample data and banner "This is a demo report..."
   - Per D-66b: contextual link pattern
   - Per D-106: filter bar with project name search + status filter + sort dropdown

3. Update `web/src/app/layout.tsx`:
   - Per D-66: add "Reports" link to main sidebar nav, grouped with Analytics and Finance
   - Use appropriate icon (chart icon SVG or emoji fallback per UI-SPEC icon library)
   - Position in nav to be near existing Finance and Analytics links
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/reports/page.tsx src/app/reports/layout.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - reports/layout.tsx contains 3 tabs: "PROJECT REPORT", "PORTFOLIO ROLLUP", "SCHEDULES"
    - reports/page.tsx fetches project list and renders cards with HealthBadge
    - reports/page.tsx includes filter/search per D-106
    - reports/page.tsx handles empty state with demo report per D-66c
    - layout.tsx contains "Reports" in navigation sidebar per D-66
    - All inline styles use CSS custom properties (var(--surface), var(--accent), etc.)
  </acceptance_criteria>
  <done>Reports landing page with project list, tab navigation, and sidebar nav link operational.</done>
</task>

<task type="auto">
  <name>Task 2: Single project report page with all section components</name>
  <files>web/src/app/reports/project/[id]/page.tsx, web/src/app/reports/components/ReportHeader.tsx, web/src/app/reports/components/BudgetSection.tsx, web/src/app/reports/components/ScheduleSection.tsx, web/src/app/reports/components/SafetySection.tsx, web/src/app/reports/components/TeamSection.tsx, web/src/app/reports/components/AIInsightsSection.tsx</files>
  <read_first>
    - web/src/lib/reports/types.ts (ProjectReport type)
    - web/src/app/reports/components/BudgetPieChart.tsx (chart component from Plan 02)
    - web/src/app/reports/components/StatCard.tsx (stat card from Plan 02)
    - web/src/app/reports/components/HealthBadge.tsx (health badge from Plan 02)
    - web/src/app/reports/components/SkeletonReport.tsx (skeleton from Plan 02)
    - .planning/phases/19-reporting-dashboards/19-UI-SPEC.md (all component specs)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-01 through D-16e section details)
  </read_first>
  <action>
1. Create `ReportHeader.tsx`:
   - Per D-10: project name, client name, date generated, health status badge
   - Per D-10: customizable company branding placeholder (logo + colors) for future white-labeling
   - Per UI-SPEC page header: background var(--surface), borderRadius 14, padding 20
   - Include HealthBadge component for health score display
   - Per D-16b: "Generated {Month DD, YYYY at HH:MM}" timestamp

2. Create `BudgetSection.tsx`:
   - Per D-13: contract value, total billed, % complete, change order net, retainage (5-6 key numbers)
   - Use StatCard grid: display grid, gridTemplateColumns repeat(auto-fit, minmax(140px, 1fr)), gap 8
   - Include BudgetPieChart (spent vs remaining donut) per D-24
   - Per D-08: if no budget data, show "None recorded"
   - Per D-16b: section freshness timestamp

3. Create `ScheduleSection.tsx`:
   - Per D-12: progress bars for milestones (% complete)
   - Per UI-SPEC Progress Bar: track background rgba(51,84,94,0.3), fill health color, borderRadius 4, height 8
   - Include ScheduleBarChart for top milestones per D-26
   - Per D-08: if no tasks, show "None recorded"

4. Create `SafetySection.tsx`:
   - Per D-16: count + severity breakdown (minor/moderate/serious) + days since last incident
   - SafetyLineChart for monthly trend per D-25
   - Full incident list when items exist per D-16
   - Per D-08: if no incidents, show "None recorded" (positive message for safety)

5. Create `TeamSection.tsx`:
   - Per D-14: member counts + 3-5 most recent activity feed entries
   - Role breakdown display
   - Per D-15: document and photo counts
   - Per D-08: if no team data, show "None recorded"

6. Create `AIInsightsSection.tsx`:
   - Per D-16d: "Key Insights" heading with AI-generated analysis
   - Per D-16e: recommendations inline per section + action items summary
   - Per UI-SPEC: accent-colored section heading
   - If no AI data: "Add more project data to unlock AI-powered insights." per UI-SPEC

7. Create `web/src/app/reports/project/[id]/page.tsx`:
   - "use client" with useState for report data and loading state
   - Fetch from /api/reports/project/{id} on mount
   - Per D-06: report date range covers full project lifetime automatically, no date picker needed
   - Per D-62: progressive loading -- show SkeletonReport while loading, then render sections
   - Per D-26h: independent loading per section with prioritized order (budget/schedule first)
   - Per D-56: show error card with "Retry" button on failed sections per UI-SPEC error state
   - Per D-08: always show all sections, empty ones show "None recorded"
   - Per D-26f: tabbed sections (Financial, Schedule, Safety, Team, Activity) + scrollable page option
   - Per D-26g: toggle between 'Charts + Data' and 'Charts Only' view modes
   - Per D-11: clickable items in Issues section linking to detail pages
   - Section heading style: fontSize 12, fontWeight 800, letterSpacing 2, textTransform uppercase
   - Per D-22: mini chart widgets concept (same data, smaller, navigates to full report -- implement as prop)
   - Export button group at top right per UI-SPEC Export Button Group
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/reports/project/\\[id\\]/page.tsx src/app/reports/components/*.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - project/[id]/page.tsx fetches from /api/reports/project/{id} and renders all sections
    - BudgetSection.tsx contains StatCard grid with 5+ KPI cards per D-13
    - BudgetSection.tsx includes BudgetPieChart component
    - ScheduleSection.tsx includes progress bars and ScheduleBarChart
    - SafetySection.tsx shows severity breakdown (minor/moderate/serious) per D-16
    - TeamSection.tsx shows member count + recent activity per D-14
    - AIInsightsSection.tsx has "Key Insights" heading per D-16d
    - All sections have "None recorded" empty state per D-08
    - All components use inline styles with CSS custom properties
  </acceptance_criteria>
  <done>Full single-project report page with all 6 sections, charts, progressive loading, and error handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response -> UI | Report data could contain unexpected values |
| Issue links | Clickable items navigate to detail pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-12 | Information Disclosure | Demo report | accept | Demo data is static sample, contains no real user data |
| T-19-13 | Tampering | Issue links | mitigate | Links use URL-safe project/entity IDs, no user input in href construction |
</threat_model>

<verification>
- `npx tsc --noEmit` passes on all report page files
- Reports link visible in layout.tsx navigation
- /reports page renders project list with health badges
- /reports/project/[id] renders all 6 sections with charts
</verification>

<success_criteria>
- REPORT-01: User can view single-project summary with budget, schedule, issues, team
- REPORT-04: Charts render in each section (pie, bar, line)
- D-03/D-66: Reports accessible via dedicated nav tab
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-05-SUMMARY.md`
</output>
