---
phase: 19-reporting-dashboards
plan: 10
type: execute
wave: 4
depends_on: ["19-01"]
files_modified:
  - ready player 8/ReportsView.swift
  - ready player 8/ProjectReportView.swift
  - ready player 8/PortfolioRollupView.swift
  - ready player 8/ReportCharts.swift
  - ready player 8/ContentView.swift
autonomous: true
requirements:
  - REPORT-01
  - REPORT-02
  - REPORT-04
must_haves:
  truths:
    - "Reports tab appears in Intel group of iOS navigation"
    - "User can view single-project report on iPhone"
    - "User can view portfolio rollup on iPhone"
    - "SwiftUI Charts render budget pie, schedule bar, safety line"
    - "iPad shows NavigationSplitView with project list + report detail"
  artifacts:
    - path: "ready player 8/ReportsView.swift"
      provides: "Main reports tab with segmented control"
    - path: "ready player 8/ProjectReportView.swift"
      provides: "Single project report view"
    - path: "ready player 8/PortfolioRollupView.swift"
      provides: "Portfolio rollup view"
    - path: "ready player 8/ReportCharts.swift"
      provides: "SwiftUI Charts components"
  key_links:
    - from: "ReportsView.swift"
      to: "ContentView.swift"
      via: "NavTab.reports case in navigation"
      pattern: "case reports"
    - from: "ProjectReportView.swift"
      to: "/api/reports/project"
      via: "URLSession fetch from web API per D-55"
      pattern: "api/reports/project"
---

<objective>
Build the iOS Reports tab with SwiftUI Charts, single-project report, portfolio rollup, and navigation integration.

Purpose: Deliver REPORT-01, REPORT-02, REPORT-04 on iOS with full parity to web.
Output: 4 Swift files + ContentView nav integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-UI-SPEC.md
@ready player 8/ContentView.swift
@ready player 8/SupabaseService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReportsView + ProjectReportView + NavTab integration</name>
  <files>ready player 8/ReportsView.swift, ready player 8/ProjectReportView.swift, ready player 8/ContentView.swift</files>
  <read_first>
    - ready player 8/ContentView.swift (NavTab enum, navItems tuple, NavigationRailView/NavigationTabsView, Theme struct -- search for "enum NavTab" and "navItems")
    - ready player 8/SupabaseService.swift (existing fetch pattern, SupabaseProject DTO)
    - .planning/phases/19-reporting-dashboards/19-UI-SPEC.md (iOS Layout iPhone + iPad specs)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-09, D-44, D-55, D-65, D-67 through D-76)
  </read_first>
  <action>
1. Update `ContentView.swift`:
   - Add `case reports` to NavTab enum per D-65
   - Add to navItems: (.reports, "Reports", "chart.bar.doc.horizontal", "intel") per D-65
   - Add case in main switch for report tab rendering: `ReportsView()`
   - Position in Intel group alongside Market and Analytics

2. Create `ReportsView.swift`:
   - Per D-44: segmented control ("Project Report" / "Portfolio Rollup") at top
   - Use `@State private var selectedTab: Int = 0` and Picker with .segmented style
   - Tab 0: Project list with health badges, tap to view ProjectReportView
   - Tab 1: PortfolioRollupView
   - Per D-09: iPhone = single-column stacked cards in ScrollView + LazyVStack
   - Per D-09: iPad = NavigationSplitView with project list sidebar (280pt) and report detail
   - Use `#if os(iOS)` with UIDevice checks or GeometryReader for layout switching
   - Per D-45: .refreshable { await loadReport() } for pull-to-refresh
   - Per D-58: skeleton loading while data loads (placeholder views)
   - Per D-68: cache report data in UserDefaults for offline viewing
   - Per D-66c: if no projects, show demo report with sample data and banner
   - Per D-69: haptic feedback on tab switch (UIImpactFeedbackGenerator.style: .light)

3. Create `ProjectReportView.swift`:
   - Per D-55: fetch from web API /api/reports/project/{id} as primary data source
   - Fallback: local aggregation from cached Supabase data when offline per D-55
   - Layout per UI-SPEC iOS (iPhone):
     - VStack spacing 12
     - Report header: project name, client, date, HealthBadge (colored circle + label)
     - Per D-10: customizable header with branding placeholder
     - Budget section: LazyVGrid 2 columns for stat cards + budget pie chart
     - Schedule section: milestone progress bars + bar chart
     - Safety section: severity badges + line chart
     - Team section: member count + activity list (3-5 entries per D-14)
     - AI Insights section per D-16d
   - Per D-08: all sections visible, empty = "None recorded"
   - Per D-15: document and photo counts
   - Per D-16: safety severity breakdown + days since last
   - Per D-16b: section freshness timestamps
   - Per D-11: tappable items navigate to detail views
   - Per D-71: landscape rotation support (charts expand to full width at 300pt height per UI-SPEC)
   - Per D-69: haptic feedback on chart data point tap
   - Per D-75: VoiceOver accessibility -- semantic announcements with interpretation (e.g., "Budget: 92% spent -- at risk")
   - Theme colors: .bg, .surface, .accent, .gold, .cyan, .green, .red, .purple, .text, .muted
   - .premiumGlow(cornerRadius: 14) on card containers
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8" && xcodebuild -scheme "ready player 8" -destination "platform=iOS Simulator,name=iPhone 16 Pro" build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ContentView.swift contains "case reports" in NavTab enum
    - ContentView.swift navItems contains ("reports", "Reports", "chart.bar.doc.horizontal", "intel")
    - ReportsView.swift has Picker with .pickerStyle(.segmented) for "Project Report" / "Portfolio Rollup"
    - ReportsView.swift has NavigationSplitView for iPad per D-09
    - ProjectReportView.swift fetches from "/api/reports/project/" per D-55
    - ProjectReportView.swift has VoiceOver .accessibilityLabel on health-related elements per D-75
    - ProjectReportView.swift has .refreshable modifier per D-45
    - Xcode build succeeds with 0 errors
  </acceptance_criteria>
  <done>iOS Reports tab with project report view, iPad split view, navigation integration, haptics, VoiceOver.</done>
</task>

<task type="auto">
  <name>Task 2: PortfolioRollupView + ReportCharts SwiftUI components</name>
  <files>ready player 8/PortfolioRollupView.swift, ready player 8/ReportCharts.swift</files>
  <read_first>
    - ready player 8/ReportsView.swift (just created, understand navigation flow)
    - ready player 8/SupabaseService.swift (existing DTO patterns)
    - .planning/phases/19-reporting-dashboards/19-UI-SPEC.md (SwiftUI Charts Configuration, iOS Layout)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-18, D-19, D-35 through D-44)
  </read_first>
  <action>
1. Create `ReportCharts.swift`:
   - Import Charts framework (SwiftUI Charts)
   - Per D-18 and UI-SPEC SwiftUI Charts Configuration:
   
   a. `BudgetPieChartView(spent: Double, remaining: Double)`:
      - Chart with SectorMark, angularInset 1.5, cornerRadius 4
      - .foregroundStyle(by: .value("Category", name)) with accent + green colors
      - Center overlay text showing "X% Complete"
      - Per D-71: pinch-to-zoom gesture
   
   b. `ScheduleBarChartView(milestones: [(name: String, percent: Double)])`:
      - Chart with BarMark, .cornerRadius(4), .foregroundStyle(Theme.cyan)
      - Top 5-8 milestones per D-26
   
   c. `SafetyLineChartView(monthlyData: [(month: String, count: Int)])`:
      - Chart with LineMark + PointMark
      - .lineStyle(StrokeStyle(lineWidth: 2)), .symbolSize(40)
      - .foregroundStyle(Theme.red) per D-25
   
   d. `ActivityTrendChartView(data: [(month: String, count: Int)])`:
      - Chart with AreaMark + LineMark
      - .opacity(0.1) on area, .foregroundStyle(Theme.purple) per D-19b
   
   All charts:
   - Per D-26b: .chartAnimate(true) for entrance animations
   - Per D-69: haptic feedback on data point selection via .chartOverlay
   - Per D-71: pinch-to-zoom via MagnificationGesture on time-series charts
   - Per D-75: .accessibilityLabel with semantic descriptions
   - Per D-26d: detect high contrast mode with @Environment(\.accessibilityHighContrast)
   - Height: 200pt per UI-SPEC iOS charts

2. Create `PortfolioRollupView.swift`:
   - Per D-55: fetch from web API /api/reports/rollup
   - Per D-36: summary KPI stat cards + project list + charts
   - Stat cards: LazyVGrid 2 columns showing total contract value, total billed, remaining, project count
   - Project list: ForEach with health badges, tap navigates to ProjectReportView per D-40
   - Per D-41: portfolio-level health score at top
   - Per D-42: status filter (Active/Delayed/Completed) via Picker
   - Per D-43: monthly spend trend chart (use chart from ReportCharts)
   - Per D-45: .refreshable for pull-to-refresh
   - Per D-68: cache rollup data in UserDefaults for offline
   - Theme styling: .background(Theme.surface), .cornerRadius(14), .padding(16)
   - Per D-44: accessible via both segmented control in ReportsView and separate nav concept
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8" && xcodebuild -scheme "ready player 8" -destination "platform=iOS Simulator,name=iPhone 16 Pro" build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ReportCharts.swift imports Charts framework
    - ReportCharts.swift has BudgetPieChartView with SectorMark per UI-SPEC
    - ReportCharts.swift has ScheduleBarChartView with BarMark per UI-SPEC
    - ReportCharts.swift has SafetyLineChartView with LineMark + PointMark per UI-SPEC
    - All chart views have .accessibilityLabel per D-75
    - PortfolioRollupView.swift fetches from "/api/reports/rollup" per D-55
    - PortfolioRollupView.swift has status filter Picker per D-42
    - PortfolioRollupView.swift has .refreshable per D-45
    - Xcode build succeeds
  </acceptance_criteria>
  <done>iOS portfolio rollup + all SwiftUI Charts components with haptics, accessibility, offline cache.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web API -> iOS | iOS fetches report data from web API over network |
| Offline cache | UserDefaults stores cached report data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-27 | Information Disclosure | Offline cache | accept | UserDefaults encrypted at rest by iOS data protection; same security as existing app data |
| T-19-28 | Tampering | API response | mitigate | Validate JSON structure before rendering; handle malformed data gracefully |
</threat_model>

<verification>
- Xcode build succeeds with 0 errors
- Reports tab appears in ContentView navigation
- All 4 chart types render in SwiftUI Charts
</verification>

<success_criteria>
- REPORT-01/02/04 on iOS: project report, portfolio rollup, charts
- D-09: iPhone single-column, iPad split view
- D-55: Fetches from web API with offline fallback
- D-65: Reports tab in Intel group navigation
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-10-SUMMARY.md`
</output>
