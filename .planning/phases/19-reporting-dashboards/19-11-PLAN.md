---
phase: 19-reporting-dashboards
plan: 11
type: execute
wave: 4
depends_on: ["19-01"]
files_modified:
  - ready player 8/ReportPDFGenerator.swift
  - ready player 8/ReportScheduleManager.swift
  - ready player 8/ReportExportView.swift
autonomous: true
requirements:
  - REPORT-03
must_haves:
  truths:
    - "iOS can generate PDF with UIGraphicsPDFRenderer"
    - "PDF includes charts as rendered images"
    - "Schedule management works on iOS"
    - "User can share/export report from iOS"
  artifacts:
    - path: "ready player 8/ReportPDFGenerator.swift"
      provides: "iOS PDF generation"
    - path: "ready player 8/ReportScheduleManager.swift"
      provides: "Schedule CRUD via web API"
    - path: "ready player 8/ReportExportView.swift"
      provides: "Export sheet with PDF preview and options"
  key_links:
    - from: "ReportPDFGenerator.swift"
      to: "UIGraphicsPDFRenderer"
      via: "native PDF rendering"
      pattern: "UIGraphicsPDFRenderer"
---

<objective>
Build iOS PDF generation, schedule management, and export capabilities.

Purpose: Deliver REPORT-03 PDF export on iOS and D-50k schedule management parity.
Output: PDF generator, schedule manager, export view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-UI-SPEC.md
@ready player 8/SupabaseService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: iOS PDF generator + export view</name>
  <files>ready player 8/ReportPDFGenerator.swift, ready player 8/ReportExportView.swift</files>
  <read_first>
    - ready player 8/SupabaseService.swift (existing service patterns)
    - ready player 8/ReportCharts.swift (chart views to render into PDF -- from Plan 10)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-28, D-29, D-30 through D-34n)
    - .planning/phases/19-reporting-dashboards/19-UI-SPEC.md (PDF Layout Contract)
  </read_first>
  <action>
1. Create `ReportPDFGenerator.swift`:
   - Per D-28: UIGraphicsPDFRenderer as primary
   - Function `generatePDF(report: ProjectReportData, options: PDFOptions) -> Data`:
     - PDFOptions: { projectName: String, companyName: String?, companyLogo: UIImage?, landscape: Bool, paperSize: CGSize, confidential: Bool, executiveSummary: String? }
     - Per D-34b: auto-detect paper size (US Letter 612x792pt vs A4 595x842pt) using Locale.current.region
     - Per D-34c: portrait for project, landscape for rollup
     - Per D-33: light theme (white background, dark text)
   - Per D-32: header on each page (company logo left, project name center, date right)
   - Per D-32: footer on each page (page number center, "Generated by ConstructionOS" left)
   - Per D-34f: confidentiality footer + DRAFT watermark
   - Per D-34d: table of contents on page 1
   - Per D-34g: executive summary section
   - Per D-30: render SwiftUI Charts to UIImage using ImageRenderer, embed in PDF
   - Per D-34j: smart page breaks (track Y position, add new page before section if would overflow)
   - Per D-34i: QR code on first page using CIFilter (CIQRCodeGenerator)
   - Per D-34k: tagged PDF accessibility (basic tagging via PDF metadata)
   - Per D-31: filename pattern "{ProjectName}-Report-{date}.pdf"

2. Create `ReportExportView.swift`:
   - Per D-29: sheet presentation with PDF preview and download
   - Preview section showing rendered PDF (PDFKit PDFView)
   - Per D-34h: password protection toggle + password field
   - Per D-34g: executive summary text editor
   - Per D-34f: confidentiality toggle
   - "Export PDF" button triggers generation + share sheet (UIActivityViewController)
   - Per D-34e: company branding settings (logo picker + color selector)
   - Per D-47: additional export options: "Export CSV", "Export JSON" (direct share sheet)
   - Per D-34m: "Export All Reports" batch export button
   - Loading indicator during generation
   - Theme styling with Theme colors
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8" && xcodebuild -scheme "ready player 8" -destination "platform=iOS Simulator,name=iPhone 16 Pro" build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ReportPDFGenerator.swift uses UIGraphicsPDFRenderer per D-28
    - ReportPDFGenerator.swift uses Locale.current.region for paper size per D-34b
    - ReportPDFGenerator.swift has CIQRCodeGenerator for QR code per D-34i
    - ReportPDFGenerator.swift renders charts via ImageRenderer per D-30
    - ReportExportView.swift has password toggle per D-34h
    - ReportExportView.swift has executive summary TextEditor per D-34g
    - ReportExportView.swift uses UIActivityViewController for sharing
    - Xcode build succeeds
  </acceptance_criteria>
  <done>iOS PDF generation with all customization options and export sharing.</done>
</task>

<task type="auto">
  <name>Task 2: iOS schedule management + iOS-specific features</name>
  <files>ready player 8/ReportScheduleManager.swift</files>
  <read_first>
    - ready player 8/SupabaseService.swift (HTTP request pattern)
    - ready player 8/ReportsView.swift (navigation context from Plan 10)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-50k, D-50u, D-70, D-74, D-76)
  </read_first>
  <action>
1. Create `ReportScheduleManager.swift`:
   - Per D-50k: full schedule management parity with web
   - Per D-55: all operations via web API (/api/reports/schedule)

   Schedule list view:
   - Per D-50u: card-based list with swipe actions
   - Swipe left: Delete (red background) per UI-SPEC
   - Swipe right: Pause/Resume (gold background) per UI-SPEC
   - Each card shows: schedule name, frequency, next run time (cyan), status (green/gold)
   - Per D-50g: "Send Now" button inline per card
   - Per D-50o: "Send Test" button

   Create/edit schedule sheet:
   - Frequency Picker (daily/weekly/biweekly/monthly) per D-49
   - Day picker (wheel style for iOS)
   - Time picker (DatePicker with .datePickerStyle(.wheel))
   - Recipient selector (multi-select from team members) per D-50e
   - Section picker (toggles per section) per D-50l

   Per D-50f: pause/resume toggle with immediate visual state change
   Per D-50t: show next run time alongside frequency

   Additional iOS features (stubs for advanced features):
   - Per D-70: Siri Shortcuts stub -- AppIntents framework, "Show project report" and "Portfolio health" intents. Create ShowReportIntent struct conforming to AppIntent
   - Per D-74: CoreSpotlight indexing stub -- CSSearchableItem for each project report
   - Per D-76: CarPlay health summary stub -- color-coded health badge + project count accessible from CarPlay scene
   - Per D-76: AirPlay presentation mode stub -- chart-only view for external displays

   Note: WidgetKit (D-67), Watch (D-72), Dynamic Island (D-73) are separate extension targets. Create placeholder files with TODO comments noting they require separate Xcode targets.
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8" && xcodebuild -scheme "ready player 8" -destination "platform=iOS Simulator,name=iPhone 16 Pro" build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ReportScheduleManager.swift fetches from "/api/reports/schedule" per D-55
    - ReportScheduleManager.swift has swipe actions (.swipeActions) for delete and pause per D-50u
    - ReportScheduleManager.swift has Picker for frequency selection per D-49
    - ReportScheduleManager.swift has Siri Shortcuts AppIntent per D-70
    - ReportScheduleManager.swift has CoreSpotlight CSSearchableItem per D-74
    - Xcode build succeeds
  </acceptance_criteria>
  <done>iOS schedule management with swipe actions, Siri shortcuts, CoreSpotlight, CarPlay stubs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS -> Web API | Schedule management via authenticated API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-29 | Spoofing | Schedule API calls | mitigate | Include auth token in all API requests; validate on server |
| T-19-30 | Information Disclosure | CoreSpotlight | accept | Spotlight data indexed locally only; iOS sandboxing protects |
</threat_model>

<verification>
- Xcode build succeeds
- PDF generation creates Data blob
- Schedule management CRUD via API
</verification>

<success_criteria>
- REPORT-03 on iOS: PDF generation with UIGraphicsPDFRenderer
- D-50k: Schedule management parity with web
- D-70/74/76: Siri, Spotlight, CarPlay stubs in place
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-11-SUMMARY.md`
</output>
