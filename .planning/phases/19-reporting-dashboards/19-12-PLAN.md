---
phase: 19-reporting-dashboards
plan: 12
type: execute
wave: 5
depends_on: ["19-05", "19-06"]
files_modified:
  - web/src/app/reports/components/CollaborationPanel.tsx
  - web/src/app/reports/components/AnnotationCanvas.tsx
  - web/src/app/reports/components/VersionHistory.tsx
  - web/src/app/api/reports/comments/route.ts
  - web/src/app/api/reports/annotations/route.ts
  - web/src/app/api/reports/history/route.ts
autonomous: true
requirements:
  - REPORT-01
must_haves:
  truths:
    - "User can comment on report sections"
    - "User can annotate charts with drawing tools"
    - "User can view report version history with diffs"
    - "Comments support threaded replies"
  artifacts:
    - path: "web/src/app/reports/components/CollaborationPanel.tsx"
      provides: "Comments panel for report sections"
    - path: "web/src/app/reports/components/AnnotationCanvas.tsx"
      provides: "Fabric.js annotation overlay on charts"
    - path: "web/src/app/reports/components/VersionHistory.tsx"
      provides: "Version history with visual diffs"
  key_links:
    - from: "AnnotationCanvas.tsx"
      to: "fabric"
      via: "Fabric.js canvas library"
      pattern: "import.*fabric"
---

<objective>
Build collaboration features: threaded comments on report sections, visual annotations on charts via Fabric.js, and report version history with diffs.

Purpose: Enable team collaboration on reports per D-98 and D-99.
Output: Comments, annotations, version history components + API routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-RESEARCH.md
@.planning/phases/19-reporting-dashboards/19-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comments + Annotations API routes and components</name>
  <files>web/src/app/api/reports/comments/route.ts, web/src/app/api/reports/annotations/route.ts, web/src/app/reports/components/CollaborationPanel.tsx, web/src/app/reports/components/AnnotationCanvas.tsx</files>
  <read_first>
    - web/src/lib/reports/db-schema.sql (cs_report_comments, cs_report_annotations schemas)
    - web/src/lib/supabase/fetch.ts (auth pattern)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-98)
    - .planning/phases/19-reporting-dashboards/19-RESEARCH.md (Fabric.js note about SSR)
  </read_first>
  <action>
1. Create `web/src/app/api/reports/comments/route.ts`:
   - GET: list comments for a report_history_id + section, include threaded replies (parent_id)
   - POST: create comment with { report_history_id, section, content, parent_id? }
   - Auth: getAuthenticatedClient
   - Validate: content not empty, report_history_id exists

2. Create `web/src/app/api/reports/annotations/route.ts`:
   - GET: list annotations for a report_history_id
   - POST: save annotation { report_history_id, chart_id, fabric_json }
   - PUT: update annotation fabric_json
   - Auth: getAuthenticatedClient

3. Create `CollaborationPanel.tsx` ("use client"):
   - Per D-98: threaded comments per report section
   - Comment list with author name, timestamp, content
   - Reply button opens nested reply form
   - Add comment form: textarea + submit button
   - Section tabs to switch between sections
   - Fetch comments from /api/reports/comments
   - Inline styles per project convention

4. Create `AnnotationCanvas.tsx` ("use client"):
   - Per D-98: visual annotation/drawing tools on charts
   - Import Fabric.js via dynamic import (next/dynamic with ssr: false) per RESEARCH.md assumption A5
   - Tools: circle, arrow, highlight, freeform draw (circles, arrows, highlights per D-98)
   - Toolbar with tool selection
   - Save/load annotations via /api/reports/annotations (Fabric.js JSON serialization)
   - Per D-98: annotations exportable in PDF (serialize to image for PDF inclusion)
   - Overlay on chart container (positioned absolutely over chart div)
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/api/reports/comments/route.ts src/app/api/reports/annotations/route.ts src/app/reports/components/CollaborationPanel.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - comments/route.ts has GET, POST handlers with auth
    - comments/route.ts supports parent_id for threaded replies per D-98
    - annotations/route.ts has GET, POST, PUT handlers
    - CollaborationPanel.tsx renders comment list with reply functionality
    - AnnotationCanvas.tsx imports fabric via dynamic import with ssr: false
    - AnnotationCanvas.tsx has toolbar with circle, arrow, highlight tools per D-98
  </acceptance_criteria>
  <done>Threaded comments and chart annotation tools with persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Version history with visual diffs</name>
  <files>web/src/app/api/reports/history/route.ts, web/src/app/reports/components/VersionHistory.tsx</files>
  <read_first>
    - web/src/lib/reports/db-schema.sql (cs_report_history schema)
    - web/src/lib/reports/types.ts (report types for snapshot comparison)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-99, D-34l)
  </read_first>
  <action>
1. Create `web/src/app/api/reports/history/route.ts`:
   - Per D-99: every report generation creates a version
   - GET: list versions for project_id, ordered by created_at DESC
   - POST: save new version snapshot { project_id, report_type, snapshot_data (full JSON) }
   - Per D-34l: store PDF in Supabase Storage, save path in pdf_storage_path
   - Auth: getAuthenticatedClient

2. Create `VersionHistory.tsx` ("use client"):
   - Per D-99: report version history with full diff
   - Version list: date, version number, summary of changes
   - Per D-99: visual diff highlights:
     - Metric changes: green arrow up / red arrow down with old->new values
     - New issues: highlighted in green
     - Resolved items: highlighted with strikethrough
   - Compare two versions side-by-side (dropdown to select comparison version)
   - Per D-117: time period comparison using version snapshots
   - Download PDF for any historical version per D-34l
   - Per D-96: data retention indicator (show retention period, mark items nearing expiry)
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/api/reports/history/route.ts src/app/reports/components/VersionHistory.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - history/route.ts has GET and POST with auth
    - history/route.ts saves snapshot_data as jsonb per D-99
    - VersionHistory.tsx shows version list with dates
    - VersionHistory.tsx has diff highlighting (up/down arrows for metric changes) per D-99
    - VersionHistory.tsx has version comparison dropdown per D-117
    - VersionHistory.tsx shows data retention indicator per D-96
  </acceptance_criteria>
  <done>Version history with visual diffs, comparison, PDF downloads, and retention tracking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Comment input | User-supplied text content |
| Fabric.js JSON | Serialized canvas objects from client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-31 | Tampering | Comments | mitigate | Sanitize comment content (strip HTML); validate length limits |
| T-19-32 | Tampering | Annotations fabric_json | mitigate | Validate JSON structure before storage; limit size |
| T-19-33 | Information Disclosure | Version history | mitigate | RLS ensures only authorized users see versions |
</threat_model>

<verification>
- TypeScript compiles all new files
- Comments API supports threaded replies
- Annotation canvas loads Fabric.js without SSR errors
- Version history shows diff highlights
</verification>

<success_criteria>
- D-98: Comments and annotations on report sections
- D-99: Version history with visual diffs
- D-34l: PDF storage in Supabase for re-download
- D-96: Data retention visibility
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-12-SUMMARY.md`
</output>
