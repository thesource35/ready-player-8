---
phase: 19-reporting-dashboards
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - web/src/app/api/reports/project/[id]/route.ts
  - web/src/app/api/reports/rollup/route.ts
  - web/src/app/api/reports/health/route.ts
  - web/src/app/api/reports/__tests__/project.test.ts
  - web/src/lib/reports/__tests__/rollup.test.ts
autonomous: true
requirements:
  - REPORT-01
  - REPORT-02
must_haves:
  truths:
    - "GET /api/reports/project/[id] returns full report with all sections"
    - "GET /api/reports/rollup returns portfolio data with per-project summaries"
    - "GET /api/reports/health validates table existence and returns status"
    - "Section failures return partial report with error details"
    - "X-Report-Debug header present on all responses"
  artifacts:
    - path: "web/src/app/api/reports/project/[id]/route.ts"
      provides: "Single project report endpoint"
      exports: ["GET"]
    - path: "web/src/app/api/reports/rollup/route.ts"
      provides: "Portfolio rollup endpoint"
      exports: ["GET"]
    - path: "web/src/app/api/reports/health/route.ts"
      provides: "Health check endpoint"
      exports: ["GET"]
  key_links:
    - from: "web/src/app/api/reports/project/[id]/route.ts"
      to: "web/src/lib/reports/aggregation.ts"
      via: "imports compute functions"
      pattern: "import.*from.*aggregation"
    - from: "web/src/app/api/reports/project/[id]/route.ts"
      to: "web/src/lib/supabase/fetch.ts"
      via: "getAuthenticatedClient for auth"
      pattern: "getAuthenticatedClient"
---

<objective>
Build the three core report API routes: single-project report, portfolio rollup, and health check.

Purpose: Deliver the data layer for REPORT-01 and REPORT-02 that web UI and iOS consume.
Output: 3 API routes with tests, partial failure handling, debug headers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-RESEARCH.md
@.planning/phases/19-reporting-dashboards/19-01-SUMMARY.md
@web/src/lib/supabase/fetch.ts
@web/src/lib/reports/types.ts
@web/src/lib/reports/aggregation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project report + rollup + health API routes</name>
  <files>web/src/app/api/reports/project/[id]/route.ts, web/src/app/api/reports/rollup/route.ts, web/src/app/api/reports/health/route.ts</files>
  <read_first>
    - web/src/lib/supabase/fetch.ts (getAuthenticatedClient pattern)
    - web/src/lib/reports/aggregation.ts (compute functions from Plan 01)
    - web/src/lib/reports/types.ts (ProjectReport, PortfolioRollup types)
    - web/src/app/api/chat/route.ts (existing API route pattern for auth + error handling)
    - web/src/lib/rate-limit.ts (existing Upstash rate limiting)
  </read_first>
  <action>
1. Create `web/src/app/api/reports/project/[id]/route.ts`:
   - Per D-51: GET handler returning full ProjectReport
   - Auth: getAuthenticatedClient(), return 401 if not authenticated
   - Per D-56: use Promise.allSettled to fetch all sections in parallel with 10s timeout each
   - Sections to fetch from Supabase (per D-02):
     a. budget: query cs_projects by id + cs_contracts WHERE project_id = id, pass to computeBudgetSection
     b. schedule: query cs_project_tasks WHERE project_id = id, pass to computeScheduleSection
     c. issues: query RFIs + change_orders WHERE project_id = id, pass to computeIssuesSection
     d. team: query team_assignments + activity_feed WHERE project_id = id, pass to computeTeamSection
     e. safety: query safety_incidents WHERE project_id = id, pass to computeSafetySection
     f. documents: COUNT from cs_documents WHERE entity_type = 'project' AND entity_id = id per D-15
     g. photos: COUNT from cs_field_photos WHERE project_id = id per D-15
   - Per D-53: single API call returns full report (all sections)
   - Per D-56: on section failure, set section to null and add to errors array. Never block entire report
   - Per D-56f: structured error format { error: { code: 'REPORT_SECTION_TIMEOUT', message, section, retryable: true } }
   - Per D-56c: set X-Report-Debug response header with timing per section, include _meta field in JSON
   - Per D-56e: clamp values (budget capped at 100%, floor counts at 0) using clampBudgetPercent/clampCount
   - Per D-07: compute health score from sections using computeHealthScore
   - Per D-16b: include freshness timestamp per section (use query time)
   - Per D-04: computed on-the-fly, no persistence
   - Rate limit with existing Upstash infrastructure

2. Create `web/src/app/api/reports/rollup/route.ts`:
   - Per D-51: GET handler returning PortfolioRollup
   - Auth check same as project route
   - Per D-39: accept query params: status, date_range, project_type, client, budget_range
   - Per D-46b: accept optional compare_period param for period comparison
   - Query all user's projects (RLS-filtered), compute per-project summaries
   - Per D-35: total contract value, total billed, remaining, change order impact
   - Per D-37: non-financial: schedule health, open issues, team utilization, safety
   - Per D-41: portfolio-level aggregate health score
   - Per D-43: monthly spend trend data for line chart
   - Per D-46: cap at 200 projects with notice if exceeded
   - Per D-16c: feature coverage per project (active features / 6 total)
   - X-Report-Debug header + _meta field

3. Create `web/src/app/api/reports/health/route.ts`:
   - Per D-56b: verify all required tables exist, views exist, Resend config, schema version
   - Per D-56d: pre-flight schema check
   - Return { status: 'ok' | 'degraded' | 'error', tables: { name: boolean }[], version: string }
   - Check cs_projects, cs_contracts, cs_project_tasks, cs_report_schedules, cs_report_shared_links exist
   - Check RESEND_API_KEY env var exists (for scheduled reports)
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/api/reports/project/\\[id\\]/route.ts src/app/api/reports/rollup/route.ts src/app/api/reports/health/route.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - project/[id]/route.ts exports GET function that calls getAuthenticatedClient
    - project/[id]/route.ts uses Promise.allSettled for parallel section fetching
    - project/[id]/route.ts sets X-Report-Debug header per D-56c
    - rollup/route.ts accepts status/date_range/compare_period query params per D-39, D-46b
    - rollup/route.ts caps results at 200 projects per D-46
    - health/route.ts checks table existence and returns status object per D-56b
    - All routes return proper 401 for unauthenticated requests
    - All files pass TypeScript type checking
  </acceptance_criteria>
  <done>Three core API routes operational with auth, partial failure handling, debug headers, rate limiting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API route integration tests</name>
  <files>web/src/app/api/reports/__tests__/project.test.ts, web/src/lib/reports/__tests__/rollup.test.ts</files>
  <read_first>
    - web/src/app/api/reports/project/[id]/route.ts (just created)
    - web/src/app/api/reports/rollup/route.ts (just created)
    - web/src/lib/reports/__tests__/fixtures/sample-project.json (fixture from Plan 01)
    - web/vitest.config.ts (test config)
  </read_first>
  <behavior>
    - Project report route returns 401 without auth
    - Project report route returns ProjectReport shape with all sections
    - Project report handles section timeout (partial report with error)
    - Rollup route returns PortfolioRollup shape
    - Rollup route applies status filter correctly
    - Rollup route caps at 200 projects
    - Health route returns status object
  </behavior>
  <action>
1. Create `web/src/app/api/reports/__tests__/project.test.ts`:
   - Mock getAuthenticatedClient to return mock supabase client
   - Mock supabase.from().select() to return fixture data
   - Test: GET returns 401 when not authenticated
   - Test: GET returns ProjectReport with all section keys present
   - Test: GET includes X-Report-Debug header equivalent (_meta field)
   - Test: section timeout produces partial report (mock one section to throw)
   - Test: health score computed and included
   - Use sample-project.json fixture data

2. Create `web/src/lib/reports/__tests__/rollup.test.ts`:
   - Test: computePortfolioRollup with sample-portfolio.json fixture
   - Test: totals correctly sum across all projects
   - Test: per-project health scores computed independently
   - Test: feature coverage counts correct
   - Test: monthly spend trend data aggregated
   - Test: empty projects array returns zero totals
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx vitest run src/app/api/reports/__tests__/project.test.ts src/lib/reports/__tests__/rollup.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - project.test.ts has tests for 401, full report shape, partial failure, and health score
    - rollup.test.ts has tests for totals, per-project health, feature coverage, empty input
    - All tests pass with 0 failures
    - Tests use shared fixtures from Plan 01
  </acceptance_criteria>
  <done>API route tests cover auth, happy path, partial failure, and edge cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Unauthenticated requests must be rejected |
| API -> Supabase | RLS filters data per user |
| URL params -> query | User-supplied filter params need validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Spoofing | API routes | mitigate | getAuthenticatedClient() on every route, 401 on failure |
| T-19-09 | Information Disclosure | project/[id] | mitigate | RLS ensures user can only see their projects; no data leakage on 404 |
| T-19-10 | Denial of Service | rollup | mitigate | Cap at 200 projects (D-46), rate limit via Upstash |
| T-19-11 | Tampering | query params | mitigate | Validate/sanitize status, date_range, budget_range params before Supabase query |
</threat_model>

<verification>
- All API route tests pass
- TypeScript compiles without errors
- Auth check present on every route handler
</verification>

<success_criteria>
- Single-project report API returns all 4 sections (budget, schedule, issues, team) + safety
- Portfolio rollup API returns aggregate financials and per-project summaries
- Health check validates infrastructure readiness
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-04-SUMMARY.md`
</output>
