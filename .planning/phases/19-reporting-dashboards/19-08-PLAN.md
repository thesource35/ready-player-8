---
phase: 19-reporting-dashboards
plan: 08
type: execute
wave: 4
depends_on: ["19-04"]
files_modified:
  - web/src/app/api/reports/schedule/route.ts
  - web/src/app/api/reports/cron/route.ts
  - web/src/lib/reports/email-template.tsx
  - web/vercel.json
  - web/src/app/reports/schedules/page.tsx
  - web/src/app/reports/components/ScheduleManagement.tsx
autonomous: true
requirements:
  - REPORT-01
  - REPORT-02
must_haves:
  truths:
    - "User can create/edit/delete report schedules"
    - "Cron endpoint processes due schedules"
    - "Email template renders branded HTML with inline metrics"
    - "Schedule management UI shows next run time and status"
  artifacts:
    - path: "web/src/app/api/reports/schedule/route.ts"
      provides: "Schedule CRUD endpoint"
      exports: ["GET", "POST", "PUT", "DELETE"]
    - path: "web/src/app/api/reports/cron/route.ts"
      provides: "Vercel Cron handler"
      exports: ["GET"]
    - path: "web/src/lib/reports/email-template.tsx"
      provides: "React Email branded template"
    - path: "web/src/app/reports/schedules/page.tsx"
      provides: "Schedule management page"
  key_links:
    - from: "cron/route.ts"
      to: "schedule/route.ts"
      via: "reads cs_report_schedules"
      pattern: "cs_report_schedules"
    - from: "cron/route.ts"
      to: "email-template.tsx"
      via: "renders email for Resend"
      pattern: "import.*email-template"
---

<objective>
Build scheduled report email delivery: schedule CRUD API, Vercel Cron handler, branded email template, and schedule management UI.

Purpose: Enable automated report delivery via email on user-defined schedules (D-49 through D-50x).
Output: Schedule API, cron job, email template, management page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-RESEARCH.md
@.planning/phases/19-reporting-dashboards/19-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schedule CRUD API + Cron handler + email template</name>
  <files>web/src/app/api/reports/schedule/route.ts, web/src/app/api/reports/cron/route.ts, web/src/lib/reports/email-template.tsx, web/vercel.json</files>
  <read_first>
    - web/src/lib/supabase/fetch.ts (getAuthenticatedClient pattern)
    - web/src/lib/reports/db-schema.sql (cs_report_schedules, cs_report_delivery_log schemas)
    - web/src/lib/reports/types.ts (ReportSchedule, DeliveryLog types)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-49 through D-50x)
    - .planning/phases/19-reporting-dashboards/19-RESEARCH.md (Pattern 4: Vercel Cron)
  </read_first>
  <action>
1. Create `web/src/app/api/reports/schedule/route.ts`:
   - Per D-54: CRUD for email schedules
   - GET: list all schedules for authenticated user, include next_run_at per D-50t
   - POST: create schedule with { frequency, day_of_week?, day_of_month?, time_utc, timezone, recipients, sections }
     - Per D-49: frequency options: daily/weekly/biweekly/monthly (custom, no presets)
     - Per D-50e: validate recipients are team members only (check against user_orgs/team tables)
     - Per D-50j: timezone stored for schedule execution
     - Per D-50p: one report (portfolio rollup) per schedule entry
     - Per D-50s: unlimited schedules per user
     - Compute next_run_at based on frequency + timezone
   - PUT: update schedule (frequency, recipients, sections, is_active per D-50f pause/resume)
   - DELETE: remove schedule per D-50f
   - Per D-50g: POST with action="send_now" triggers immediate send
   - Per D-50o: POST with action="send_test" sends test to current user only
   - Auth: getAuthenticatedClient on all operations
   - Input validation with zod schemas

2. Create `web/src/app/api/reports/cron/route.ts`:
   - Per D-50i: Vercel Cron handler
   - Per RESEARCH.md Pattern 4: verify CRON_SECRET bearer token
   - Query cs_report_schedules WHERE is_active = true AND next_run_at <= NOW()
   - Per D-50v: row lock (SELECT ... FOR UPDATE SKIP LOCKED) prevents overlapping runs
   - For each due schedule:
     a. Generate portfolio rollup report data (call /api/reports/rollup internally or compute directly)
     b. Generate PDF per D-34n (pre-rendered template with fresh data)
     c. Send email via Resend per D-50
     d. Log to cs_report_delivery_log per D-50h
     e. Update next_run_at on schedule
   - Per D-50n: on PDF failure, send email without PDF, note failure
   - Per D-50x: on Resend failure, store report for download, notify user
   - Per D-50d: emit notifications for delivery AND failure via Phase 14 notification system
   - Per D-50f: auto-pause on inactive projects (Completed/On Hold)

3. Create `web/src/lib/reports/email-template.tsx`:
   - Per D-50c: branded HTML email using @react-email/components
   - ConstructionOS header with logo and brand colors
   - Inline metrics summary: health score badge, budget %, key numbers
   - PDF attachment note
   - Link to live web report
   - Per D-50q: noreply from address (reports@constructionos.com)
   - Per D-50r: auto-generated subject line ("ConstructionOS Portfolio Report -- Week of {date}")
   - Per D-50m: no unsubscribe link (team members managed by sender)

4. Create/update `web/vercel.json`:
   - Per D-50i: add cron configuration
   ```json
   { "crons": [{ "path": "/api/reports/cron", "schedule": "*/15 * * * *" }] }
   ```
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/api/reports/schedule/route.ts src/app/api/reports/cron/route.ts src/lib/reports/email-template.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - schedule/route.ts exports GET, POST, PUT, DELETE handlers
    - schedule/route.ts validates recipients against team members per D-50e
    - cron/route.ts checks CRON_SECRET bearer token per RESEARCH.md
    - cron/route.ts uses SELECT FOR UPDATE SKIP LOCKED per D-50v
    - email-template.tsx imports from @react-email/components
    - email-template.tsx has health score badge and inline metrics per D-50c
    - vercel.json has cron entry for /api/reports/cron at */15 frequency
    - schedule/route.ts has send_now and send_test action support per D-50g, D-50o
  </acceptance_criteria>
  <done>Schedule CRUD, cron processing, and branded email delivery operational.</done>
</task>

<task type="auto">
  <name>Task 2: Schedule management UI page</name>
  <files>web/src/app/reports/schedules/page.tsx, web/src/app/reports/components/ScheduleManagement.tsx</files>
  <read_first>
    - web/src/lib/reports/types.ts (ReportSchedule type)
    - .planning/phases/19-reporting-dashboards/19-UI-SPEC.md (Schedule Management Card spec)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-50b through D-50t)
  </read_first>
  <action>
1. Create `ScheduleManagement.tsx` ("use client"):
   - Per UI-SPEC Schedule Management Card:
     - Card: background var(--surface), borderRadius 10, padding 16, marginBottom 8
     - Schedule name: fontSize 12, fontWeight 800
     - Frequency label: fontSize 8, color var(--muted)
     - Next run: fontSize 8, fontWeight 800, color var(--cyan) per D-50t
     - Status: fontSize 8, fontWeight 800, green for active, gold for paused per D-50f
     - Actions: "Pause"/"Resume", "Send Now", "Send Test", "Delete" per D-50f, D-50g, D-50o
   - Create schedule modal/dialog:
     - Frequency picker (daily/weekly/biweekly/monthly) per D-49
     - Day picker (for weekly: day of week, for monthly: day of month)
     - Time picker
     - Recipient selector (team members from Phase 15 data) per D-50e
     - Section picker (optional, all by default) per D-50l
   - Per D-50b: dedicated section in Reports tab
   - Per D-110: bulk operations (pause/resume/update/delete multiple schedules)
   - Last delivery status shown per card per D-50t
   - Per UI-SPEC Schedule delete/pause confirmations

2. Create `web/src/app/reports/schedules/page.tsx`:
   - Fetch schedules from /api/reports/schedule
   - Render ScheduleManagement component
   - "Create Schedule" button at top
   - List of schedule cards
   - Per D-118: mini-calendar showing upcoming deliveries
   - Per D-118: sortable list view of upcoming deliveries
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/reports/schedules/page.tsx src/app/reports/components/ScheduleManagement.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ScheduleManagement.tsx has frequency picker with daily/weekly/biweekly/monthly per D-49
    - ScheduleManagement.tsx has Pause/Resume, Send Now, Send Test, Delete actions per D-50f/g/o
    - ScheduleManagement.tsx shows next run time per D-50t
    - schedules/page.tsx renders schedule list with create button
    - Recipient selector fetches team members per D-50e
    - Bulk operation buttons present per D-110
    - Mini-calendar for upcoming deliveries per D-118
  </acceptance_criteria>
  <done>Schedule management page with create/edit/delete, bulk operations, and mini-calendar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron endpoint | Must verify Vercel authorization |
| Email recipients | Must be validated team members only |
| Schedule input | User-controlled frequency/time values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-19 | Elevation of Privilege | cron/route.ts | mitigate | Verify CRON_SECRET bearer token; reject unauthorized requests with 401 |
| T-19-20 | Spoofing | Email recipients | mitigate | Validate recipients against team members table (D-50e); no external addresses |
| T-19-21 | Denial of Service | Schedule creation | mitigate | Rate limit schedule API; Resend handles email rate limiting |
| T-19-22 | Repudiation | Email delivery | mitigate | Full delivery log in cs_report_delivery_log (D-50h) with timestamp and status |
</threat_model>

<verification>
- TypeScript compiles all schedule files
- Schedule CRUD API has all 4 HTTP methods
- Cron handler verifies auth token
- Email template renders branded HTML
</verification>

<success_criteria>
- D-49 through D-50x: Complete scheduled email delivery system
- D-118: Schedule calendar integration
- D-110: Bulk schedule operations
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-08-SUMMARY.md`
</output>
