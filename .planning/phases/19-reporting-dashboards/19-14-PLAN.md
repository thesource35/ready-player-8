---
phase: 19-reporting-dashboards
plan: 14
type: execute
wave: 5
depends_on: ["19-04"]
files_modified:
  - web/src/lib/reports/notifications-integration.ts
  - web/src/lib/reports/automation-rules.ts
  - web/src/app/api/reports/metrics/route.ts
  - web/src/app/api/reports/embed/route.ts
  - web/src/app/reports/components/NotificationPreferences.tsx
  - web/src/app/reports/components/AutomationRuleBuilder.tsx
  - web/src/app/reports/components/EmbedCodeGenerator.tsx
autonomous: true
requirements:
  - REPORT-01
must_haves:
  truths:
    - "Health score changes emit notifications"
    - "Metric threshold alerts configurable per project"
    - "Automation rules trigger actions on conditions"
    - "Embed codes generate iframe HTML"
    - "Metrics endpoint returns p50/p95 response times"
  artifacts:
    - path: "web/src/lib/reports/notifications-integration.ts"
      provides: "Notification emission for report events"
    - path: "web/src/lib/reports/automation-rules.ts"
      provides: "If-then rule engine for report automation"
    - path: "web/src/app/api/reports/embed/route.ts"
      provides: "Embed authentication and rendering"
  key_links:
    - from: "notifications-integration.ts"
      to: "Phase 14 notification system"
      via: "emits notifications for report events"
      pattern: "notification"
---

<objective>
Build notification integration, automation rules, performance metrics, and embed functionality.

Purpose: Connect reports to notification system (D-100 through D-102), add automation (D-103), embedding (D-104), and analytics metrics (D-62c, D-105).
Output: Notification hooks, rule engine, embed route, metrics endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notification integration + automation rules</name>
  <files>web/src/lib/reports/notifications-integration.ts, web/src/lib/reports/automation-rules.ts, web/src/app/reports/components/NotificationPreferences.tsx, web/src/app/reports/components/AutomationRuleBuilder.tsx</files>
  <read_first>
    - web/src/lib/notifications/notifications.ts (Phase 14 notification patterns -- check if exists)
    - web/src/lib/reports/types.ts (HealthScore type)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-100, D-101, D-102, D-103, D-50d)
  </read_first>
  <action>
1. Create `web/src/lib/reports/notifications-integration.ts`:
   - Per D-100: emit notifications on health score color transitions (green->yellow, yellow->red, etc.)
   - Per D-100: default alerts on all color transitions + user-customizable overrides
   - Per D-50d: emit notifications for delivery, failure, health changes, shared link accessed, batch export complete
   - Functions:
     - `emitHealthChangeNotification(projectId, oldHealth, newHealth)` -- only emits if color changed
     - `emitDeliveryNotification(scheduleId, status, recipients)`
     - `emitSharedLinkAccessNotification(linkId, viewerInfo)`
     - `emitBatchExportNotification(userId, status, downloadUrl)`
   - Per D-102: metric threshold alerts -- `checkThresholds(projectId, metrics, userThresholds)` returns alerts for exceeded thresholds
   - Per D-102: change detection -- flag unexpected movements (>X% change in single reporting period)
   - Integration: call Phase 14 notification system (insert into cs_notifications table or call notification API)

2. Create `web/src/lib/reports/automation-rules.ts`:
   - Per D-103: built-in automation trigger templates:
     - "Send report when health drops to red"
     - "Auto-export PDF weekly"
     - "Alert team when budget exceeds 85%"
   - Per D-103: custom if-then rule builder
   - Rule type: { id, name, condition: { metric, operator, threshold }, action: { type, params } }
   - Evaluate function: `evaluateRules(rules, currentMetrics)` returns triggered actions
   - Action types: 'send_report', 'send_notification', 'pause_schedule', 'create_task'

3. Create `NotificationPreferences.tsx` ("use client"):
   - Per D-101: per-type toggles (health changes, deliveries, threshold alerts, shared link access)
   - Per D-101: per-project granularity (toggle notifications per project)
   - Per D-101: daily digest mode option (batch notifications into daily summary)
   - Save preferences to user settings (Supabase or localStorage)
   - Toggle list with section headers

4. Create `AutomationRuleBuilder.tsx` ("use client"):
   - Per D-103: visual if-then rule builder
   - Condition picker: metric dropdown + operator (>, <, =, changes by) + threshold input
   - Action picker: action type dropdown + params
   - Built-in template buttons for common rules
   - Rule list showing active/inactive rules
   - Enable/disable toggle per rule
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/lib/reports/notifications-integration.ts src/lib/reports/automation-rules.ts src/app/reports/components/NotificationPreferences.tsx src/app/reports/components/AutomationRuleBuilder.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - notifications-integration.ts exports emitHealthChangeNotification that checks color transition per D-100
    - notifications-integration.ts has threshold checking per D-102
    - automation-rules.ts exports evaluateRules function per D-103
    - automation-rules.ts has built-in templates per D-103
    - NotificationPreferences.tsx has per-type toggles and per-project granularity per D-101
    - AutomationRuleBuilder.tsx has condition picker (metric + operator + threshold) per D-103
  </acceptance_criteria>
  <done>Notification integration, threshold alerts, and automation rule engine operational.</done>
</task>

<task type="auto">
  <name>Task 2: Embed codes + metrics endpoint + analytics</name>
  <files>web/src/app/api/reports/metrics/route.ts, web/src/app/api/reports/embed/route.ts, web/src/app/reports/components/EmbedCodeGenerator.tsx</files>
  <read_first>
    - web/src/lib/supabase/fetch.ts (auth pattern)
    - web/src/lib/rate-limit.ts (rate limiting)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-104, D-105, D-62c)
    - web/next.config.ts (CSP headers -- needs update for iframe embedding)
  </read_first>
  <action>
1. Create `web/src/app/api/reports/embed/route.ts`:
   - Per D-104: serve embeddable report content for iframe
   - GET with token query param: validate share token, serve report HTML
   - Set appropriate headers: X-Frame-Options: ALLOWALL (override default DENY for embed routes only)
   - Content-Security-Policy: frame-ancestors * (allow embedding anywhere)
   - Auth via share token (same as shared links per D-64b)
   - Render simplified report view (charts + data, no navigation chrome)
   - Rate limit same as shared links per D-64e

2. Create `web/src/app/api/reports/metrics/route.ts`:
   - Per D-62c: custom metrics endpoint
   - Return { p50, p95, avg } response times per API section
   - Track timing data in-memory (recent 100 requests per endpoint)
   - Per D-105: Vercel Analytics events: report_viewed, report_exported, schedule_created
   - Per D-105: PostHog integration for detailed analytics (funnels, cohorts, feature usage)
   - Initialize PostHog client (posthog-node for server, posthog-js for client)
   - Auth: admin only (check role)

3. Create `EmbedCodeGenerator.tsx` ("use client"):
   - Per D-104: generate iframe HTML code for embedding
   - Input: select chart or full report to embed
   - Output: textarea with copyable iframe code
   - Preview: small iframe preview showing how embed looks
   - Include share token in iframe src URL
   - Copy button with "Copied!" toast
   - Size options: small (400x300), medium (600x400), large (800x600), responsive (100% width)
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/api/reports/metrics/route.ts src/app/api/reports/embed/route.ts src/app/reports/components/EmbedCodeGenerator.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - embed/route.ts sets X-Frame-Options to allow embedding per D-104
    - embed/route.ts validates share token
    - metrics/route.ts returns p50/p95 timing data per D-62c
    - EmbedCodeGenerator.tsx generates iframe HTML with token per D-104
    - EmbedCodeGenerator.tsx has size options and copy button
    - Files reference posthog-node or posthog-js per D-105
  </acceptance_criteria>
  <done>Embed functionality, performance metrics, and analytics integration operational.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Embed iframe | Third-party sites embedding report content |
| Automation rules | User-defined rules trigger system actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-36 | Information Disclosure | embed/route.ts | mitigate | Share token validation; auto-mask sensitive data per D-64f; rate limit per D-64e |
| T-19-37 | Elevation of Privilege | Automation rules | mitigate | Rules can only trigger predefined actions; no arbitrary code execution |
| T-19-38 | Denial of Service | metrics/route.ts | mitigate | Admin-only access; rate limit; in-memory buffer bounded to 100 entries |
</threat_model>

<verification>
- TypeScript compiles all files
- Embed route allows iframe with correct headers
- Metrics endpoint returns timing data
- Notification integration emits on health changes
</verification>

<success_criteria>
- D-100/101/102: Notification integration with customizable alerts
- D-103: Automation rule builder with templates
- D-104: Embed codes for external sites
- D-105: PostHog + Vercel Analytics events
- D-62c: Performance metrics endpoint
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-14-SUMMARY.md`
</output>
