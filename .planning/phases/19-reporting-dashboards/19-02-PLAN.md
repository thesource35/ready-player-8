---
phase: 19-reporting-dashboards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/reports/chart-config.ts
  - web/src/app/reports/components/BudgetPieChart.tsx
  - web/src/app/reports/components/ScheduleBarChart.tsx
  - web/src/app/reports/components/SafetyLineChart.tsx
  - web/src/app/reports/components/ActivityTrendChart.tsx
  - web/src/app/reports/components/TeamUtilizationChart.tsx
  - web/src/app/reports/components/ChartExportButton.tsx
  - web/src/app/reports/components/StatCard.tsx
  - web/src/app/reports/components/HealthBadge.tsx
  - web/src/app/reports/components/SkeletonReport.tsx
  - web/src/app/reports/__tests__/charts.test.tsx
autonomous: true
requirements:
  - REPORT-04
must_haves:
  truths:
    - "Budget pie chart renders with spent vs remaining segments"
    - "Schedule bar chart renders top 5-8 milestones"
    - "Safety line chart renders monthly incident data"
    - "All charts have tooltips, animations, and inline labels"
    - "Charts are exportable as PNG/SVG"
    - "StatCard, HealthBadge, SkeletonReport components render correctly"
  artifacts:
    - path: "web/src/app/reports/components/BudgetPieChart.tsx"
      provides: "Budget donut chart per D-24"
    - path: "web/src/app/reports/components/ScheduleBarChart.tsx"
      provides: "Milestone bar chart per D-26"
    - path: "web/src/app/reports/components/SafetyLineChart.tsx"
      provides: "Monthly safety line chart per D-25"
    - path: "web/src/app/reports/components/ChartExportButton.tsx"
      provides: "PNG/SVG export per chart per D-26c"
    - path: "web/src/app/reports/components/StatCard.tsx"
      provides: "Reusable KPI card per UI-SPEC"
  key_links:
    - from: "BudgetPieChart.tsx"
      to: "chart-config.ts"
      via: "shared chart colors and tooltip config"
      pattern: "import.*from.*chart-config"
---

<objective>
Build all Recharts chart components, reusable UI atoms (StatCard, HealthBadge, Skeleton), and chart export functionality.

Purpose: Deliver REPORT-04 chart visualizations and shared components used by all report pages.
Output: 11 component files, chart config, component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-UI-SPEC.md
@web/src/app/finance/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chart config + all Recharts chart components with interactivity</name>
  <files>web/src/lib/reports/chart-config.ts, web/src/app/reports/components/BudgetPieChart.tsx, web/src/app/reports/components/ScheduleBarChart.tsx, web/src/app/reports/components/SafetyLineChart.tsx, web/src/app/reports/components/ActivityTrendChart.tsx, web/src/app/reports/components/TeamUtilizationChart.tsx, web/src/app/reports/components/ChartExportButton.tsx</files>
  <read_first>
    - web/src/app/finance/page.tsx (layout pattern to follow for inline styles)
    - .planning/phases/19-reporting-dashboards/19-UI-SPEC.md (chart container specs, chart type specs, animation specs, color palette)
    - .planning/phases/19-reporting-dashboards/19-RESEARCH.md (Recharts pattern example for BudgetPieChart)
  </read_first>
  <action>
1. Create `web/src/lib/reports/chart-config.ts`:
   - Export `CHART_TOOLTIP_STYLE`: { background: 'var(--surface)', border: '1px solid var(--border)', borderRadius: 8, padding: 8, fontSize: 12, fontWeight: 400 } per UI-SPEC
   - Export `CHART_AXIS_STYLE`: { fontSize: 8, fontWeight: 400, fill: 'var(--muted)' } per UI-SPEC
   - Export `CHART_GRID_STYLE`: { stroke: 'var(--border)', strokeOpacity: 0.3 } per UI-SPEC
   - Export `CHART_ANIMATION`: { duration: 600, easing: 'ease-out' } per D-26b
   - Export `CHART_SERIES_COLORS`: ['#F29E3D', '#4AC4CC', '#69D294', '#8A8FCC', '#FCC757', '#D94D48'] per UI-SPEC
   - Export `CHART_HEIGHT`: { default: 240, mini: 180 } per UI-SPEC
   - Export chart wrapper style: { background: 'var(--panel)', borderRadius: 14, padding: 16, marginBottom: 16 } per UI-SPEC

2. Create `BudgetPieChart.tsx` ("use client"):
   - Props: `{ spent: number; remaining: number; mini?: boolean }`
   - Per D-24: PieChart with Pie (donut: innerRadius={mini ? 30 : 60}, outerRadius={mini ? 50 : 90})
   - 2 cells: spent = CHART_SERIES_COLORS[0] (accent), remaining = CHART_SERIES_COLORS[2] (green)
   - Per D-23: inline labels with color dot + label (no separate legend)
   - Per D-20: Tooltip with formatted dollar values
   - Per D-26b: animationDuration 600, animationEasing ease-out
   - Center text showing "X% Complete"
   - Per D-26e: wrap in ResponsiveContainer width="100%" height={mini ? 180 : 240}
   - Per D-26d: aria-label on chart container, role="img"

3. Create `ScheduleBarChart.tsx` ("use client"):
   - Props: `{ milestones: Array<{ name: string; percentComplete: number }> }`
   - Per D-26: Show top 5-8 milestones (slice if more)
   - BarChart with Bar, barSize={24}, fill=cyan, radius=[4,4,0,0]
   - XAxis with milestone names, YAxis 0-100%
   - Tooltip, CartesianGrid with grid style from config
   - Per D-20: click handler on bar emits onMilestoneClick callback

4. Create `SafetyLineChart.tsx` ("use client"):
   - Props: `{ monthlyData: Array<{ month: string; count: number }> }`
   - Per D-25: monthly data points
   - LineChart with Line strokeWidth=2, dot radius=4, stroke=red
   - XAxis months, YAxis incident count
   - Per D-26b: animation enabled

5. Create `ActivityTrendChart.tsx` ("use client"):
   - Props: `{ data: Array<{ month: string; count: number }> }`
   - Per D-19b: AreaChart with Area fillOpacity=0.1, stroke=purple

6. Create `TeamUtilizationChart.tsx` ("use client"):
   - Props: `{ roleBreakdown: Record<string, number>; workload?: Array<{ name: string; hours: number }> }`
   - Per D-19b: stacked bar chart for members per role/trade
   - Horizontal bar for workload per member if workload provided

7. Create `ChartExportButton.tsx` ("use client"):
   - Props: `{ chartRef: React.RefObject<HTMLDivElement>; filename: string }`
   - Per D-26c: small download icon (SVG), 24x24px hit area, positioned top-right
   - aria-label="Export chart" per UI-SPEC
   - On click: dropdown with "PNG" and "SVG" options
   - PNG export: use html2canvas on chartRef element
   - SVG export: extract SVG from Recharts container, create blob download
   - Tooltip on hover: "Export chart (PNG/SVG)" per UI-SPEC

All chart components must include:
- Chart wrapper div with background: var(--panel), borderRadius: 14, padding: 16 per UI-SPEC
- Chart title: 12px weight 800 letterSpacing 1 textTransform uppercase per UI-SPEC
- ChartExportButton rendered in top-right corner
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/reports/components/*.tsx src/lib/reports/chart-config.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - BudgetPieChart.tsx contains "use client", imports from "recharts", has PieChart with innerRadius={60}
    - ScheduleBarChart.tsx renders BarChart with barSize={24} and radius=[4,4,0,0]
    - SafetyLineChart.tsx renders LineChart with strokeWidth: 2 and dot radius 4
    - ChartExportButton.tsx imports html2canvas, has aria-label="Export chart"
    - All chart components use ResponsiveContainer with width="100%" per D-26e
    - chart-config.ts exports CHART_ANIMATION with duration: 600 per D-26b
    - All files pass TypeScript type checking
  </acceptance_criteria>
  <done>All 5 chart components + ChartExportButton render correctly with Recharts, follow UI-SPEC styling exactly.</done>
</task>

<task type="auto">
  <name>Task 2: StatCard, HealthBadge, SkeletonReport components + chart component tests</name>
  <files>web/src/app/reports/components/StatCard.tsx, web/src/app/reports/components/HealthBadge.tsx, web/src/app/reports/components/SkeletonReport.tsx, web/src/app/reports/__tests__/charts.test.tsx</files>
  <read_first>
    - .planning/phases/19-reporting-dashboards/19-UI-SPEC.md (KPI Stat Card, Health Score Badge, Skeleton Loading specs)
    - web/src/app/finance/page.tsx (existing stat card layout pattern)
    - web/src/app/reports/components/BudgetPieChart.tsx (just created, verify import pattern)
  </read_first>
  <action>
1. Create `StatCard.tsx`:
   - Props: `{ value: string; label: string; color?: string }`
   - Per UI-SPEC KPI Stat Card: textAlign center, padding 16, background rgba({accent-rgb}, 0.06), borderRadius 10
   - Value: fontSize 24, fontWeight 800, color from prop or var(--text)
   - Label: fontSize 8, fontWeight 800, color var(--muted), textTransform uppercase

2. Create `HealthBadge.tsx`:
   - Props: `{ score: number; color: 'green' | 'gold' | 'red'; label: string }`
   - Per UI-SPEC: inline-flex, alignItems center, gap 4, padding 4px 8px, borderRadius 6
   - Dot: width 8, height 8, borderRadius 50%, background color mapped to CSS var
   - Label: fontSize 8, fontWeight 800, color mapped to CSS var
   - Color mapping: green -> var(--green), gold -> var(--gold), red -> var(--red)

3. Create `SkeletonReport.tsx`:
   - Per UI-SPEC Skeleton Loading and D-58:
   - Shimmer animation: linear-gradient(90deg, var(--surface) 25%, var(--panel) 50%, var(--surface) 75%), backgroundSize: 200% 100%, animation: shimmer 1.5s infinite
   - Add @keyframes shimmer via inline style or CSS module
   - KPI skeleton: 4 cards at height 72px in grid
   - Chart skeleton: single block at height 240px, borderRadius 14
   - List skeleton: 4 rows at height 48px, gap 8
   - Per D-62: progressive loading order (KPI cards first, then charts)
   - Per D-26h: each chart area loads independently

4. Create `web/src/app/reports/__tests__/charts.test.tsx`:
   - Per D-79: component tests using Vitest + React Testing Library (jsdom)
   - Test BudgetPieChart renders with spent=300000 remaining=150000, verify "67% Complete" text
   - Test ScheduleBarChart renders with 3 milestones, verify milestone names appear
   - Test SafetyLineChart renders with 6 months of data
   - Test StatCard renders value and label text
   - Test HealthBadge renders correct label for each color
   - Test SkeletonReport renders skeleton elements
   - Use @testing-library/react render + screen.getByText/getByRole assertions
   - Note: Recharts may need mock for ResponsiveContainer in jsdom (mock with div)
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx vitest run src/app/reports/__tests__/charts.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - StatCard.tsx has fontSize: 24 for value and fontSize: 8 for label per UI-SPEC
    - HealthBadge.tsx maps 'green' to 'var(--green)', 'gold' to 'var(--gold)', 'red' to 'var(--red)'
    - SkeletonReport.tsx contains shimmer animation with 1.5s infinite
    - charts.test.tsx passes all tests with 0 failures
    - All components use inline styles (not CSS modules) matching finance page pattern
  </acceptance_criteria>
  <done>All UI atoms render per UI-SPEC. Chart component tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chart data props | Data from API could contain extreme values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Denial of Service | Chart components | mitigate | Limit data points rendered (milestones capped at 8 per D-26, monthly data bounded) |
| T-19-04 | Information Disclosure | ChartExportButton | accept | Charts only contain data user already has access to view |
</threat_model>

<verification>
- `cd web && npx vitest run src/app/reports/__tests__/charts.test.tsx` all pass
- `npx tsc --noEmit` zero errors on all new components
- All chart components have "use client" directive
</verification>

<success_criteria>
- 5 chart types + 3 UI atoms + skeleton loader implemented per UI-SPEC
- Chart export (PNG/SVG) functional via ChartExportButton
- Component tests verify rendering with test data
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-02-SUMMARY.md`
</output>
