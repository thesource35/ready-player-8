---
phase: 19-reporting-dashboards
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - web/src/app/reports/page.tsx
  - web/src/app/reports/project/[id]/page.tsx
  - web/src/app/reports/rollup/page.tsx
  - web/src/app/reports/components/StatCard.tsx
  - web/src/app/reports/components/HealthBadge.tsx
  - web/src/app/reports/components/BudgetPieChart.tsx
  - web/src/app/reports/components/ScheduleBarChart.tsx
  - web/src/app/reports/components/SafetyLineChart.tsx
  - web/src/app/reports/components/SkeletonReport.tsx
  - web/src/app/reports/components/PortfolioTable.tsx
  - web/src/app/reports/components/ReportHeader.tsx
  - web/src/app/reports/layout.tsx
  - web/src/app/reports/error.tsx
  - web/src/app/layout.tsx
autonomous: false

requirements:
  - REPORT-01
  - REPORT-02
  - REPORT-04

must_haves:
  truths:
    - "User can navigate to /reports and see tab switcher (Project Report / Portfolio Rollup)"
    - "User can select a project and see a full single-project report with budget, schedule, issues, safety, team sections"
    - "User can view a portfolio rollup with KPI cards and sortable project table"
    - "User sees budget pie chart (donut), schedule bar chart, safety line chart with proper colors"
    - "Charts show tooltips on hover with exact values"
    - "Empty sections display 'None recorded' message"
    - "Loading state shows skeleton matching KPI cards + chart layout"
    - "Reports link appears in web sidebar navigation"
  artifacts:
    - path: "web/src/app/reports/page.tsx"
      provides: "Reports landing page with project list and tab navigation"
    - path: "web/src/app/reports/project/[id]/page.tsx"
      provides: "Single project report view"
    - path: "web/src/app/reports/rollup/page.tsx"
      provides: "Portfolio rollup dashboard"
    - path: "web/src/app/reports/components/BudgetPieChart.tsx"
      provides: "Recharts donut pie chart for budget spent vs remaining"
    - path: "web/src/app/reports/components/ScheduleBarChart.tsx"
      provides: "Recharts bar chart for milestone completion"
    - path: "web/src/app/reports/components/SafetyLineChart.tsx"
      provides: "Recharts line chart for safety incidents over time"
  key_links:
    - from: "web/src/app/reports/project/[id]/page.tsx"
      to: "/api/reports/project/[id]"
      via: "fetch in useEffect"
      pattern: "fetch.*api/reports/project"
    - from: "web/src/app/reports/rollup/page.tsx"
      to: "/api/reports/rollup"
      via: "fetch in useEffect"
      pattern: "fetch.*api/reports/rollup"
    - from: "web/src/app/layout.tsx"
      to: "/reports"
      via: "nav link"
      pattern: "href.*reports"
---

<objective>
Build the web report UI: chart components (Recharts), single-project report page, portfolio rollup page, and navigation integration.

Purpose: REPORT-01 (single-project view), REPORT-02 (portfolio rollup view), REPORT-04 (chart visualizations) all require web UI. This plan delivers the complete web reporting experience using data from Plan 02's API routes.
Output: Full /reports web route with project reports, portfolio rollup, interactive charts, and navigation link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-UI-SPEC.md
@.planning/phases/19-reporting-dashboards/19-01-SUMMARY.md
@.planning/phases/19-reporting-dashboards/19-02-SUMMARY.md
@web/src/app/finance/page.tsx
@web/src/app/layout.tsx
@web/src/lib/reports/types.ts
@web/src/lib/reports/constants.ts
@web/src/lib/reports/chart-config.ts

<interfaces>
From web/src/app/finance/page.tsx (layout pattern to replicate):
- Page container: `padding: 20, maxWidth: 1200, margin: "0 auto"`
- Header card: `background: "var(--surface)", borderRadius: 14, padding: 20, border: "1px solid rgba(105,210,148,0.08)"`
- Section heading: `fontSize: 11, fontWeight: 800, letterSpacing: 2, color: "var(--accent)"`
- Stat card: `textAlign: "center", padding: 14, background: "rgba(242,158,61,0.06)", borderRadius: 10`
- List row: `background: "var(--surface)", borderRadius: 10, padding: 12, marginBottom: 8, display: "flex", justifyContent: "space-between", alignItems: "center"`
- Status badge: `fontSize: 8, fontWeight: 900, padding: "3px 8px", borderRadius: 4`

From web/src/app/layout.tsx (nav groups):
- INTEL group: { href: "/ops", ... }, { href: "/hub", ... }, etc.
- Reports should be added to INTEL group per D-66

From web/src/lib/reports/chart-config.ts (Plan 01):
- CHART_ANIMATION, CHART_HEIGHT, PIE_CONFIG, BAR_CONFIG, LINE_CONFIG
- CHART_AXIS_STYLE, CHART_GRID_STYLE, CHART_TOOLTIP_STYLE

From web/src/lib/reports/constants.ts (Plan 01):
- CHART_COLORS (CSS vars), CHART_COLORS_HEX (for Recharts fills)
- HEALTH_LABELS, HEALTH_THRESHOLDS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reusable chart and UI components</name>
  <files>web/src/app/reports/components/StatCard.tsx, web/src/app/reports/components/HealthBadge.tsx, web/src/app/reports/components/BudgetPieChart.tsx, web/src/app/reports/components/ScheduleBarChart.tsx, web/src/app/reports/components/SafetyLineChart.tsx, web/src/app/reports/components/SkeletonReport.tsx, web/src/app/reports/components/PortfolioTable.tsx, web/src/app/reports/components/ReportHeader.tsx</files>
  <read_first>web/src/app/finance/page.tsx, web/src/lib/reports/types.ts, web/src/lib/reports/constants.ts, web/src/lib/reports/chart-config.ts, .planning/phases/19-reporting-dashboards/19-UI-SPEC.md</read_first>
  <action>
Create 8 components in web/src/app/reports/components/. All client components must have `"use client"` directive. Follow existing inline style pattern from finance/page.tsx.

**1. StatCard.tsx** (per UI-SPEC KPI Stat Card):
```
"use client";
Props: { value: string; label: string; color?: string }
Render: div with textAlign center, padding 16, background rgba({accent-rgb},0.06), borderRadius 10
  - Value: fontSize 24, fontWeight 800, color prop or var(--accent)
  - Label: fontSize 8, fontWeight 800, color var(--muted), textTransform uppercase
```

**2. HealthBadge.tsx** (per UI-SPEC Health Score Badge):
```
"use client";
Props: { score: number; color: HealthColor; label: string }
Render: inline-flex container, alignItems center, gap 4, padding "4px 8px", borderRadius 6
  - Dot: width 8, height 8, borderRadius 50%, background mapped from color (green=#69D294, yellow=#FCC757, red=#D94D48)
  - Label: fontSize 8, fontWeight 800, color same as dot
```

**3. BudgetPieChart.tsx** (per UI-SPEC Chart Types + D-19, D-24):
```
"use client";
import { PieChart, Pie, Cell, ResponsiveContainer, Tooltip } from "recharts";
Props: { spent: number; remaining: number; mini?: boolean }
Data: [{name: "Spent", value: spent, color: CHART_COLORS_HEX.primary}, {name: "Remaining", value: remaining, color: CHART_COLORS_HEX.secondary}]
Render: ResponsiveContainer width="100%" height={mini ? CHART_HEIGHT.mini : CHART_HEIGHT.default}
  - PieChart > Pie with innerRadius/outerRadius from PIE_CONFIG (donut)
  - Cell fills from data colors
  - Tooltip with dollar formatting: (value: number) => `$${value.toLocaleString()}`
  - Inline label (D-23): color dot + label below chart, not separate legend
  - Animation: animationDuration from CHART_ANIMATION.duration
  - Center text showing percentage complete
```

**4. ScheduleBarChart.tsx** (per UI-SPEC + D-19, D-26):
```
"use client";
import { BarChart, Bar, XAxis, YAxis, ResponsiveContainer, Tooltip, CartesianGrid } from "recharts";
Props: { milestones: Array<{ name: string; percentComplete: number }> }
Render: ResponsiveContainer height={CHART_HEIGHT.default}
  - BarChart > Bar with fill CHART_COLORS.secondary, barSize from BAR_CONFIG.barSize, radius from BAR_CONFIG.radius
  - XAxis with dataKey "name", tick style from CHART_AXIS_STYLE
  - YAxis domain [0, 100], tick style from CHART_AXIS_STYLE
  - CartesianGrid with stroke and strokeOpacity from CHART_GRID_STYLE
  - Tooltip styled per CHART_TOOLTIP_STYLE, formatter shows "%"
  - Top 5-8 milestones only (per D-26)
  - Animation: animationDuration from CHART_ANIMATION.duration
```

**5. SafetyLineChart.tsx** (per UI-SPEC + D-19, D-25):
```
"use client";
import { LineChart, Line, XAxis, YAxis, ResponsiveContainer, Tooltip, CartesianGrid } from "recharts";
Props: { data: Array<{ month: string; incidents: number }> }
Render: ResponsiveContainer height={CHART_HEIGHT.default}
  - LineChart > Line with stroke CHART_COLORS_HEX.alert, strokeWidth from LINE_CONFIG.strokeWidth, dot with r from LINE_CONFIG.dotRadius
  - Monthly data points (per D-25)
  - Axis styles from CHART_AXIS_STYLE, grid from CHART_GRID_STYLE
  - Tooltip styled per CHART_TOOLTIP_STYLE
  - Animation: animationDuration from CHART_ANIMATION.duration
```

**6. SkeletonReport.tsx** (per UI-SPEC Skeleton Loading):
```
"use client";
No props.
Render: shimmer animation via CSS keyframes (linear-gradient, backgroundSize 200%, animation shimmer 1.5s infinite)
  - 4 KPI skeleton cards: height 72px in stat card grid (gridTemplateColumns: repeat(auto-fit, minmax(140px, 1fr)), gap 8)
  - 1 chart skeleton block: height 240px, borderRadius 14
  - 4 list skeleton rows: height 48px, gap 8
All with background var(--surface), borderRadius 10.
```

**7. PortfolioTable.tsx** (per UI-SPEC Portfolio Rollup Table + D-36, D-39, D-40):
```
"use client";
Props: { projects: PortfolioProject[]; onSort: (column: string) => void; sortColumn: string; sortOrder: "asc" | "desc" }
Render: table container background var(--surface), borderRadius 14, overflow hidden
  - Header row: background var(--panel), padding "8px 16px", fontSize 8, fontWeight 800, color var(--muted), letterSpacing 1, textTransform uppercase
  - Columns: Project Name, Status, Health, Contract Value, Billed %, Open Issues, Schedule, Features
  - Sortable columns: click header to toggle sort (per D-39). Sort indicator: fontSize 8, color var(--accent)
  - Data rows: padding "12px 16px", borderBottom "1px solid rgba(51,84,94,0.3)", cursor pointer
  - Row hover: background rgba(242,158,61,0.04)
  - Project name: fontSize 12, fontWeight 800
  - Metric columns: fontSize 12, fontWeight 400, textAlign right
  - Health column: inline HealthBadge component
  - Feature coverage: "{N}/6 features active" badge (per D-16c)
  - Click row: navigate to /reports/project/{id} via router.push (per D-40)
```

**8. ReportHeader.tsx** (per UI-SPEC Report Page Shell + D-10):
```
"use client";
Props: { projectName?: string; clientName?: string; health?: HealthScore; generatedAt: string }
Render: header card matching finance page pattern
  - background var(--surface), borderRadius 14, padding 20, border "1px solid rgba(105,210,148,0.08)"
  - Label: "REPORTS" in 12px weight 800 letterSpacing 3 color var(--green) uppercase
  - Title: projectName || "Portfolio Rollup" in 24px weight 800
  - Subtitle: clientName (if project) or "Cross-project financial dashboard" in 12px color var(--muted)
  - Right side: HealthBadge if health provided
  - Generated timestamp: "Generated {formatted date}" in 12px color var(--muted) (per D-16b)
```
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && ls src/app/reports/components/*.tsx | wc -l && grep -l "use client" src/app/reports/components/*.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 8 component files exist in web/src/app/reports/components/
    - All 8 files contain "use client" directive
    - BudgetPieChart.tsx contains "PieChart" and "Pie" imports from "recharts"
    - BudgetPieChart.tsx contains "innerRadius" prop matching PIE_CONFIG
    - ScheduleBarChart.tsx contains "BarChart" and "Bar" imports from "recharts"
    - SafetyLineChart.tsx contains "LineChart" and "Line" imports from "recharts"
    - SkeletonReport.tsx contains "shimmer" animation CSS
    - PortfolioTable.tsx contains "onSort" prop
    - PortfolioTable.tsx contains "router.push" or link to "/reports/project/"
    - HealthBadge.tsx contains "#69D294" or "var(--green)" for green color
    - ReportHeader.tsx contains "REPORTS" label text
    - StatCard.tsx contains fontSize 24 for value and fontSize 8 for label
  </acceptance_criteria>
  <done>8 reusable report components created with Recharts charts, matching UI-SPEC exactly. All use inline styles following finance page pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Report pages, layout, nav integration, and error boundary</name>
  <files>web/src/app/reports/page.tsx, web/src/app/reports/project/[id]/page.tsx, web/src/app/reports/rollup/page.tsx, web/src/app/reports/layout.tsx, web/src/app/reports/error.tsx, web/src/app/layout.tsx</files>
  <read_first>web/src/app/finance/page.tsx, web/src/app/layout.tsx, web/src/app/reports/components/StatCard.tsx, web/src/app/reports/components/BudgetPieChart.tsx, web/src/lib/reports/types.ts, web/src/lib/reports/constants.ts, .planning/phases/19-reporting-dashboards/19-UI-SPEC.md</read_first>
  <action>
**1. web/src/app/reports/layout.tsx**:
Simple pass-through layout (no special wrapping needed): `export default function ReportsLayout({ children }: { children: React.ReactNode }) { return <>{children}</>; }`

**2. web/src/app/reports/error.tsx** (per established error.tsx pattern):
```
"use client";
export default function ReportsError({ error, reset }: { error: Error; reset: () => void }) {
  // Display: "Report data could not be loaded. Check your connection and try again." per UI-SPEC Copywriting
  // "Try Again" button calling reset()
  // Style matching finance page error pattern
}
```

**3. web/src/app/reports/page.tsx** (Reports landing + tab navigation):
```
"use client";
```
- State: `activeTab` ("project" | "rollup"), `projects` array, `loading` boolean
- On mount (useEffect): fetch /api/reports/health to verify tables, then fetch project list from /api/reports/rollup (reuse rollup endpoint to get project names and health badges)
- Tab bar (per UI-SPEC Tab Bar): 3 tabs -- "PROJECT REPORT" / "PORTFOLIO ROLLUP" / "SCHEDULES"
  - Container: display flex, gap 0, borderRadius 8, overflow hidden, marginBottom 16
  - Active tab: background var(--accent), color var(--bg)
  - Inactive tab: background var(--surface), color var(--muted), cursor pointer
  - Tab font: fontSize 8, fontWeight 800, letterSpacing 1, textTransform uppercase
- When "PROJECT REPORT" active: Show project list. Each project is a clickable card (background var(--surface), borderRadius 10, padding 12) linking to /reports/project/{id}. Shows project name, client, health badge.
- When "PORTFOLIO ROLLUP" active: Redirect/navigate to /reports/rollup
- When "SCHEDULES" active: Show placeholder text "Scheduled reports coming soon" (Phase 19b scope)
- Empty state (per UI-SPEC Copywriting): "No Projects Yet" heading + "Create your first project..." body + "View Demo Report" button
- Loading: Show SkeletonReport component
- Page container: padding 20, maxWidth 1200, margin "0 auto"

**4. web/src/app/reports/project/[id]/page.tsx** (Single-project report per REPORT-01):
```
"use client";
```
- Extract id from URL params via `useParams()`
- State: `report` (ProjectReport | null), `loading` boolean, `error` string | null
- On mount: fetch `/api/reports/project/${id}`, set report state
- Layout per D-05 (stat cards + lists):
  - ReportHeader with project name, client, health, generated_at
  - KPI stat card grid (per UI-SPEC): gridTemplateColumns repeat(auto-fit, minmax(140px, 1fr)), gap 8
    - Contract Value (color var(--accent))
    - Total Billed (color var(--cyan))
    - % Complete (color var(--green))
    - Change Order Net (color var(--gold))
    - Open Issues (color var(--red))
  - Budget section: heading "BUDGET & FINANCIALS" (per D-02), BudgetPieChart, then budget summary stats (per D-13: contract value, total billed, % complete, change order net, retainage)
  - Schedule section: heading "SCHEDULE & MILESTONES" (per D-02), ScheduleBarChart with milestones, progress bars (per D-12: ProgressBar component inline -- track background rgba(51,84,94,0.3) borderRadius 4 height 8, fill with health color)
  - Issues section: heading "ISSUES & RISKS" (per D-02), list of RFIs and change orders with clickable items (per D-11: link to /projects/{id}/rfis/{rfiId}). Show criticalOpen count prominently
  - Safety section: heading "SAFETY" (per D-16), severity breakdown (minor/moderate/serious counts), days since last incident, incident list when items exist. SafetyLineChart with monthly data
  - Team section: heading "TEAM & ACTIVITY" (per D-14), member count, role breakdown, 3-5 recent activity entries
  - Documents section: heading "DOCUMENTS & PHOTOS" (per D-15), document count + photo count stat cards
  - Per D-08: ALL sections always shown. Empty sections show "None recorded" (per UI-SPEC empty section text)
  - Per D-16b: each section shows "Last updated {relative time}" timestamp
  - Per D-56: failed sections show error card with "This section failed to load." + "Retry" button
- Loading: SkeletonReport
- Error: error card with "Report data could not be loaded..." per UI-SPEC

**5. web/src/app/reports/rollup/page.tsx** (Portfolio rollup per REPORT-02):
```
"use client";
```
- State: `rollup` (PortfolioRollup | null), `loading`, `statusFilter` ("Active" default), `sortColumn` ("name" default), `sortOrder` ("asc" default)
- On mount and when filters change: fetch `/api/reports/rollup?status=${statusFilter}&sort=${sortColumn}&order=${sortOrder}`
- Layout per D-36 (summary KPIs + table + charts):
  - ReportHeader with "Portfolio Rollup" title
  - Filter bar: status select (Active/Delayed/Completed/All per D-39), refresh button
  - KPI stat cards: Total Contract Value, Total Billed, Total Remaining, Change Order Impact, Aggregate Health
  - PortfolioTable component with sortable columns (per D-39, D-40)
  - Per D-42: show all projects with status badges, active by default
  - Per D-41: aggregate health score badge prominently displayed
- Loading: SkeletonReport

**6. Update web/src/app/layout.tsx**:
Add Reports link to the INTEL nav group (per D-66). Insert `{ href: "/reports", label: "Reports" }` into the INTEL links array, after "Angelic AI" and before any existing items. The INTEL group currently has: Ops, Hub, Security, Pricing, Angelic AI. Add Reports after Angelic AI.
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && test -f src/app/reports/page.tsx && test -f src/app/reports/project/\\[id\\]/page.tsx && test -f src/app/reports/rollup/page.tsx && grep "reports" src/app/layout.tsx | head -3</automated>
  </verify>
  <acceptance_criteria>
    - web/src/app/reports/page.tsx contains "use client" and "PROJECT REPORT" and "PORTFOLIO ROLLUP" tab labels
    - web/src/app/reports/page.tsx contains "No Projects Yet" empty state text
    - web/src/app/reports/project/[id]/page.tsx contains "use client" and "api/reports/project" fetch
    - web/src/app/reports/project/[id]/page.tsx contains "BUDGET" section heading
    - web/src/app/reports/project/[id]/page.tsx contains "SCHEDULE" section heading
    - web/src/app/reports/project/[id]/page.tsx contains "SAFETY" section heading
    - web/src/app/reports/project/[id]/page.tsx contains "None recorded" empty section text
    - web/src/app/reports/project/[id]/page.tsx contains "BudgetPieChart" import
    - web/src/app/reports/project/[id]/page.tsx contains "ScheduleBarChart" import
    - web/src/app/reports/project/[id]/page.tsx contains "SafetyLineChart" import
    - web/src/app/reports/rollup/page.tsx contains "use client" and "api/reports/rollup" fetch
    - web/src/app/reports/rollup/page.tsx contains "PortfolioTable" import
    - web/src/app/reports/rollup/page.tsx contains statusFilter state or select element
    - web/src/app/layout.tsx contains href="/reports" in the nav groups
    - web/src/app/reports/error.tsx contains "Report data could not be loaded"
    - web/src/app/reports/layout.tsx exists
  </acceptance_criteria>
  <done>Complete web reporting UI: /reports landing with tabs, /reports/project/[id] with all 6 data sections and 3 chart types, /reports/rollup with KPI cards and sortable table, navigation link in sidebar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of web reporting UI</name>
  <files>web/src/app/reports/page.tsx</files>
  <action>Human verifies the complete web reporting UI renders correctly with charts, navigation, and data.</action>
  <what-built>Complete web reporting UI with interactive Recharts charts, single-project reports, and portfolio rollup dashboard</what-built>
  <how-to-verify>
    1. Start dev server: cd web && npm run dev
    2. Navigate to http://localhost:3000/reports
    3. Verify "Reports" link appears in the INTEL nav group in the top navigation bar
    4. Verify tab bar shows "PROJECT REPORT" / "PORTFOLIO ROLLUP" / "SCHEDULES"
    5. If projects exist: click a project to view its report. Verify all 6 sections (Budget, Schedule, Issues, Safety, Team, Documents) are visible
    6. Verify budget pie chart renders as a donut (hole in center) with accent/cyan colors
    7. Verify schedule bar chart renders with cyan bars
    8. Verify safety line chart renders with red line and dots
    9. Click "PORTFOLIO ROLLUP" tab -- verify KPI summary cards and project table
    10. Verify sorting works on the portfolio table columns
    11. Verify health badges show colored dots with correct labels
    12. If no projects: verify empty state shows "No Projects Yet" message
  </how-to-verify>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms reports page renders
    - Human confirms charts display with correct colors
    - Human confirms navigation link is visible
  </acceptance_criteria>
  <done>Human approves the visual appearance of the web reporting UI</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params | Project ID from URL could be tampered |
| API response | Client renders API data -- must handle malformed responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | I (Info Disclosure) | project/[id] page | mitigate | API already enforces RLS auth. Client-side shows only what API returns |
| T-19-08 | T (Tampering) | URL params | accept | Project ID validated by API (returns 404 for non-existent). No client-side security impact |
</threat_model>

<verification>
- `npm run dev` starts without errors
- /reports route renders with tab bar
- /reports/project/{id} renders with charts
- /reports/rollup renders with table
- Charts use correct Recharts components (Pie, Bar, Line)
- Navigation shows Reports link
</verification>

<success_criteria>
- User can navigate to /reports from sidebar
- User can view a single-project report with all 6 sections and 3 chart types
- User can view portfolio rollup with KPI cards and sortable project table
- Charts render with correct colors (accent donut, cyan bars, red line)
- Empty sections show "None recorded"
- Loading shows skeleton layout
- Human checkpoint approves visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-03-SUMMARY.md`
</output>
