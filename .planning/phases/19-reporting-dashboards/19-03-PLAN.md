---
phase: 19-reporting-dashboards
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/reports/db-schema.sql
  - web/src/lib/reports/db-views.sql
autonomous: true
requirements:
  - REPORT-01
  - REPORT-02
must_haves:
  truths:
    - "All 8 cs_report_* tables exist with correct columns"
    - "RLS policies exist on all report tables"
    - "Database views exist for report aggregation"
    - "Indexes exist for performance-critical queries"
  artifacts:
    - path: "web/src/lib/reports/db-schema.sql"
      provides: "Complete SQL schema for report tables"
      contains: "cs_report_schedules"
    - path: "web/src/lib/reports/db-views.sql"
      provides: "Database views for aggregation"
  key_links:
    - from: "db-schema.sql"
      to: "Supabase database"
      via: "manual SQL execution"
      pattern: "create table cs_report_"
---

<objective>
Create the complete database schema SQL files for all report tables, RLS policies, indexes, and aggregation views.

Purpose: Define the data persistence layer that API routes and features depend on.
Output: SQL schema files ready for Supabase execution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-RESEARCH.md
@ready player 8/SupabaseService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create report tables SQL schema with RLS and indexes</name>
  <files>web/src/lib/reports/db-schema.sql</files>
  <read_first>
    - ready player 8/SupabaseService.swift (existing table schemas, especially cs_projects, cs_contracts column names)
    - .planning/phases/19-reporting-dashboards/19-RESEARCH.md (SQL schema section with all 8 tables)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-91 through D-94, D-50h, D-64b, D-98, D-99, D-112)
  </read_first>
  <action>
Create `web/src/lib/reports/db-schema.sql` with the complete schema from RESEARCH.md, enhanced with all decision requirements:

1. **cs_report_schedules** (D-54, D-91, D-92):
   - id uuid PK, user_id uuid NOT NULL references auth.users(id), org_id uuid per D-92
   - frequency text NOT NULL check ('daily','weekly','biweekly','monthly') per D-49
   - day_of_week int, day_of_month int, time_utc time NOT NULL
   - timezone text NOT NULL default 'America/New_York' per D-50j
   - recipients uuid[] NOT NULL per D-50e (team members only)
   - sections text[] default '{}' per D-50l
   - is_active boolean NOT NULL default true per D-50f
   - last_run_at timestamptz, next_run_at timestamptz per D-50t
   - created_at timestamptz default now(), updated_at timestamptz default now()

2. **cs_report_delivery_log** (D-50h, D-91, D-92):
   - id uuid PK, schedule_id uuid references cs_report_schedules(id)
   - user_id uuid NOT NULL, org_id uuid
   - recipients uuid[], status text check ('sent','failed','partial')
   - error_message text, pdf_storage_path text per D-34l
   - email_html text per D-50h content archive
   - created_at timestamptz default now()

3. **cs_report_shared_links** (D-64b, D-91, D-92):
   - id uuid PK, token text NOT NULL UNIQUE
   - user_id uuid NOT NULL, org_id uuid, project_id uuid
   - report_type text check ('project','rollup')
   - expires_at timestamptz NOT NULL (30 day default per D-64b)
   - view_count int default 0, max_views_per_day int default 100 per D-64e
   - is_revoked boolean default false
   - created_at timestamptz default now()

4. **cs_report_history** (D-99, D-91, D-92):
   - id uuid PK, user_id uuid NOT NULL, org_id uuid, project_id uuid
   - report_type text NOT NULL, snapshot_data jsonb NOT NULL
   - pdf_storage_path text, created_at timestamptz default now()

5. **cs_report_templates** (D-93, D-94, D-91, D-92):
   - id uuid PK, user_id uuid NOT NULL, org_id uuid
   - name text NOT NULL, description text
   - template_config jsonb NOT NULL (section ordering, visibility, customCSS)
   - is_default boolean default false
   - created_at/updated_at timestamptz

6. **cs_report_comments** (D-98, D-91, D-92):
   - id uuid PK, user_id uuid NOT NULL, org_id uuid
   - report_history_id uuid references cs_report_history(id)
   - section text NOT NULL, content text NOT NULL
   - parent_id uuid references cs_report_comments(id) for threads
   - created_at timestamptz default now()

7. **cs_report_annotations** (D-98, D-91, D-92):
   - id uuid PK, user_id uuid NOT NULL, org_id uuid
   - report_history_id uuid references cs_report_history(id)
   - chart_id text NOT NULL, fabric_json jsonb NOT NULL
   - created_at timestamptz default now()

8. **cs_report_audit_log** (D-112, D-91, D-92):
   - id uuid PK, user_id uuid, org_id uuid
   - action text NOT NULL ('viewed','exported','shared','scheduled')
   - report_type text, project_id uuid, device_info text
   - created_at timestamptz default now()

Enable RLS on all 8 tables per D-63:
```sql
ALTER TABLE cs_report_* ENABLE ROW LEVEL SECURITY;
```

RLS policies for each table:
- SELECT: user_id = auth.uid() OR org_id IN (SELECT org_id FROM user_orgs WHERE user_id = auth.uid())
- INSERT: user_id = auth.uid()
- UPDATE: user_id = auth.uid() (schedules, templates only)
- DELETE: user_id = auth.uid() (schedules, templates only)
- Special: cs_report_audit_log INSERT open to authenticated users (logging)

Indexes per D-61 and RESEARCH.md:
- idx_report_schedules_next_run on cs_report_schedules(next_run_at) WHERE is_active = true
- idx_report_delivery_log_schedule on cs_report_delivery_log(schedule_id, created_at DESC)
- idx_report_shared_links_token on cs_report_shared_links(token) WHERE is_revoked = false
- idx_report_history_project on cs_report_history(project_id, created_at DESC)
- idx_report_audit_log_user on cs_report_audit_log(user_id, created_at DESC)
  </action>
  <verify>
    <automated>grep -c "create table cs_report_" "/Users/beverlyhunter/Desktop/ready player 8/web/src/lib/reports/db-schema.sql" && grep -c "enable row level security" "/Users/beverlyhunter/Desktop/ready player 8/web/src/lib/reports/db-schema.sql" && grep -c "create index" "/Users/beverlyhunter/Desktop/ready player 8/web/src/lib/reports/db-schema.sql"</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 8 CREATE TABLE statements (cs_report_schedules, cs_report_delivery_log, cs_report_shared_links, cs_report_history, cs_report_templates, cs_report_comments, cs_report_annotations, cs_report_audit_log)
    - All 8 tables have org_id column per D-92
    - RLS enabled on all 8 tables (8 ALTER TABLE ... ENABLE ROW LEVEL SECURITY statements)
    - At least 5 CREATE INDEX statements per D-61
    - cs_report_schedules has frequency CHECK constraint with 'daily','weekly','biweekly','monthly'
    - cs_report_shared_links has token UNIQUE constraint
  </acceptance_criteria>
  <done>Complete SQL schema with 8 tables, RLS policies, and indexes ready for Supabase execution.</done>
</task>

<task type="auto">
  <name>Task 2: Create database views for report aggregation</name>
  <files>web/src/lib/reports/db-views.sql</files>
  <read_first>
    - web/src/lib/reports/db-schema.sql (just created)
    - ready player 8/SupabaseService.swift (existing table names: cs_projects, cs_contracts, cs_project_tasks, etc.)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-56g standard vs materialized views)
  </read_first>
  <action>
Create `web/src/lib/reports/db-views.sql` per D-56g:

1. **Standard views** (light aggregation, always fresh):
   - `v_project_budget_summary`: JOIN cs_projects + cs_contracts, compute contract_value_total, total_billed, change_order_net per project
   - `v_project_issue_counts`: COUNT of open RFIs, open change orders, critical issues per project
   - `v_project_team_summary`: COUNT of team members, role breakdown per project
   - `v_project_safety_summary`: COUNT incidents by severity, MAX date for days-since-last per project

2. **Materialized views** (heavy aggregation, periodic refresh per D-56g):
   - `mv_portfolio_financial_rollup`: Aggregate all projects' financial metrics for rollup dashboard
   - `mv_monthly_spend_trend`: Monthly financial data across portfolio for trend charts per D-43
   - Include `REFRESH MATERIALIZED VIEW CONCURRENTLY` commands as comments

3. Add `CREATE OR REPLACE FUNCTION refresh_report_views()` that refreshes all materialized views, callable via pg_cron per D-56g

Note: These views are reference SQL. The API routes will use them if available but fall back to raw queries (per D-56g: "Stale materialized views fall back to raw queries").
  </action>
  <verify>
    <automated>grep -c "CREATE.*VIEW" "/Users/beverlyhunter/Desktop/ready player 8/web/src/lib/reports/db-views.sql" && grep -c "MATERIALIZED" "/Users/beverlyhunter/Desktop/ready player 8/web/src/lib/reports/db-views.sql"</automated>
  </verify>
  <acceptance_criteria>
    - At least 4 standard views (v_project_budget_summary, v_project_issue_counts, v_project_team_summary, v_project_safety_summary)
    - At least 2 materialized views (mv_portfolio_financial_rollup, mv_monthly_spend_trend)
    - refresh_report_views function exists
    - Views reference existing table names (cs_projects, cs_contracts, cs_project_tasks)
  </acceptance_criteria>
  <done>Database views defined for both standard and materialized aggregation patterns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL execution | Schema applied to production database |
| RLS policies | Gate between users and report data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Information Disclosure | RLS policies | mitigate | All tables have RLS enabled with user_id = auth.uid() or org_id membership check |
| T-19-06 | Elevation of Privilege | cs_report_audit_log | mitigate | INSERT-only for non-owners; no UPDATE/DELETE policies on audit log |
| T-19-07 | Tampering | cs_report_templates | mitigate | Only template owner can UPDATE/DELETE via RLS policy |
</threat_model>

<verification>
- db-schema.sql has 8 CREATE TABLE, 8 ENABLE RLS, 5+ CREATE INDEX
- db-views.sql has 4+ standard views and 2+ materialized views
- All tables include org_id column per D-92
</verification>

<success_criteria>
- Complete SQL schema covers D-91, D-92, D-93, D-94, D-50h, D-64b, D-98, D-99, D-112
- RLS policies enforce per-user data isolation per D-63
- Materialized views support portfolio rollup performance per D-56g
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-03-SUMMARY.md`
</output>
