---
phase: 19-reporting-dashboards
plan: 15
type: execute
wave: 5
depends_on: ["19-05"]
files_modified:
  - web/src/app/reports/components/FeatureDiscovery.tsx
  - web/src/app/reports/components/DemoReport.tsx
  - web/src/app/reports/components/HelpSection.tsx
  - web/src/app/reports/components/TemplateManager.tsx
  - web/src/app/reports/components/AuditDashboard.tsx
  - web/src/lib/reports/csv-import.ts
autonomous: true
requirements:
  - REPORT-01
must_haves:
  truths:
    - "New users see tooltip tour on first visit"
    - "Demo report renders with sample construction data"
    - "Help section links to documentation"
    - "Template manager supports create/edit/apply"
    - "Audit dashboard shows access patterns"
    - "CSV import wizard maps columns"
  artifacts:
    - path: "web/src/app/reports/components/DemoReport.tsx"
      provides: "Demo report with sample data per D-66c"
    - path: "web/src/app/reports/components/TemplateManager.tsx"
      provides: "Report template CRUD per D-93/94"
    - path: "web/src/app/reports/components/AuditDashboard.tsx"
      provides: "Audit trail visualization per D-112"
  key_links:
    - from: "TemplateManager.tsx"
      to: "/api/reports/templates"
      via: "Template CRUD operations"
      pattern: "template"
---

<objective>
Build feature discovery (tooltips, demo, help), template management, audit dashboard, and CSV import.

Purpose: Cover D-66b/c/d, D-93/94, D-112, D-115 user experience and administration features.
Output: Discovery components, template manager, audit dashboard, CSV import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature discovery + demo report + help section</name>
  <files>web/src/app/reports/components/FeatureDiscovery.tsx, web/src/app/reports/components/DemoReport.tsx, web/src/app/reports/components/HelpSection.tsx</files>
  <read_first>
    - web/src/app/reports/page.tsx (landing page to integrate discovery into)
    - .planning/phases/19-reporting-dashboards/19-UI-SPEC.md (copywriting for empty states and demo)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-66b, D-66c, D-66d)
  </read_first>
  <action>
1. Create `FeatureDiscovery.tsx` ("use client"):
   - Per D-66b: first-time tooltip tour (dismissable, re-accessible from help menu)
   - Track "tour completed" in localStorage
   - Tooltip steps: 1) "Reports overview" on landing, 2) "Project reports" on first project, 3) "Portfolio rollup" on rollup tab, 4) "Export options" on export button, 5) "Schedule delivery" on schedules tab
   - Each tooltip: positioned near target element, with "Next" / "Skip tour" / "Step X of 5" indicator
   - Highlight target element (semi-transparent overlay around tooltip target)
   - Per D-66b: contextual links on related pages ('View full report ->' on project detail) -- export as helper function

2. Create `DemoReport.tsx`:
   - Per D-66c: demo report with sample construction data for new users with no projects
   - Hardcoded sample data: "Riverside Mixed-Use Development" project with realistic construction metrics
   - Budget: $2.4M contract, $1.8M billed, 75% complete
   - Schedule: 8 milestones at various % complete
   - Safety: 3 minor incidents, 1 moderate, 14 days since last
   - Team: 12 members, 5 recent activity entries
   - Per UI-SPEC: banner "This is a demo report with sample data. Create a project to see your own data."
   - Uses all the same section components (BudgetSection, ScheduleSection, etc.) with demo data
   - "View Demo Report" CTA button on empty state

3. Create `HelpSection.tsx` ("use client"):
   - Per D-66d: in-app help section
   - Collapsible panel with FAQ items about reports
   - "Learn more" link to external documentation URL
   - "Restart tour" button to trigger FeatureDiscovery again
   - "Keyboard shortcuts" link to shortcut help panel (per D-108)
   - Accessible from help icon in report header
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/reports/components/FeatureDiscovery.tsx src/app/reports/components/DemoReport.tsx src/app/reports/components/HelpSection.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - FeatureDiscovery.tsx uses localStorage to track tour completion per D-66b
    - FeatureDiscovery.tsx has 5 tooltip steps with "Next" and "Skip tour" buttons
    - DemoReport.tsx has hardcoded sample construction data per D-66c
    - DemoReport.tsx shows banner text per UI-SPEC copywriting
    - HelpSection.tsx has "Learn more" external link per D-66d
    - HelpSection.tsx has "Restart tour" button per D-66b
  </acceptance_criteria>
  <done>Feature discovery tour, demo report, and help section implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Template manager + audit dashboard + CSV import</name>
  <files>web/src/app/reports/components/TemplateManager.tsx, web/src/app/reports/components/AuditDashboard.tsx, web/src/lib/reports/csv-import.ts</files>
  <read_first>
    - web/src/lib/reports/db-schema.sql (cs_report_templates, cs_report_audit_log schemas)
    - web/src/lib/reports/types.ts (ReportTemplate type)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-93, D-94, D-112, D-115)
  </read_first>
  <action>
1. Create `TemplateManager.tsx` ("use client"):
   - Per D-93: list built-in templates (static JSON in repo) + user-created templates (from Supabase)
   - Built-in templates: "Standard", "Executive Summary", "Safety Focus", "Financial Detail", "Minimal"
   - Per D-94: tiered customization:
     a. Basic: section ordering + visibility (drag-and-drop reorder, toggle sections on/off)
     b. Advanced: full visual editor (section layout, chart configuration)
     c. Power user: JSON/code-based template editing (textarea with JSON syntax highlighting)
   - Create template: name, description, section config
   - Apply template: load template config, apply to current report view
   - Save template to Supabase cs_report_templates
   - Set default template toggle per D-93
   - Template preview showing what sections are included

2. Create `AuditDashboard.tsx` ("use client"):
   - Per D-112: summarized dashboard showing access patterns and popular reports
   - Fetch from cs_report_audit_log
   - Charts: report views over time (line chart), most viewed reports (bar chart), export types (pie chart)
   - Table: recent access log entries (user, timestamp, report type, project, device)
   - Filter by date range, user, action type
   - Per D-112: device/browser info displayed per entry

3. Create `web/src/lib/reports/csv-import.ts`:
   - Per D-115: generic CSV/Excel import wizard with column mapping
   - Function `parseCSV(csvString)`: parse CSV to array of objects
   - Function `detectColumns(data)`: auto-detect column types (numeric, date, text)
   - Function `mapColumns(data, mapping)`: apply user column mapping to standard schema
   - Per D-115: Procore-specific mapping templates for later
   - Return typed data matching existing Supabase table schemas
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/reports/components/TemplateManager.tsx src/app/reports/components/AuditDashboard.tsx src/lib/reports/csv-import.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - TemplateManager.tsx has 5 built-in template presets per D-93
    - TemplateManager.tsx has 3 customization tiers (basic, advanced, power user) per D-94
    - AuditDashboard.tsx renders access log table with user, timestamp, report type, device per D-112
    - AuditDashboard.tsx has charts for access patterns per D-112
    - csv-import.ts exports parseCSV, detectColumns, mapColumns per D-115
  </acceptance_criteria>
  <done>Template management, audit dashboard, and CSV import operational.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV upload | User-supplied file content |
| Template JSON | User-supplied template configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-39 | Tampering | CSV import | mitigate | Validate CSV content; limit file size to 10MB; sanitize cell values |
| T-19-40 | Tampering | Template config | mitigate | Validate template JSON schema; sanitize custom CSS per D-94 |
| T-19-41 | Information Disclosure | Audit dashboard | mitigate | Admin-only access; RLS on cs_report_audit_log |
</threat_model>

<verification>
- TypeScript compiles all files
- Demo report renders with sample data
- Template manager shows 5 presets
- Audit dashboard queries cs_report_audit_log
</verification>

<success_criteria>
- D-66b/c/d: Feature discovery, demo report, help section
- D-93/94: Template management with tiered customization
- D-112: Audit dashboard with access patterns
- D-115: CSV import wizard with column mapping
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-15-SUMMARY.md`
</output>
