---
phase: 19-reporting-dashboards
plan: 07
type: execute
wave: 4
depends_on: ["19-05"]
files_modified:
  - web/src/lib/reports/pdf-generator.ts
  - web/src/lib/reports/__tests__/pdf-generator.test.ts
  - web/src/app/reports/components/PDFPreview.tsx
  - web/src/app/reports/components/ExportButtonGroup.tsx
autonomous: true
requirements:
  - REPORT-03
must_haves:
  truths:
    - "User can click Export PDF and see a preview"
    - "PDF includes charts as static images"
    - "PDF has header, footer, page numbers, TOC"
    - "PDF uses light theme with white background"
    - "PDF file named {ProjectName}-Report-{YYYY-MM-DD}.pdf"
  artifacts:
    - path: "web/src/lib/reports/pdf-generator.ts"
      provides: "jsPDF + html2canvas PDF generation"
      exports: ["generateReportPDF"]
    - path: "web/src/app/reports/components/PDFPreview.tsx"
      provides: "PDF preview in new tab"
    - path: "web/src/app/reports/components/ExportButtonGroup.tsx"
      provides: "Export PDF/CSV/Excel/PPTX/JSON button group"
  key_links:
    - from: "pdf-generator.ts"
      to: "jspdf"
      via: "import jsPDF from 'jspdf'"
      pattern: "import.*jsPDF"
    - from: "pdf-generator.ts"
      to: "html2canvas"
      via: "DOM capture for charts"
      pattern: "html2canvas"
---

<objective>
Implement client-side PDF generation with jsPDF + html2canvas, PDF preview, and the full export button group for all formats.

Purpose: Deliver REPORT-03 PDF export capability.
Output: PDF generator, preview component, multi-format export buttons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-RESEARCH.md
@.planning/phases/19-reporting-dashboards/19-UI-SPEC.md
@.planning/phases/19-reporting-dashboards/19-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PDF generator with smart page breaks, TOC, branding, and accessibility</name>
  <files>web/src/lib/reports/pdf-generator.ts, web/src/lib/reports/__tests__/pdf-generator.test.ts</files>
  <read_first>
    - .planning/phases/19-reporting-dashboards/19-RESEARCH.md (Pattern 2: Client-Side PDF Generation example)
    - .planning/phases/19-reporting-dashboards/19-UI-SPEC.md (PDF Layout Contract)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-27 through D-34n)
    - web/src/lib/reports/constants.ts (PDF_SETTINGS from Plan 01)
  </read_first>
  <action>
1. Create `web/src/lib/reports/pdf-generator.ts`:

   Export `generateReportPDF(options)` function:
   - Per D-60: client-side PDF generation using jsPDF, near-instant for single reports (no server round-trip)
   - options: { reportElement: HTMLElement; projectName: string; companyName?: string; companyLogo?: string; landscape?: boolean; paperSize?: 'letter' | 'a4'; password?: string; isDraft?: boolean; executiveSummary?: string; confidential?: boolean; reportUrl?: string }

   Per D-34b: auto-detect paper size (US/Canada: Letter, international: A4). Use Intl.DateTimeFormat().resolvedOptions().locale
   Per D-34c: auto-detect orientation (portrait for single-project, landscape for rollup)

   Per D-33: light/print theme -- set white background on capture
   html2canvas options: { scale: 2, useCORS: true, backgroundColor: '#ffffff' } per RESEARCH.md pitfall 1

   Per D-34d: auto-generated table of contents on page 1 with clickable internal links
   Per D-32: header on each page (company logo left max 120px, project name center 18pt bold, date right 10pt)
   Per D-32: footer on each page (page number center 9pt, "Generated by ConstructionOS" left 8pt)
   Per D-34f: confidentiality footer toggleable "Confidential -- {Company Name}" right 8pt
   Per D-34f: DRAFT watermark on preview/test PDFs -- diagonal "DRAFT" at 45deg, 72pt, opacity 0.08

   Per D-34g: optional executive summary text area rendered after TOC
   Per D-30: charts rendered as static images (html2canvas captures them)
   Per D-34j: smart page breaks -- never break mid-chart/mid-table. Measure section heights, add page break before oversized sections
   Per D-34i: QR code on first page bottom-right linking to live web report (use simple QR SVG generation or data URI)
   Per D-34k: tagged PDF for accessibility (add structural tags -- jsPDF supports basic tagging via outline API)
   Per D-34h: password protection via jsPDF encryption if password provided

   Per D-31: filename "{ProjectName}-Report-{YYYY-MM-DD}.pdf"
   Per D-34e: company branding (logo + colors) applied to header/footer

   Return: Promise<Blob>

2. Export `generateRollupPDF(options)` for portfolio rollup PDF (landscape, table-heavy)

3. Create `web/src/lib/reports/__tests__/pdf-generator.test.ts`:
   - Per D-78: test PDF generation produces a Blob
   - Test filename format matches pattern
   - Test paper size auto-detection logic
   - Test orientation auto-detection
   - Mock html2canvas to return a canvas element
   - Mock jsPDF methods and verify they're called with correct args
   - Test DRAFT watermark applied when isDraft=true
   - Test confidentiality footer when confidential=true
   - Test password parameter passed to jsPDF encryption
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx vitest run src/lib/reports/__tests__/pdf-generator.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - pdf-generator.ts imports jsPDF from 'jspdf' and html2canvas
    - generateReportPDF accepts password option per D-34h
    - generateReportPDF includes DRAFT watermark logic per D-34f
    - generateReportPDF has smart page break detection per D-34j
    - generateReportPDF auto-detects paper size per D-34b
    - pdf-generator.test.ts passes all tests
    - File exports both generateReportPDF and generateRollupPDF
  </acceptance_criteria>
  <done>PDF generator handles all D-27 through D-34n requirements with tests.</done>
</task>

<task type="auto">
  <name>Task 2: PDF preview component and multi-format export button group</name>
  <files>web/src/app/reports/components/PDFPreview.tsx, web/src/app/reports/components/ExportButtonGroup.tsx</files>
  <read_first>
    - web/src/lib/reports/pdf-generator.ts (just created)
    - .planning/phases/19-reporting-dashboards/19-UI-SPEC.md (Export Button Group spec, Export Flow interaction)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-29, D-47, D-48, D-64)
  </read_first>
  <action>
1. Create `PDFPreview.tsx` ("use client"):
   - Per D-29: opens in new tab/modal with download button
   - Renders the report in light theme (white background, dark text) for PDF capture
   - "Download PDF" button at top triggers generateReportPDF and blob download
   - Per D-34h: password protection checkbox with password field (min 4 chars)
   - Per D-34g: executive summary textarea for optional user notes
   - Per D-34f: confidentiality toggle checkbox
   - Per D-34f: DRAFT watermark shown on preview (removed on final export)
   - Loading spinner during generation

2. Create `ExportButtonGroup.tsx` ("use client"):
   - Per UI-SPEC Export Button Group:
     - Primary CTA: "Export PDF" -- background var(--accent), color var(--bg), fontSize 12, fontWeight 800, padding 8px 24px, borderRadius 8
     - Secondary: "Export CSV", "Export Excel", "Export PowerPoint", "Export JSON" per D-47
     - Secondary style: background var(--surface), color var(--text), fontSize 8, fontWeight 800, padding 8px 16px, borderRadius 8, border var(--border)
     - "Share Report" button per D-64b (generates shareable link, copies to clipboard, toast "Link copied")
   - Per D-47: Excel uses xlsx (SheetJS) -- import xlsx, create workbook with formatted columns, multiple sheets per section, chart images as separate sheets
   - Per D-47: PowerPoint uses pptxgenjs -- each section as a slide with chart images
   - Per D-48: CSV two options via dropdown: 'Summary CSV' (flat table) and 'Detailed CSV' (multi-section)
   - Per D-47: JSON export as formatted JSON download
   - Direct download for CSV/Excel/PPTX/JSON (no preview per UI-SPEC interaction)
   - Per D-34m: "Export All Reports" button for batch export (triggers background job, notification when ready)
   - Props: `{ reportData: ProjectReport | PortfolioRollup; reportRef: React.RefObject<HTMLDivElement>; projectName: string }`
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/reports/components/PDFPreview.tsx src/app/reports/components/ExportButtonGroup.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - PDFPreview.tsx has password checkbox + password input per D-34h
    - PDFPreview.tsx has executive summary textarea per D-34g
    - PDFPreview.tsx has confidentiality toggle per D-34f
    - ExportButtonGroup.tsx has buttons for PDF, CSV, Excel, PowerPoint, JSON per D-47
    - ExportButtonGroup.tsx imports xlsx for Excel export
    - ExportButtonGroup.tsx imports pptxgenjs for PowerPoint export
    - ExportButtonGroup.tsx has "Share Report" button per D-64b
    - CSV dropdown offers Summary and Detailed options per D-48
    - "Export All Reports" button for batch export per D-34m
  </acceptance_criteria>
  <done>PDF preview with all customization options and multi-format export group operational.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> PDF | Executive summary text, company name injected into PDF |
| Password -> encryption | User-supplied password for PDF protection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-16 | Tampering | PDF generation | mitigate | Sanitize executive summary text before PDF rendering (strip HTML tags, limit length) |
| T-19-17 | Information Disclosure | PDF password | mitigate | Use jsPDF built-in encryption (AES), never custom crypto per RESEARCH.md |
| T-19-18 | Denial of Service | Batch export | mitigate | Rate limit batch export to 10 req/min per D-62b, process as background job |
</threat_model>

<verification>
- PDF generator tests pass
- TypeScript compiles all components
- Export button group renders all 5 format options
</verification>

<success_criteria>
- REPORT-03: PDF export with charts, TOC, branding, smart page breaks
- D-47: All export formats (PDF, CSV, Excel, PowerPoint, JSON) available
- D-34h: Optional password protection on PDF
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-07-SUMMARY.md`
</output>
