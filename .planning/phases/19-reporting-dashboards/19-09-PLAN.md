---
phase: 19-reporting-dashboards
plan: 09
type: execute
wave: 4
depends_on: ["19-04"]
files_modified:
  - web/src/app/api/reports/share/route.ts
  - web/src/app/reports/shared/[token]/page.tsx
  - web/src/app/api/reports/export/[type]/route.ts
  - web/src/lib/reports/csv-generator.ts
  - web/src/lib/reports/excel-generator.ts
  - web/src/lib/reports/pptx-generator.ts
autonomous: true
requirements:
  - REPORT-01
  - REPORT-02
  - REPORT-03
must_haves:
  truths:
    - "User can generate shareable report link"
    - "Shared link shows report with auto-masked sensitive data"
    - "Export endpoint serves CSV, Excel, JSON, PowerPoint formats"
    - "Shared links have 30-day expiry and view count tracking"
  artifacts:
    - path: "web/src/app/api/reports/share/route.ts"
      provides: "Shareable link CRUD"
      exports: ["GET", "POST", "DELETE"]
    - path: "web/src/app/reports/shared/[token]/page.tsx"
      provides: "Public shared report view"
    - path: "web/src/lib/reports/csv-generator.ts"
      provides: "CSV export logic"
    - path: "web/src/lib/reports/excel-generator.ts"
      provides: "Excel export with SheetJS"
    - path: "web/src/lib/reports/pptx-generator.ts"
      provides: "PowerPoint export with pptxgenjs"
  key_links:
    - from: "shared/[token]/page.tsx"
      to: "share/route.ts"
      via: "validates token and fetches report"
      pattern: "cs_report_shared_links"
---

<objective>
Build shareable report links (create/revoke/view) and multi-format export generators (CSV, Excel, PowerPoint).

Purpose: Enable report sharing and complete the multi-format export system (D-47, D-48, D-64b through D-64g).
Output: Share API, public report page, CSV/Excel/PPTX generators.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-RESEARCH.md
@.planning/phases/19-reporting-dashboards/19-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shareable links API + public report view + access control</name>
  <files>web/src/app/api/reports/share/route.ts, web/src/app/reports/shared/[token]/page.tsx</files>
  <read_first>
    - web/src/lib/supabase/fetch.ts (getAuthenticatedClient)
    - web/src/lib/reports/db-schema.sql (cs_report_shared_links schema)
    - web/src/lib/reports/types.ts (SharedLink type)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-64b through D-64g)
    - web/src/lib/rate-limit.ts (existing rate limiting)
  </read_first>
  <action>
1. Create `web/src/app/api/reports/share/route.ts`:
   - POST: create shareable link
     - Generate cryptographically random token via crypto.randomUUID() per RESEARCH.md security
     - Per D-64b: 30-day expiry, read-only, live data
     - Per D-64b: view_count tracking, manual revoke support
     - Per D-64e: max_views_per_day default 100
     - Store in cs_report_shared_links with project_id and report_type
     - Return { token, url, expires_at }
   - GET: list user's shared links with view counts
   - DELETE: revoke link (set is_revoked = true) per D-64b
   - Per D-110: bulk revoke support (accept array of link IDs)
   - Auth: getAuthenticatedClient for create/list/revoke

2. Create `web/src/app/reports/shared/[token]/page.tsx`:
   - NO auth required (public page)
   - Validate token against cs_report_shared_links:
     - Check not revoked, not expired, not over daily view limit
     - Increment view_count
   - Per D-64b: live data (fetch current report data, not snapshot)
   - Per D-64f: automatic sensitive field masking -- hide financial totals and personal names, show only percentages and aggregates
   - Per D-64c: show user's custom company branding (logo, colors)
   - Per D-64e: rate limit at 100 views per link per day
   - Render report using same section components but in read-only mode
   - No export buttons on shared view
   - Log access to cs_report_audit_log per D-112

3. Per D-64g: role-based permissions matrix implementation:
   - Admin: full access (view + export + share + schedule)
   - Manager: view + export + share
   - Viewer: view only
   - Check permissions in share/route.ts before allowing share creation
   - Per D-119: three-tier inheritance: org defaults -> project overrides -> report-level overrides
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/api/reports/share/route.ts src/app/reports/shared/\\[token\\]/page.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - share/route.ts uses crypto.randomUUID() for token generation
    - share/route.ts sets 30-day expiry per D-64b
    - shared/[token]/page.tsx validates token expiry and revocation before rendering
    - shared/[token]/page.tsx masks financial totals per D-64f (grep for "mask" or "redact")
    - shared/[token]/page.tsx increments view_count per D-64b
    - share/route.ts checks role permissions per D-64g
    - Bulk revoke support via DELETE with array body per D-110
  </acceptance_criteria>
  <done>Shareable links with 30-day expiry, data masking, branding, and role-based permissions.</done>
</task>

<task type="auto">
  <name>Task 2: CSV, Excel, PowerPoint export generators + export API route</name>
  <files>web/src/app/api/reports/export/[type]/route.ts, web/src/lib/reports/csv-generator.ts, web/src/lib/reports/excel-generator.ts, web/src/lib/reports/pptx-generator.ts</files>
  <read_first>
    - web/src/lib/reports/types.ts (ProjectReport, PortfolioRollup types)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-47, D-48, D-52, D-114)
    - .planning/phases/19-reporting-dashboards/19-RESEARCH.md (SheetJS and pptxgenjs notes)
  </read_first>
  <action>
1. Create `web/src/lib/reports/csv-generator.ts`:
   - Per D-48: two functions:
     - `generateSummaryCSV(report)`: flat table, key metrics as columns (project, budget, billed, %, health, open issues, safety)
     - `generateDetailedCSV(report)`: multi-section with headers per section, all data rows
   - Per D-114: QuickBooks-compatible financial export format (standard CSV with account/amount columns)
   - Return CSV string

2. Create `web/src/lib/reports/excel-generator.ts`:
   - Per D-47: xlsx with formatted columns, multiple sheets, embedded charts
   - Import xlsx (SheetJS)
   - Function `generateExcel(report)`:
     - Sheet 1: "Summary" -- KPI metrics
     - Sheet 2: "Budget" -- financial details with formatted currency columns
     - Sheet 3: "Schedule" -- milestones with % complete
     - Sheet 4: "Issues" -- RFIs and change orders
     - Sheet 5: "Safety" -- incident data
     - Sheet 6: "Charts" -- chart images as base64 (per RESEARCH.md pitfall 6)
   - Return Blob

3. Create `web/src/lib/reports/pptx-generator.ts`:
   - Per D-47: pptxgenjs, each section as a slide with chart images
   - Import pptxgenjs
   - Function `generatePPTX(report, chartImages)`:
     - Slide 1: Title slide with project name, health score, date
     - Slide 2: Budget overview with pie chart image
     - Slide 3: Schedule with bar chart image
     - Slide 4: Safety with line chart image
     - Slide 5: Team summary
     - Slide 6: Key insights (if available)
   - Return Blob

4. Create `web/src/app/api/reports/export/[type]/route.ts`:
   - Per D-52: POST handler for export
   - Accept type param: 'csv-summary', 'csv-detailed', 'excel', 'json', 'pptx'
   - Auth: getAuthenticatedClient
   - Fetch report data, generate appropriate format, return as downloadable response
   - Per D-114: JSON API export endpoint for external tools
   - Per D-56h: emit webhook events on report generation for Zapier/Make integration
   - Per D-34l: optionally store exported file in Supabase Storage for report history
   - Per D-62b: rate limit on export endpoint (tighter than view: 10 req/min for PDF per D-62b)
   - Log to cs_report_audit_log per D-112
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/lib/reports/csv-generator.ts src/lib/reports/excel-generator.ts src/lib/reports/pptx-generator.ts src/app/api/reports/export/\\[type\\]/route.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - csv-generator.ts exports generateSummaryCSV and generateDetailedCSV per D-48
    - excel-generator.ts imports xlsx and creates workbook with multiple sheets per D-47
    - pptx-generator.ts imports pptxgenjs and creates slides per section per D-47
    - export/[type]/route.ts handles 'csv-summary', 'csv-detailed', 'excel', 'json', 'pptx' types
    - export/[type]/route.ts has rate limiting per D-62b
    - export/[type]/route.ts logs to audit log per D-112
    - JSON export returns formatted project report data per D-114
  </acceptance_criteria>
  <done>All export formats operational: CSV (2 modes), Excel, PowerPoint, JSON with rate limiting and audit logging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public shared link | Unauthenticated access to report data |
| Token validation | Token could be guessed or reused |
| Export endpoint | Resource-intensive operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-23 | Information Disclosure | shared/[token] | mitigate | Auto-mask financial totals per D-64f; rate limit 100 views/day per D-64e |
| T-19-24 | Spoofing | Token guessing | mitigate | crypto.randomUUID() (122 bits entropy); 30-day expiry per D-64b |
| T-19-25 | Denial of Service | Export endpoint | mitigate | Rate limit 10 req/min on export per D-62b; Upstash Redis |
| T-19-26 | Repudiation | Shared link access | mitigate | Log all access to cs_report_audit_log per D-112 |
</threat_model>

<verification>
- TypeScript compiles all new files
- Share API creates/validates/revokes tokens
- All 5 export formats generate valid output
</verification>

<success_criteria>
- D-64b: Shareable links with expiry, view tracking, revocation
- D-64f: Sensitive data auto-masked on shared views
- D-47/D-48: CSV, Excel, PowerPoint, JSON exports functional
- D-114: QuickBooks-compatible and JSON API export
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-09-SUMMARY.md`
</output>
