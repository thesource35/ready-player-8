---
phase: 19-reporting-dashboards
plan: 16
type: execute
wave: 5
depends_on: ["19-05"]
files_modified:
  - web/src/lib/reports/caching.ts
  - web/src/lib/reports/feature-flags.ts
  - web/src/lib/reports/data-retention.ts
  - web/src/lib/reports/pwa-sw.ts
  - web/public/sw-reports.js
  - web/src/app/reports/components/OfflineBanner.tsx
  - web/src/app/reports/components/DataExportBackup.tsx
autonomous: true
requirements:
  - REPORT-01
must_haves:
  truths:
    - "SWR caching works with 5-min TTL"
    - "Feature flags control gradual rollout"
    - "Data retention policy configurable"
    - "PWA Service Worker caches report assets"
    - "Offline banner shows when viewing cached data"
  artifacts:
    - path: "web/src/lib/reports/caching.ts"
      provides: "Multi-layer caching per D-59"
    - path: "web/src/lib/reports/feature-flags.ts"
      provides: "Feature flag rollout per D-95"
    - path: "web/public/sw-reports.js"
      provides: "Service Worker for offline per D-113"
  key_links:
    - from: "caching.ts"
      to: "Upstash Redis"
      via: "server-side cache layer"
      pattern: "upstash"
---

<objective>
Build caching infrastructure, feature flags, data retention, PWA offline support, and data export/backup.

Purpose: Cover performance (D-57 through D-62), migration/rollout (D-95 through D-97), and offline (D-113).
Output: Caching layer, feature flags, Service Worker, retention policy, backup export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-RESEARCH.md
@web/src/lib/rate-limit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-layer caching + feature flags + data retention</name>
  <files>web/src/lib/reports/caching.ts, web/src/lib/reports/feature-flags.ts, web/src/lib/reports/data-retention.ts</files>
  <read_first>
    - web/src/lib/rate-limit.ts (existing Upstash Redis setup)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-57, D-59, D-62, D-62b, D-95, D-96)
    - .planning/phases/19-reporting-dashboards/19-RESEARCH.md (caching architecture)
  </read_first>
  <action>
1. Create `web/src/lib/reports/caching.ts`:
   - Per D-59: multi-layer caching implementation:
     a. Client SWR: export `useReportSWR(key, fetcher)` hook wrapping useSWR with 5-min TTL (revalidateOnFocus: false, refreshInterval: 300000)
     b. Server in-memory: Map-based cache with 1-min TTL for DB view results. Export `serverCache.get(key)`, `serverCache.set(key, value, ttlMs)`
     c. Vercel Edge Cache: set Cache-Control headers with s-maxage=60, stale-while-revalidate=300. Export `edgeCacheHeaders(projectId)` returning headers object with cache tags
   - Per D-59: invalidation on data write -- export `invalidateReportCache(projectId)` that purges all 3 layers
   - Per D-57: on-demand reports computed fresh; cached for scheduled reports only
   - Per D-50w: request deduplication via SWR (client) + server-side fingerprinting (hash request params, skip if same hash within 5s)
   - Per D-62b: export `reportRateLimit(type)` using Upstash Redis. Limits: general reports 60/min, PDF generation 10/min, batch export 3/min

2. Create `web/src/lib/reports/feature-flags.ts`:
   - Per D-95: feature flag gradual rollout
   - Export `isReportingEnabled(userId)`: check against rollout percentage
   - Rollout stages: { percentage: 10 | 50 | 100, beta_opt_in: boolean }
   - Per D-95: beta opt-in toggle (store in user preferences)
   - Flag storage: environment variable REPORT_ROLLOUT_PERCENT (default 100)
   - Hash userId to determine if in rollout bucket (deterministic, consistent per user)
   - Export `getFeatureFlags()`: returns object with all feature flag states

3. Create `web/src/lib/reports/data-retention.ts`:
   - Per D-96: user-configurable data retention period
   - Options: 6 months, 1 year, 2 years, unlimited
   - Export `getRetentionPolicy(userId)`: read from user settings (default: 1 year)
   - Export `applyRetention(userId)`: delete expired records from cs_report_history, cs_report_delivery_log older than retention period
   - Export `getExpiringItems(userId)`: list items within 30 days of retention expiry
   - Per D-97: full data export function -- ZIP with all stored PDFs, schedules, templates, delivery logs
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/lib/reports/caching.ts src/lib/reports/feature-flags.ts src/lib/reports/data-retention.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - caching.ts exports useReportSWR with 5-min TTL (refreshInterval: 300000) per D-59
    - caching.ts exports serverCache with 1-min TTL per D-59
    - caching.ts exports reportRateLimit with PDF at 10/min per D-62b
    - feature-flags.ts exports isReportingEnabled with percentage-based rollout per D-95
    - feature-flags.ts reads REPORT_ROLLOUT_PERCENT env var
    - data-retention.ts exports getRetentionPolicy with 4 options per D-96
    - data-retention.ts has applyRetention for cleanup per D-96
  </acceptance_criteria>
  <done>Multi-layer caching, feature flags with gradual rollout, and data retention policy.</done>
</task>

<task type="auto">
  <name>Task 2: PWA offline + Service Worker + data backup export</name>
  <files>web/src/lib/reports/pwa-sw.ts, web/public/sw-reports.js, web/src/app/reports/components/OfflineBanner.tsx, web/src/app/reports/components/DataExportBackup.tsx</files>
  <read_first>
    - web/src/app/layout.tsx (check for existing service worker registration)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-113, D-97)
  </read_first>
  <action>
1. Create `web/public/sw-reports.js`:
   - Per D-113: Service Worker for offline report viewing
   - Cache strategies:
     a. Cache-first for static assets (report page shell, CSS, JS)
     b. Network-first for API data (report JSON responses)
     c. Stale-while-revalidate for chart images
   - Cache name: 'constructionos-reports-v1'
   - Per D-113: full offline report viewing from cache
   - Per D-113: installable as PWA (manifest.json additions)
   - Handle offline report generation from cached data

2. Create `web/src/lib/reports/pwa-sw.ts`:
   - Export `registerReportServiceWorker()`: register sw-reports.js
   - Export `cacheReportData(projectId, data)`: manually cache report API response
   - Export `isOffline()`: check navigator.onLine
   - Call from report pages to cache data for offline use

3. Create `OfflineBanner.tsx` ("use client"):
   - Per D-113: show "Offline -- showing cached data" banner when offline
   - Listen to online/offline events
   - Yellow banner at top of report view (var(--gold) background)
   - Auto-dismiss when back online with "Back online" toast

4. Create `DataExportBackup.tsx` ("use client"):
   - Per D-97: full data export for backup
   - "Export All Data" button
   - Generates ZIP with:
     a. All stored PDFs from Supabase Storage
     b. Schedule configurations as JSON
     c. Templates as JSON
     d. Delivery logs as CSV
     e. Report history snapshots as JSON
   - Download progress indicator
   - Per D-97: note about automated Supabase backups
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/lib/reports/pwa-sw.ts src/app/reports/components/OfflineBanner.tsx src/app/reports/components/DataExportBackup.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - sw-reports.js has cache-first strategy for static assets per D-113
    - sw-reports.js has network-first strategy for API data per D-113
    - pwa-sw.ts exports registerReportServiceWorker and cacheReportData
    - OfflineBanner.tsx listens for online/offline events per D-113
    - OfflineBanner.tsx shows "Offline" with var(--gold) background
    - DataExportBackup.tsx generates ZIP with PDFs, schedules, templates per D-97
  </acceptance_criteria>
  <done>PWA offline support with Service Worker, offline banner, and full data backup export.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service Worker | Caches potentially sensitive report data |
| Feature flags | Controls access to features |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-42 | Information Disclosure | Service Worker cache | accept | Browser sandboxing protects SW cache; same-origin policy applies |
| T-19-43 | Tampering | Feature flags | mitigate | Percentage calculation is deterministic; server-side validation as backup |
| T-19-44 | Denial of Service | Data backup export | mitigate | Rate limit backup export to 1/hour; limit export size |
</threat_model>

<verification>
- TypeScript compiles all files
- Service Worker file is valid JS
- Feature flags default to 100% rollout
- Caching hooks use 5-min TTL
</verification>

<success_criteria>
- D-57/59: Multi-layer caching with SWR + server + edge
- D-62b: Rate limiting on expensive operations
- D-95: Feature flag gradual rollout
- D-96: Configurable data retention
- D-97: Full data export/backup
- D-113: PWA offline support
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-16-SUMMARY.md`
</output>
