---
phase: 19-reporting-dashboards
plan: 06
type: execute
wave: 3
depends_on: ["19-01", "19-02", "19-04"]
files_modified:
  - web/src/app/reports/rollup/page.tsx
  - web/src/app/reports/components/PortfolioTable.tsx
  - web/src/app/reports/components/PortfolioCharts.tsx
  - web/src/app/reports/components/PortfolioTimeline.tsx
  - web/src/app/reports/components/ComparisonView.tsx
autonomous: true
requirements:
  - REPORT-02
  - REPORT-04
must_haves:
  truths:
    - "User can view portfolio rollup at /reports/rollup"
    - "Portfolio shows KPI cards + sortable project table + charts"
    - "Table rows link to individual project reports"
    - "Filters persist via URL params"
    - "Virtual scrolling activates at 25+ projects"
  artifacts:
    - path: "web/src/app/reports/rollup/page.tsx"
      provides: "Portfolio rollup dashboard page"
    - path: "web/src/app/reports/components/PortfolioTable.tsx"
      provides: "Sortable project table with react-window"
    - path: "web/src/app/reports/components/PortfolioCharts.tsx"
      provides: "Portfolio-level charts (grouped bar, radar, spend trend)"
  key_links:
    - from: "web/src/app/reports/rollup/page.tsx"
      to: "/api/reports/rollup"
      via: "fetch for portfolio data"
      pattern: "fetch.*api/reports/rollup"
---

<objective>
Build the portfolio rollup dashboard page with sortable table, portfolio charts, timeline, comparison view, and filter/sort functionality.

Purpose: Deliver REPORT-02 cross-project financial rollup dashboard.
Output: Rollup page with table, charts, filters, virtual scrolling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-UI-SPEC.md
@.planning/phases/19-reporting-dashboards/19-01-SUMMARY.md
@.planning/phases/19-reporting-dashboards/19-04-SUMMARY.md
@web/src/app/schedule/RollupTimeline.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portfolio rollup page with KPI cards, filters, and table</name>
  <files>web/src/app/reports/rollup/page.tsx, web/src/app/reports/components/PortfolioTable.tsx</files>
  <read_first>
    - web/src/app/schedule/RollupTimeline.tsx (existing timeline rollup pattern)
    - web/src/lib/reports/types.ts (PortfolioRollup, ProjectSummary types)
    - .planning/phases/19-reporting-dashboards/19-UI-SPEC.md (Portfolio Rollup Table spec)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-35 through D-46b)
  </read_first>
  <action>
1. Create `PortfolioTable.tsx` ("use client"):
   - Per UI-SPEC Portfolio Rollup Table:
     - Table container: background var(--surface), borderRadius 14, overflow hidden
     - Header: background var(--panel), padding 8px 16px, fontSize 8, fontWeight 800, letterSpacing 1
     - Data row: padding 12px 16px, borderBottom, cursor pointer
     - Row hover: background rgba(242,158,61,0.04)
   - Columns: Project Name, Health, Contract Value, Billed, % Complete, Open Issues, Safety, Status
   - Per D-39: sortable columns (click header to sort), sort indicator per UI-SPEC
   - Per D-40: click row navigates to /reports/project/{id}
   - Per D-42: status badges (Active/Delayed/Completed), active by default, toggle completed
   - Per D-41: health badges inline per row
   - Per D-16c: feature coverage badge "{N}/6 features active" per row
   - Per D-46: use react-window FixedSizeList when projects.length >= 25
   - Props: `{ projects: ProjectSummary[]; onSort: (key: string) => void; sortKey: string; sortDir: 'asc' | 'desc' }`

2. Create `web/src/app/reports/rollup/page.tsx` ("use client"):
   - Fetch from /api/reports/rollup with query params
   - Per D-36: layout = summary KPI cards + sortable project table + charts
   - KPI cards at top: StatCard grid showing total contract value, total billed, total remaining, change order impact, project count, avg health score per D-35
   - Per D-39: filter bar with status dropdown, date range, project type, client, budget range
   - Per D-39: filters persist via URL searchParams (useSearchParams). User-saved defaults as fallback
   - Per D-45: on-load fetch + manual refresh button + auto-refresh polling every 5 min
   - Per D-45: pull-to-refresh not applicable on web, but refresh button with spin animation
   - Per D-46b: period comparison toggle -- when enabled, show delta % changes alongside current values
   - PortfolioTable component for project list
   - SkeletonReport while loading per D-58
   - Per D-106: search across report content (client-side filter on project names)
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/reports/rollup/page.tsx src/app/reports/components/PortfolioTable.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - rollup/page.tsx fetches from /api/reports/rollup with query params
    - PortfolioTable.tsx imports from react-window for virtual scrolling per D-46
    - PortfolioTable.tsx has sortable columns with sort indicator per D-39
    - rollup/page.tsx uses useSearchParams for filter persistence per D-39
    - rollup/page.tsx has 5-minute auto-refresh polling per D-45
    - Feature coverage badge visible per row per D-16c
  </acceptance_criteria>
  <done>Portfolio rollup page with KPI cards, sortable/filterable table, virtual scrolling, URL-persisted filters.</done>
</task>

<task type="auto">
  <name>Task 2: Portfolio charts, timeline, and comparison view</name>
  <files>web/src/app/reports/components/PortfolioCharts.tsx, web/src/app/reports/components/PortfolioTimeline.tsx, web/src/app/reports/components/ComparisonView.tsx</files>
  <read_first>
    - web/src/lib/reports/chart-config.ts (shared chart config from Plan 02)
    - web/src/app/reports/components/BudgetPieChart.tsx (chart component pattern)
    - web/src/app/schedule/RollupTimeline.tsx (existing timeline pattern)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-19c, D-38, D-43, D-117)
    - .planning/phases/19-reporting-dashboards/19-UI-SPEC.md (Chart Types Recharts Configuration for portfolio)
  </read_first>
  <action>
1. Create `PortfolioCharts.tsx` ("use client"):
   - Per D-19c: grouped bar chart for financial comparison (BarChart with 2 Bars: contract value + billed)
   - Per D-19c: radar/spider chart for multi-dimensional health (custom implementation using Recharts RadarChart)
   - Per D-43: monthly spend trend line chart (LineChart with multiple Lines: per-project + aggregate bold)
   - Per D-43: toggle between aggregate line, per-project lines, and stacked area
   - All charts use shared CHART_TOOLTIP_STYLE, CHART_AXIS_STYLE, CHART_ANIMATION from chart-config.ts
   - Each chart in a chart wrapper div per UI-SPEC
   - ChartExportButton on each chart

2. Create `PortfolioTimeline.tsx` ("use client"):
   - Per D-38: horizontal timeline bars showing all projects' start/end dates (Gantt-style portfolio view)
   - Each project as a horizontal bar from start_date to end_date
   - Color-coded by health score (green/gold/red)
   - Per D-20: hover tooltip with project name, dates, duration
   - Use Recharts BarChart with horizontal layout (layout="vertical")

3. Create `ComparisonView.tsx` ("use client"):
   - Per D-117: project vs project side-by-side comparison
   - Two-column layout: select Project A and Project B from dropdowns
   - Show key metrics side by side with delta highlighting (green up, red down)
   - Per D-117: time period comparison using version history snapshots
   - Per D-117: industry benchmarking section (placeholder with static data per D-116)
   - Per D-116: show AGC/ENR benchmark data labels alongside project metrics
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/reports/components/PortfolioCharts.tsx src/app/reports/components/PortfolioTimeline.tsx src/app/reports/components/ComparisonView.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - PortfolioCharts.tsx has grouped BarChart, RadarChart, and multi-line LineChart
    - PortfolioCharts.tsx has toggle for aggregate/per-project/stacked area on spend trend per D-43
    - PortfolioTimeline.tsx renders horizontal bars per project with health coloring per D-38
    - ComparisonView.tsx has two-column side-by-side layout with project selector dropdowns per D-117
    - ComparisonView.tsx shows benchmark data labels per D-116
    - All charts use ChartExportButton per D-26c
  </acceptance_criteria>
  <done>Portfolio-level charts, timeline view, and comparison tools implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params -> filters | User-controlled filter values from URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-14 | Tampering | URL filter params | mitigate | Validate filter values against allowed enums before API call |
| T-19-15 | Information Disclosure | Comparison view | accept | User can only compare projects they have access to (API is RLS-filtered) |
</threat_model>

<verification>
- TypeScript compiles all new components
- Rollup page renders KPI cards + table + charts
- Filters persist via URL params
</verification>

<success_criteria>
- REPORT-02: Cross-project financial rollup with sortable table, filters, charts
- D-38: Portfolio timeline with horizontal project bars
- D-117: Side-by-side comparison view operational
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-06-SUMMARY.md`
</output>
