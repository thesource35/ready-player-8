---
phase: 19-reporting-dashboards
plan: 13
type: execute
wave: 5
depends_on: ["19-05", "19-07"]
files_modified:
  - web/src/lib/reports/i18n-messages/en.json
  - web/src/middleware.ts
  - web/src/i18n.ts
  - web/src/app/reports/components/ReportThemeSwitcher.tsx
  - web/src/app/reports/components/KeyboardShortcuts.tsx
  - web/src/app/reports/components/BookmarkButton.tsx
  - web/src/app/reports/components/BulkOperationsBar.tsx
  - web/src/lib/reports/report-themes.ts
autonomous: true
requirements:
  - REPORT-01
must_haves:
  truths:
    - "Report labels are translatable via next-intl"
    - "Keyboard shortcuts work for common actions"
    - "Report themes change visual appearance"
    - "Bookmarks pin favorite reports"
    - "Bulk operations bar enables multi-select actions"
  artifacts:
    - path: "web/src/lib/reports/i18n-messages/en.json"
      provides: "English translations for all report labels"
    - path: "web/src/app/reports/components/KeyboardShortcuts.tsx"
      provides: "Keyboard shortcut handler"
  key_links:
    - from: "i18n-messages/en.json"
      to: "next-intl"
      via: "message files for translations"
      pattern: "useTranslations"
---

<objective>
Add i18n, keyboard shortcuts, report theming, bookmarks/favorites, and bulk operations.

Purpose: Polish features covering D-86/87, D-108, D-109, D-111, D-110.
Output: i18n setup, keyboard handler, theme switcher, bookmark/bulk components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-RESEARCH.md
@.planning/phases/19-reporting-dashboards/19-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n setup with next-intl + report themes + keyboard shortcuts</name>
  <files>web/src/lib/reports/i18n-messages/en.json, web/src/middleware.ts, web/src/i18n.ts, web/src/app/reports/components/ReportThemeSwitcher.tsx, web/src/app/reports/components/KeyboardShortcuts.tsx, web/src/lib/reports/report-themes.ts</files>
  <read_first>
    - web/src/middleware.ts (check if exists -- may need to create or update)
    - web/src/lib/reports/constants.ts (REPORT_THEMES from Plan 01)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-86, D-87, D-108, D-109)
    - .planning/phases/19-reporting-dashboards/19-RESEARCH.md (next-intl pitfall 8)
  </read_first>
  <action>
1. Create `web/src/lib/reports/i18n-messages/en.json`:
   - Per D-86: all report labels and section titles translatable
   - Include all UI-SPEC copywriting: "Export PDF", "Export CSV", "Generate Report", "Project Report", "Portfolio Rollup", "Schedules", "No Projects Yet", "None recorded", error states, health labels, confirmation dialogs
   - Per D-87: currency and date format labels with ICU message format for locale-aware formatting
   - Structure: { "reports": { "tabs": {...}, "sections": {...}, "export": {...}, "errors": {...}, "health": {...}, "schedule": {...} } }

2. Create/update `web/src/i18n.ts`:
   - Per D-86: next-intl configuration for App Router
   - Default locale: 'en', supported locales: ['en'] (extensible)
   - getRequestConfig for server components
   - Per RESEARCH.md pitfall 8: keep config minimal, English-only to start

3. Update `web/src/middleware.ts`:
   - Add next-intl middleware for locale detection
   - Per D-86: support for future multi-language expansion
   - Minimal impact -- just locale detection, no route rewriting initially

4. Create `web/src/lib/reports/report-themes.ts`:
   - Per D-109: built-in themes as CSS variable overrides
   - Themes: Professional (default dark), Construction (high contrast amber/orange), Corporate (muted blues), Minimal (monochrome), Executive (dark navy/gold)
   - Each theme: { name, colors: { bg, surface, accent, text, muted }, fontScale?, borderRadius? }
   - Per D-109: custom CSS themes for enterprise (accept raw CSS string)
   - Per D-107: integrate with white-labeling (basic: logo + colors from branded theme)

5. Create `ReportThemeSwitcher.tsx` ("use client"):
   - Per D-109: dropdown/popover to select report theme
   - Apply theme by overriding CSS custom properties on report container
   - Per D-107: basic white-labeling (company logo + brand colors)
   - Per D-107: full white-labeling (custom domain, email domain) as enterprise feature indicator
   - Preview of theme before applying

6. Create `KeyboardShortcuts.tsx` ("use client"):
   - Per D-108: register keyboard event listeners
   - Cmd+P: trigger print (window.print with @media print styles per D-21)
   - Cmd+E: trigger export (open export dialog)
   - Cmd+S: trigger share (generate shareable link)
   - Cmd+R: refresh report data
   - Cmd+?: show keyboard shortcut help panel
   - Per D-108: vim-like shortcuts opt-in (j/k navigation, g+r go to rollup)
   - Visual help panel showing all shortcuts
   - Wrap in useEffect with addEventListener/removeEventListener cleanup
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/lib/reports/i18n-messages/en.json 2>/dev/null; npx tsc --noEmit src/app/reports/components/KeyboardShortcuts.tsx src/app/reports/components/ReportThemeSwitcher.tsx src/lib/reports/report-themes.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - en.json contains report tab labels, section titles, error messages, health labels per UI-SPEC copywriting
    - i18n.ts configures next-intl with default locale 'en'
    - report-themes.ts exports 5 themes: Professional, Construction, Corporate, Minimal, Executive per D-109
    - KeyboardShortcuts.tsx registers Cmd+P, Cmd+E, Cmd+S, Cmd+R, Cmd+? per D-108
    - ReportThemeSwitcher.tsx applies theme via CSS custom property overrides per D-109
    - en.json is valid JSON
  </acceptance_criteria>
  <done>i18n foundation, 5 report themes, keyboard shortcuts with help panel.</done>
</task>

<task type="auto">
  <name>Task 2: Bookmarks/favorites + bulk operations bar</name>
  <files>web/src/app/reports/components/BookmarkButton.tsx, web/src/app/reports/components/BulkOperationsBar.tsx</files>
  <read_first>
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-110, D-111)
    - web/src/app/reports/page.tsx (report list to integrate bookmark into)
  </read_first>
  <action>
1. Create `BookmarkButton.tsx` ("use client"):
   - Per D-111: pin favorite reports to top of list
   - Star/bookmark icon toggle (filled = bookmarked, outline = not)
   - Store bookmarks in localStorage (client-side, per-user)
   - Per D-111: custom personal dashboard built from bookmarked charts
   - Dashboard view: drag-and-drop grid arrangement of bookmarked chart widgets
   - Use simple CSS grid for dashboard layout
   - Props: { reportId: string; reportType: string }

2. Create `BulkOperationsBar.tsx` ("use client"):
   - Per D-110: bulk operations toolbar that appears when items selected
   - Checkbox selection on report list items / schedule items / shared links
   - Actions: "Delete Selected", "Export Selected", "Revoke Selected Links", "Pause/Resume Schedules"
   - Per D-110: bulk delete history, bulk revoke links, bulk schedule management, bulk re-export
   - Confirmation dialog for destructive actions
   - Fixed position bar at bottom of viewport when items selected
   - Show count of selected items
   - Inline styles per project convention
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/reports/components/BookmarkButton.tsx src/app/reports/components/BulkOperationsBar.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - BookmarkButton.tsx uses localStorage for bookmark persistence per D-111
    - BookmarkButton.tsx has star icon toggle
    - BulkOperationsBar.tsx has actions for delete, export, revoke, pause/resume per D-110
    - BulkOperationsBar.tsx shows selected item count
    - BulkOperationsBar.tsx has confirmation dialog for destructive actions
  </acceptance_criteria>
  <done>Bookmark/favorites system and bulk operations toolbar implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard events | Key handlers could conflict with browser defaults |
| Bulk operations | Multiple items deleted/modified at once |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-34 | Denial of Service | Bulk operations | mitigate | Limit bulk operations to 50 items per request; confirmation required |
| T-19-35 | Tampering | Theme CSS injection | mitigate | Sanitize custom CSS themes; only allow CSS property overrides, no script injection |
</threat_model>

<verification>
- TypeScript compiles all new files
- i18n messages file is valid JSON
- Keyboard shortcuts register and unregister cleanly
</verification>

<success_criteria>
- D-86/87: i18n foundation with translatable labels
- D-108: Keyboard shortcuts operational
- D-109: 5 report themes switchable
- D-111: Bookmark/favorites system
- D-110: Bulk operations bar
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-13-SUMMARY.md`
</output>
