---
phase: 19-reporting-dashboards
plan: 18
type: execute
wave: 6
depends_on: ["19-05", "19-06", "19-07", "19-10"]
files_modified:
  - web/src/lib/reports/__tests__/integration.test.ts
  - web/tests/reports-export.spec.ts
  - ready player 8 Tests/ReportTests.swift
  - web/src/lib/reports/__tests__/email.test.ts
  - web/src/app/reports/components/ConditionalFormatting.tsx
  - web/src/app/reports/components/PrintStyles.tsx
  - web/src/app/reports/components/WebhookConfig.tsx
autonomous: true
requirements:
  - REPORT-01
  - REPORT-02
  - REPORT-03
  - REPORT-04
must_haves:
  truths:
    - "Integration tests verify full report flow"
    - "E2E test navigates to report, verifies charts, exports PDF"
    - "iOS XCTests verify aggregation with shared fixtures"
    - "Email tests mock Resend API"
    - "Conditional formatting applies to report tables"
    - "Print CSS renders clean output"
  artifacts:
    - path: "web/src/lib/reports/__tests__/integration.test.ts"
      provides: "Integration test suite"
    - path: "web/tests/reports-export.spec.ts"
      provides: "Playwright E2E test per D-81"
    - path: "ready player 8 Tests/ReportTests.swift"
      provides: "iOS XCTests per D-85"
  key_links:
    - from: "web/tests/reports-export.spec.ts"
      to: "/reports"
      via: "Playwright navigates to report page"
      pattern: "page.goto.*reports"
---

<objective>
Complete the test suite (unit, integration, E2E, iOS), add remaining UI polish features (conditional formatting, print styles, webhook config), and verify everything end-to-end.

Purpose: Deliver D-77 through D-85 testing requirements and remaining polish features.
Output: Test files, remaining components, verification of all requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-RESEARCH.md
@web/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests + E2E Playwright test + email tests</name>
  <files>web/src/lib/reports/__tests__/integration.test.ts, web/tests/reports-export.spec.ts, web/src/lib/reports/__tests__/email.test.ts</files>
  <read_first>
    - web/vitest.config.ts (test configuration)
    - web/src/lib/reports/__tests__/aggregation.test.ts (existing test pattern from Plan 01)
    - web/src/lib/reports/__tests__/fixtures/sample-project.json (shared fixtures)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-77 through D-84)
    - .planning/phases/19-reporting-dashboards/19-RESEARCH.md (Validation Architecture section)
  </read_first>
  <action>
1. Create `web/src/lib/reports/__tests__/integration.test.ts`:
   - Per D-77: integration tests against test DB for report data accuracy
   - Test full flow: mock Supabase client -> call aggregation functions -> verify output matches expected
   - Test budget computation with real-format text fields ("$450,000", "$0", "TBD")
   - Test health score computation end-to-end (raw data -> sections -> health)
   - Test portfolio rollup with 5 projects of varying health
   - Test partial failure scenario (one section throws, others succeed)
   - Per D-80: use shared JSON fixtures from fixtures/ directory
   - Per D-83: verify coverage remains at 100% for aggregation functions

2. Create `web/tests/reports-export.spec.ts`:
   - Per D-81: E2E Playwright test for full report workflow
   - Test flow:
     a. Navigate to /reports
     b. Verify page loads with project list or demo report
     c. Click first project (or demo)
     d. Verify all chart containers render (budget, schedule, safety)
     e. Verify stat cards show numeric values
     f. Click "Export PDF" button
     g. Verify PDF download triggered (check downloads)
     h. Navigate to /reports/rollup
     i. Verify portfolio table renders
     j. Verify filter controls present
   - Per D-84: CI generates sample report + rollup PDF from fixture data (generate in test, save as artifact)
   - Per D-79: visual screenshot of chart rendering for regression

3. Create `web/src/lib/reports/__tests__/email.test.ts`:
   - Per D-82: mock Resend in unit tests
   - Mock Resend SDK: `vi.mock('resend', () => ({ Resend: vi.fn() }))`
   - Test email template renders with health score, budget %, key numbers
   - Test email subject line generation per D-50r
   - Test failure handling: Resend throws, fallback to stored report per D-50x
   - Test recipient validation (team members only per D-50e)
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx vitest run src/lib/reports/__tests__/integration.test.ts src/lib/reports/__tests__/email.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - integration.test.ts tests full aggregation flow with shared fixtures per D-77
    - integration.test.ts tests partial failure scenario per D-56
    - reports-export.spec.ts has Playwright test navigating to /reports per D-81
    - reports-export.spec.ts verifies chart containers and PDF export per D-81
    - email.test.ts mocks Resend SDK per D-82
    - email.test.ts tests failure fallback per D-50x
    - All vitest tests pass with 0 failures
  </acceptance_criteria>
  <done>Integration, E2E, and email test suites complete per D-77 through D-84.</done>
</task>

<task type="auto">
  <name>Task 2: iOS XCTests + conditional formatting + print styles + webhook config</name>
  <files>ready player 8 Tests/ReportTests.swift, web/src/app/reports/components/ConditionalFormatting.tsx, web/src/app/reports/components/PrintStyles.tsx, web/src/app/reports/components/WebhookConfig.tsx</files>
  <read_first>
    - web/src/lib/reports/__tests__/fixtures/sample-project.json (shared fixtures per D-80)
    - .planning/phases/19-reporting-dashboards/19-CONTEXT.md (D-85, D-26j, D-21, D-56h)
  </read_first>
  <action>
1. Create `ready player 8 Tests/ReportTests.swift`:
   - Per D-85: iOS XCTests for report functionality
   - Per D-80: read shared JSON test fixtures (bundle resource) for cross-platform consistency
   - Test: parseBudgetString Swift equivalent produces same results as TypeScript
   - Test: health score computation matches expected values
   - Test: chart data preparation from raw project data
   - Per D-85: swift-snapshot-testing reference (add comment noting snapshot test framework needed)
   - Per D-85: XCUITest for functional interaction (navigate to Reports tab, verify sections visible)

2. Create `ConditionalFormatting.tsx` ("use client"):
   - Per D-26j: built-in conditional formatting on report tables
   - Health-based coloring auto-applied: green background for on-track, gold for at-risk, red for critical
   - Per D-26j: user-defined custom conditional formatting rules
   - Rule builder: column selector + condition (>, <, =, contains) + value + format (background color, text color, bold)
   - Apply rules to PortfolioTable and section list rows
   - Store custom rules in localStorage (per-user)
   - Props: `{ rules: ConditionalRule[]; onRulesChange: (rules) => void }`

3. Create `PrintStyles.tsx`:
   - Per D-21: print-friendly CSS @media print rules
   - Create `<style>` tag with @media print:
     - White background
     - Hide nav, export buttons, filter bars, tooltips
     - Show all sections expanded
     - Chart images at full size
     - Table borders visible
     - Page break before each major section
   - Render as a style tag in report pages
   - Triggered by Cmd+P (browser print dialog) per D-108

4. Create `WebhookConfig.tsx` ("use client"):
   - Per D-56h: report API webhook events for Zapier/Make integration
   - Configure webhook URL and events to send
   - Events: report_generated, report_exported, report_shared, schedule_delivered
   - Test webhook button (send sample payload)
   - Per D-114: JSON API endpoints listed for external tool integration
   - Simple form: webhook URL input + event checkboxes + save button
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx tsc --noEmit src/app/reports/components/ConditionalFormatting.tsx src/app/reports/components/PrintStyles.tsx src/app/reports/components/WebhookConfig.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ReportTests.swift has XCTest functions for budget parsing and health score per D-85
    - ReportTests.swift reads shared JSON fixture per D-80
    - ConditionalFormatting.tsx has rule builder with column + condition + value + format per D-26j
    - ConditionalFormatting.tsx auto-applies health-based coloring per D-26j
    - PrintStyles.tsx has @media print with white background, hidden nav per D-21
    - WebhookConfig.tsx has URL input + event checkboxes per D-56h
    - Web components pass TypeScript checking
  </acceptance_criteria>
  <done>iOS tests, conditional formatting, print styles, and webhook config complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webhook URL | User-configured external URL receives report data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-46 | Information Disclosure | Webhook | mitigate | Only send event metadata (not full report data) to webhooks; user must explicitly configure |
| T-19-47 | Denial of Service | Webhook delivery | mitigate | Timeout webhook calls at 5s; retry once; log failures |
</threat_model>

<verification>
- `cd web && npx vitest run src/lib/reports/__tests__/ --reporter=verbose` all pass
- Xcode build includes ReportTests.swift
- Print styles produce clean @media print output
</verification>

<success_criteria>
- D-77 through D-85: Complete test coverage (unit, integration, E2E, iOS)
- D-26j: Conditional formatting on tables
- D-21: Print-friendly CSS
- D-56h: Webhook configuration for integrations
- All 4 REPORT requirements verified end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-18-SUMMARY.md`
</output>
