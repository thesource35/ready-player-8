---
phase: 19-reporting-dashboards
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/package-lock.json
  - web/src/lib/reports/types.ts
  - web/src/lib/reports/constants.ts
  - web/src/lib/reports/aggregation.ts
  - web/src/lib/reports/__tests__/aggregation.test.ts
  - web/src/lib/reports/__tests__/fixtures/sample-project.json
  - web/src/lib/reports/__tests__/fixtures/sample-portfolio.json
autonomous: true
requirements:
  - REPORT-01
  - REPORT-02
  - REPORT-04
must_haves:
  truths:
    - "All report type definitions exist and are importable"
    - "Budget string parser correctly handles $, commas, N/A, TBD, empty"
    - "Health score computation returns green/yellow/red based on budget+schedule+issues"
    - "Single-project aggregation produces all 4 sections from raw data"
    - "Portfolio rollup aggregation computes totals across multiple projects"
    - "All aggregation functions have 100% line coverage"
  artifacts:
    - path: "web/src/lib/reports/types.ts"
      provides: "Report type definitions"
      exports: ["ProjectReport", "PortfolioRollup", "ReportSection", "HealthScore", "BudgetSection", "ScheduleSection", "IssuesSection", "TeamSection", "SafetySection"]
    - path: "web/src/lib/reports/constants.ts"
      provides: "Health thresholds, chart colors, section config"
      exports: ["HEALTH_THRESHOLDS", "CHART_COLORS", "SECTION_LABELS"]
    - path: "web/src/lib/reports/aggregation.ts"
      provides: "Pure computation functions"
      exports: ["computeHealthScore", "computeBudgetSection", "computeScheduleSection", "computeIssuesSection", "computeTeamSection", "computeSafetySection", "computePortfolioRollup", "parseBudgetString"]
    - path: "web/src/lib/reports/__tests__/aggregation.test.ts"
      provides: "100% coverage unit tests"
    - path: "web/src/lib/reports/__tests__/fixtures/sample-project.json"
      provides: "Shared test fixture for cross-platform consistency (D-80)"
  key_links:
    - from: "web/src/lib/reports/aggregation.ts"
      to: "web/src/lib/reports/types.ts"
      via: "imports type definitions"
      pattern: "import.*from.*types"
---

<objective>
Install npm dependencies, define all report types, and implement TDD aggregation functions with 100% line coverage.

Purpose: Establish the data foundation that all subsequent plans depend on -- types, constants, and pure computation logic.
Output: Tested aggregation library, shared fixtures, report type system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-reporting-dashboards/19-CONTEXT.md
@.planning/phases/19-reporting-dashboards/19-RESEARCH.md
@.planning/phases/19-reporting-dashboards/19-UI-SPEC.md
@web/src/lib/supabase/fetch.ts
@web/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install npm dependencies and define report types + constants</name>
  <files>web/package.json, web/src/lib/reports/types.ts, web/src/lib/reports/constants.ts</files>
  <read_first>
    - web/package.json (current dependencies)
    - web/src/lib/supabase/fetch.ts (existing fetch patterns)
    - ready player 8/SupabaseService.swift (DTO field names for cross-platform alignment)
  </read_first>
  <action>
1. Run `cd web && npm install recharts jspdf html2canvas resend xlsx pptxgenjs next-intl fabric posthog-js posthog-node react-window @react-email/components` per RESEARCH.md.

2. Create `web/src/lib/reports/types.ts` with all report data types per D-01 through D-16e:
   - `HealthScore`: { score: number; color: 'green' | 'gold' | 'red'; label: 'On Track' | 'At Risk' | 'Critical' }
   - `BudgetSection`: { contractValue: number; totalBilled: number; percentComplete: number; changeOrderNet: number; retainage: number; spent: number; remaining: number } per D-13
   - `ScheduleSection`: { milestones: Array<{ name: string; percentComplete: number; status: string }>; delayedCount: number; totalCount: number } per D-12
   - `IssuesSection`: { rfis: Array<{ id: string; subject: string; status: string; created_at: string }>; changeOrders: Array<{ id: string; description: string; amount: number; status: string }>; criticalOpen: number; totalOpen: number } per D-11
   - `TeamSection`: { memberCount: number; recentActivity: Array<{ user: string; action: string; timestamp: string }>; roleBreakdown: Record<string, number> } per D-14
   - `SafetySection`: { totalIncidents: number; severityBreakdown: { minor: number; moderate: number; serious: number }; daysSinceLastIncident: number; monthlyData: Array<{ month: string; count: number }>; incidents: Array<{ id: string; description: string; severity: string; date: string }> } per D-16
   - `AIInsightsSection`: { summary: string; recommendations: Array<{ section: string; text: string; actionable: boolean }> } per D-16d, D-16e
   - `ReportSection`: { data: unknown; freshness: string; error?: { code: string; message: string; retryable: boolean } } per D-16b, D-56f
   - `ProjectReport`: { project_id: string; project_name: string; client_name: string; generated_at: string; health: HealthScore; budget: BudgetSection | null; schedule: ScheduleSection | null; issues: IssuesSection | null; team: TeamSection | null; safety: SafetySection | null; ai_insights: AIInsightsSection | null; documents: { count: number }; photos: { count: number }; errors: Array<{ section: string; error: string }> } per D-01, D-02, D-08, D-15
   - `PortfolioRollup`: { generated_at: string; health: HealthScore; projects: Array<ProjectSummary>; totals: { contractValue: number; totalBilled: number; changeOrderNet: number }; monthlySpend: Array<{ month: string; amount: number }> } per D-35 through D-46b
   - `ProjectSummary`: { id: string; name: string; status: string; health: HealthScore; contractValue: number; billed: number; percentComplete: number; scheduleHealth: string; openIssues: number; safetyIncidents: number; featureCoverage: { active: number; total: number } } per D-16c, D-37, D-41
   - `ReportSchedule`: { id: string; frequency: 'daily' | 'weekly' | 'biweekly' | 'monthly'; day_of_week?: number; day_of_month?: number; time_utc: string; timezone: string; recipients: string[]; sections: string[]; is_active: boolean; last_run_at: string | null; next_run_at: string | null } per D-49, D-50b
   - `SharedLink`: { id: string; token: string; project_id: string | null; report_type: 'project' | 'rollup'; expires_at: string; view_count: number; is_revoked: boolean } per D-64b
   - `DeliveryLog`: { id: string; schedule_id: string; recipients: string[]; status: 'sent' | 'failed' | 'partial'; error_message?: string; pdf_storage_path?: string; created_at: string } per D-50h
   - `ReportTemplate`: { id: string; name: string; description: string; template_config: { sections: string[]; ordering: string[]; visibility: Record<string, boolean>; customCSS?: string } } per D-93, D-94
   - `ComparePeriod`: { current: PortfolioRollup; previous: PortfolioRollup; deltas: Record<string, number> } per D-46b
   - Export all types.

3. Create `web/src/lib/reports/constants.ts`:
   - `HEALTH_THRESHOLDS`: { green: { min: 80 }, gold: { min: 60 }, red: { min: 0 } } per UI-SPEC health score color mapping
   - `CHART_COLORS`: { primary: '#F29E3D', secondary: '#4AC4CC', positive: '#69D294', tertiary: '#8A8FCC', quaternary: '#FCC757', alert: '#D94D48' } per UI-SPEC chart color palette
   - `SECTION_LABELS`: Record mapping section keys to display labels
   - `REPORT_THEMES`: { professional: {...}, construction: {...}, corporate: {...}, minimal: {...}, executive: {...} } per D-109 as static theme objects
   - `PDF_SETTINGS`: { margins: 0.75, letterWidth/a4Width dimensions, fontSizes } per UI-SPEC PDF layout
   - `FEATURE_TOTAL`: 6 per D-16c (projects, contracts, tasks, team, field, documents)
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && node -e "require('./src/lib/reports/types.ts')" 2>&1 || npx tsc --noEmit src/lib/reports/types.ts src/lib/reports/constants.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - web/package.json contains "recharts", "jspdf", "html2canvas", "resend", "xlsx", "pptxgenjs", "next-intl", "fabric", "posthog-js", "posthog-node", "react-window", "@react-email/components" as dependencies
    - web/src/lib/reports/types.ts exports ProjectReport, PortfolioRollup, HealthScore, BudgetSection, ScheduleSection, IssuesSection, TeamSection, SafetySection
    - web/src/lib/reports/constants.ts exports HEALTH_THRESHOLDS, CHART_COLORS, SECTION_LABELS, REPORT_THEMES
    - `npx tsc --noEmit` on both files produces zero errors
  </acceptance_criteria>
  <done>All report types defined, constants exported, npm deps installed with no TypeScript errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD aggregation functions with shared fixtures and 100% line coverage</name>
  <files>web/src/lib/reports/aggregation.ts, web/src/lib/reports/__tests__/aggregation.test.ts, web/src/lib/reports/__tests__/fixtures/sample-project.json, web/src/lib/reports/__tests__/fixtures/sample-portfolio.json</files>
  <read_first>
    - web/src/lib/reports/types.ts (just created in Task 1)
    - web/src/lib/reports/constants.ts (just created in Task 1)
    - web/vitest.config.ts (test runner config)
    - ready player 8/SupabaseService.swift (field names for cs_projects, cs_contracts budget column -- TEXT type)
  </read_first>
  <behavior>
    - parseBudgetString("$450,000") returns 450000
    - parseBudgetString("N/A") returns 0
    - parseBudgetString("TBD") returns 0
    - parseBudgetString("") returns 0
    - parseBudgetString("---") returns 0
    - parseBudgetString("1234.56") returns 1234.56
    - computeHealthScore with budget 50% spent, 0 delays, 0 critical issues returns { score >= 80, color: 'green', label: 'On Track' }
    - computeHealthScore with budget 95% spent, 40% delayed milestones, 5 critical issues returns { score < 60, color: 'red', label: 'Critical' }
    - computeHealthScore with null sections returns { score: 100, color: 'green' }
    - computeBudgetSection with project+contracts data returns correct contractValue, totalBilled, percentComplete, changeOrderNet, retainage
    - computeScheduleSection returns milestones with percentComplete and delayed count
    - computeIssuesSection returns RFIs and change orders with open counts
    - computeTeamSection returns member count and recent activity
    - computeSafetySection returns severity breakdown, days since last, monthly data per D-16
    - computePortfolioRollup returns totals across multiple projects with per-project summaries
    - clampBudgetPercent(150) returns 100 per D-56e
    - clampCount(-5) returns 0 per D-56e
  </behavior>
  <action>
RED phase:
1. Create `web/src/lib/reports/__tests__/fixtures/sample-project.json` with mock data matching Supabase table schemas: project record with budget as TEXT "$450,000", contracts array, tasks array, team_assignments, safety_incidents, rfis, change_orders, activity_feed entries, document/photo counts.

2. Create `web/src/lib/reports/__tests__/fixtures/sample-portfolio.json` with 3 project records of varying health (one green, one yellow, one red) for rollup testing.

3. Create `web/src/lib/reports/__tests__/aggregation.test.ts` with test cases for every function listed in behavior block above. Include edge cases: empty arrays, null values, text budget fields, zero-data projects. Per D-83, aim for 100% line coverage on aggregation.ts.

GREEN phase:
4. Create `web/src/lib/reports/aggregation.ts` implementing all pure functions:
   - `parseBudgetString(budget: string): number` -- strip $, commas, spaces; handle N/A/TBD/---/empty; parseFloat with NaN guard
   - `clampBudgetPercent(pct: number): number` -- Math.min(100, Math.max(0, pct)) per D-56e
   - `clampCount(n: number): number` -- Math.max(0, n) per D-56e
   - `computeHealthScore(sections)` -- per RESEARCH.md example: budget 40% weight, schedule 35%, issues 25%. Thresholds from constants.ts
   - `computeBudgetSection(project, contracts)` -- aggregate from cs_projects.budget (TEXT!), cs_contracts rows
   - `computeScheduleSection(tasks)` -- from cs_project_tasks, compute milestone % complete, delayed count
   - `computeIssuesSection(rfis, changeOrders)` -- count open/critical
   - `computeTeamSection(assignments, activityFeed)` -- count members, slice last 5 activity entries per D-14
   - `computeSafetySection(incidents)` -- severity breakdown (minor/moderate/serious), daysSinceLastIncident, monthly grouping per D-16, D-25
   - `computePortfolioRollup(projects[])` -- aggregate totals, per-project summaries with featureCoverage per D-16c
   - `computeFeatureCoverage(projectId, tableCounts)` -- returns { active: N, total: 6 } per D-16c

All functions must be pure (no side effects, no Supabase calls). They accept raw data arrays as arguments.

REFACTOR: Ensure all exports typed with types.ts interfaces.
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npx vitest run src/lib/reports/__tests__/aggregation.test.ts --reporter=verbose --coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/reports/__tests__/aggregation.test.ts` passes with 0 failures
    - Coverage output shows >= 95% line coverage on aggregation.ts (target 100% per D-83)
    - sample-project.json contains budget field as string "$450,000" matching actual Supabase TEXT column
    - parseBudgetString handles all 6 edge cases listed in behavior
    - computeHealthScore returns correct color for green/yellow/red scenarios
    - aggregation.ts imports from types.ts (grep confirms `import.*from.*types`)
  </acceptance_criteria>
  <done>All aggregation functions pass tests with 100% line coverage. Shared fixtures created for cross-platform use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw data -> aggregation | Budget strings from Supabase TEXT columns could contain arbitrary text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | parseBudgetString | mitigate | Strip all non-numeric characters except decimal point; return 0 for unparseable values |
| T-19-02 | Denial of Service | computePortfolioRollup | accept | Pure function with bounded input (max 200 projects per D-46); no network calls |
</threat_model>

<verification>
- `cd web && npx vitest run src/lib/reports/ --coverage` passes with 100% line coverage on aggregation.ts
- `npx tsc --noEmit` produces zero errors on all new files
- All 12 npm packages appear in package.json dependencies
</verification>

<success_criteria>
- Report type system covers all 4 REPORT requirements
- Aggregation functions are pure and tested with shared fixtures
- Budget string parser handles TEXT column edge cases
- Health score produces correct green/gold/red classifications
</success_criteria>

<output>
After completion, create `.planning/phases/19-reporting-dashboards/19-01-SUMMARY.md`
</output>
