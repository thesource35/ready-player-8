---
phase: 11-ios-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ".github/workflows/ci.yml"
autonomous: true
requirements: [INFRA-02, INFRA-03]
must_haves:
  truths:
    - "CI web-build job runs vitest after build step"
    - "CI web-build job runs npm audit with moderate severity threshold"
    - "CI pipeline does not break existing lint/typecheck/build steps"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline with test and audit steps"
      contains: "npm run test"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "web/package.json"
      via: "npm run test script"
      pattern: "npm run test"
---

<objective>
Add `npm run test` (vitest) and `npm audit` steps to the CI web-build job so regressions and vulnerable dependencies are caught automatically on every push and PR.

Purpose: CI currently only lints, typechecks, and builds the web app -- it does not run the existing vitest suite or check for security advisories in dependencies.
Output: Updated ci.yml with two new steps in the web-build job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.github/workflows/ci.yml
@web/package.json

<interfaces>
<!-- Current ci.yml web-build job structure -->
From .github/workflows/ci.yml (web-build job):
```yaml
web-build:
  runs-on: ubuntu-latest
  timeout-minutes: 10
  defaults:
    run:
      working-directory: web
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: "20"
        cache: "npm"
        cache-dependency-path: web/package-lock.json
    - run: npm ci
    - run: npm run lint
    - run: npm run typecheck
    - run: npm run build
    # INFRA-02: npm run test goes here
    # INFRA-03: npm audit goes here
```

From web/package.json scripts:
```json
{
  "scripts": {
    "test": "vitest run",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "build": "next build --webpack"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vitest and npm audit steps to CI web-build job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full file -- understand current job structure)
    - web/package.json (verify "test" script exists and runs vitest)
  </read_first>
  <action>
    Edit `.github/workflows/ci.yml` to add two new steps to the `web-build` job, placed AFTER the existing `npm run build` step:

    1. Add `- run: npm run test` step (INFRA-02). This runs vitest in non-watch mode (the "test" script in package.json is "vitest run"). Place it after build because build validates compilation -- tests should run on valid builds.

    2. Add `- run: npm audit --audit-level=moderate` step (INFRA-03). Use `--audit-level=moderate` to avoid failing on low-severity advisories in transitive dependencies (per RESEARCH.md pitfall 3). Place it after tests as a separate quality gate.

    Do NOT change any other part of the ci.yml file. Do not modify the build-and-test job (iOS), the link-health job, or the existing web-build steps.

    The final web-build steps section should be:
    ```yaml
    - run: npm ci
    - run: npm run lint
    - run: npm run typecheck
    - run: npm run build
    - run: npm run test
    - run: npm audit --audit-level=moderate
    ```
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8" && grep -A 2 "npm run test" .github/workflows/ci.yml && grep "npm audit" .github/workflows/ci.yml && cd web && npm run test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "npm run test" .github/workflows/ci.yml returns 1
    - grep -c "npm audit --audit-level=moderate" .github/workflows/ci.yml returns 1
    - grep -c "npm run lint" .github/workflows/ci.yml returns 1 (existing step preserved)
    - grep -c "npm run build" .github/workflows/ci.yml returns 1 (existing step preserved)
    - The web-build job has exactly 6 run steps (npm ci, lint, typecheck, build, test, audit)
  </acceptance_criteria>
  <done>
    CI web-build job includes `npm run test` step that runs vitest suite. CI web-build job includes `npm audit --audit-level=moderate` step. All existing CI steps (lint, typecheck, build) unchanged. Local `npm run test` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline -> npm registry | npm audit queries registry for advisory data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Denial of Service | npm audit blocking CI | mitigate | Use --audit-level=moderate to avoid low-severity false positives blocking all PRs |
</threat_model>

<verification>
1. Verify ci.yml syntax: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` or visual inspection
2. Verify vitest runs locally: `cd web && npm run test`
3. Verify npm audit runs locally: `cd web && npm audit --audit-level=moderate`
4. Verify existing CI steps unchanged (lint, typecheck, build still present in correct order)
</verification>

<success_criteria>
- ci.yml web-build job has `npm run test` step after build
- ci.yml web-build job has `npm audit --audit-level=moderate` step after test
- All existing CI steps preserved and unchanged
- Local verification of both commands succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/11-ios-tests/11-02-SUMMARY.md`
</output>
