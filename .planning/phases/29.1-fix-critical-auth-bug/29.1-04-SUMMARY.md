---
phase: 29.1-fix-critical-auth-bug
plan: 04
subsystem: auth
tags: [supabase, swiftui, auth-gate, signup, ios]

requires:
  - phase: 29.1-01
    provides: "AuthGateTests scaffold (5 @Test stubs A-E)"
  - phase: 29.1-02
    provides: "SupabaseService.signOutEverywhere() composite helper"
  - phase: 29.1-03
    provides: "UserProfileStore.login shim removed"
provides:
  - "iOS app-entry auth gate predicate bound to supabase.isAuthenticated (AUTH-GATE-01 structural half)"
  - "Signup handler inverted — supabase.signUp() awaited before profileStore.createAccount() (AUTH-GATE-03)"
  - "AuthGateTests testSignupRollbackOnServerFailure body completed against SupabaseError.notConfigured path"
affects: [phase-30-notifications, phase-29.1-05-verification, future-signup-ui-work]

tech-stack:
  added: []
  patterns:
    - "Server-first signup ordering: await remote mutation, then commit local store"
    - "Gate predicate uses server truth, not client-persisted profile"

key-files:
  created: []
  modified:
    - ready player 8/ContentView.swift
    - ready player 8Tests/AuthGateTests.swift

key-decisions:
  - "Preserve DEBUG 'Continue to demo' button semantics — it already sets supabase.accessToken first, which flips isAuthenticated before seeding demo currentUser"
  - "Read-only duplicate-email pre-check kept in signup flow to avoid wasting a Supabase signUp call"
  - "Use existing withClearedAuthState test helper rather than introducing mocks for Wave 0 test body"

patterns-established:
  - "AUTH-GATE-01: server isAuthenticated is the sole discriminator for gating the app shell"
  - "AUTH-GATE-03: any future local-state commit that depends on a remote mutation must await the remote call first"

requirements-completed:
  - AUTH-GATE-01
  - AUTH-GATE-03

duration: ~20min
completed: 2026-04-21
---

# Phase 29.1 Plan 04: Auth Gate Rebind + Signup Inversion

**Gate predicate now reads `!supabase.isAuthenticated`; signup handler awaits server success before committing local profile store.**

## Performance

- **Duration:** ~20 min (including 401 API interruption on bookkeeping step, recovered inline by orchestrator)
- **Started:** 2026-04-21T04:12:00Z
- **Completed:** 2026-04-21T04:32:43Z (final test commit); bookkeeping recovery 2026-04-21T06:05:00Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- `ContentView.swift` app-entry gate predicate rebound to `!supabase.isAuthenticated` (structural half of AUTH-GATE-01)
- Signup handler flow inverted — `supabase.signUp()` awaited BEFORE `profileStore.createAccount()` writes local state (AUTH-GATE-03)
- `AuthGateTests.testSignupRollbackOnServerFailure` body replaces Wave 0 placeholder with a real assertion that exercises `SupabaseError.notConfigured` and asserts both stores remain clean
- `xcodebuild build -scheme "ready player 8" -destination "platform=iOS Simulator,name=iPhone 17"` returns `** BUILD SUCCEEDED **`

## Task Commits

1. **Task 1: Rebind gate predicate** — `5177361` (fix)
2. **Task 2: Invert signup ordering** — `7503bbd` (fix)
3. **Task 3: Complete testSignupRollbackOnServerFailure body** — `9c8679b` (test)

**Plan metadata:** (this commit)

## Files Created/Modified

- `ready player 8/ContentView.swift` — 2 targeted edits: gate predicate (+5/-2) and signup handler (+21/-9)
- `ready player 8Tests/AuthGateTests.swift` — testSignupRollbackOnServerFailure body (+40/-4)

## Decisions Made

- Kept DEBUG demo button byte-identical — it already sets `supabase.accessToken` first, making the new gate predicate transparent to the demo path
- Preserved existing `restoreSession()` call at ~L650 onAppear — logged-in users keep access across app launches by refreshing Keychain tokens (RESEARCH §A5 blast-radius mitigation)
- UI-SPEC §Copywriting Contract respected: removed the "Account created locally but server signup failed: …" fallback string (it implied a broken invariant that is now prevented structurally); introduced one new string "Couldn't create your account: $err. Try again."

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Scope Boundary] Pre-existing ready_player_8Tests.swift async errors**
- **Found during:** Task 3 verification (compile check of test target)
- **Issue:** 45+ `'async' call in a function that does not support concurrency` errors in `ready_player_8Tests.swift` (first seen Phase 22) block end-to-end `xcodebuild test` — but `AuthGateTests.swift` itself compiles clean
- **Fix:** Tolerated per `deferred-items.md`. Mirrors the precedent set by Plans 01/02/03. Phase 30 NOTIF cluster owns remediation.
- **Files modified:** none (scope boundary)
- **Verification:** `AuthGateTests.swift` absent from failed-commands list; `xcodebuild build` succeeds
- **Committed in:** (no code change needed)

**2. [Environment Substitution] iPhone 16 → iPhone 17 simulator**
- **Found during:** Task 1 verification
- **Issue:** Plan spec references iPhone 16 sim; only iPhone 17 sim installed locally
- **Fix:** Same iOS 26.3.1 SDK, semantically equivalent verification
- **Files modified:** none
- **Verification:** BUILD SUCCEEDED on substituted simulator
- **Committed in:** (N/A)

---

**Total deviations:** 2 tolerated (1 scope-boundary, 1 environment substitution). 0 scope creep.
**Impact on plan:** Plan executed as written for all in-scope behavior.

## Issues Encountered

- Transient 401 authentication error hit the executor agent on the final SUMMARY.md/STATE/ROADMAP bookkeeping commit. All 3 code commits had already landed. Orchestrator recovered inline: re-ran `xcodebuild build` (succeeded), then wrote this SUMMARY and advanced STATE/ROADMAP. No code was lost or duplicated.

## User Setup Required

None — no external service configuration needed.

## Next Phase Readiness

- Plan 29.1-05 (verification + human UAT) is the only plan remaining. It is a checkpoint plan (`autonomous: false`) and will pause for you to walk through the three manual UAT scenarios from `29.1-VALIDATION.md §Manual-Only Verifications` on the iOS Simulator
- AUTH-GATE-01 (both halves) and AUTH-GATE-03 are closed in code. AUTH-GATE-02 was closed by Plan 02. Plan 05's role is to register these IDs in `.planning/REQUIREMENTS.md` and document evidence in `29.1-VERIFICATION.md`
- `ready_player_8Tests.swift` async errors remain deferred; they do not block phase 29.1 verification because the in-scope test file (`AuthGateTests.swift`) compiles and the criteria can be verified via build + manual UAT

---
*Phase: 29.1-fix-critical-auth-bug*
*Completed: 2026-04-21*
