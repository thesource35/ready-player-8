---
phase: 29.1-fix-critical-auth-bug
plan: 03
subsystem: auth
tags: [ios, swift, auth, userdefaults, supabase, defense-in-depth]

# Dependency graph
requires:
  - phase: 29.1-fix-critical-auth-bug
    provides: Plan 01 AuthGateTests scaffold with testProfileStoreLocalLoginRemoved stub (Criterion E)
provides:
  - "UserProfileStore.login(email:password:) REMOVED from ready player 8/UserProfileNetwork.swift"
  - "AUTH-GATE-01 sentinel comment anchoring the removal to RESEARCH.md for future maintainers"
affects:
  - 29.1-04-PLAN (Wave 2 — gate-predicate rebinding can now assume no local-only login path exists)
  - 29.1-05-PLAN (Wave 2 — phase verification sweep)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Sentinel-comment pattern: when deleting security-relevant code, leave a grep-discoverable tag (AUTH-GATE-01) + RESEARCH.md reference so code search surfaces the rationale and prevents accidental re-introduction"

key-files:
  created: []
  modified:
    - "ready player 8/UserProfileNetwork.swift"

key-decisions:
  - "Single-task plan executed as specified — zero Rule 1/2/3 deviations needed; RESEARCH §Assumption A2 (grep-verified zero callers) held on re-check at execution time"
  - "Sentinel comment placed exactly where the deleted method lived (between createAccount and updateProfile) with one blank line above + below — preserves Swift source-formatting norms, keeps diff trivially reviewable"
  - "iPhone 17 simulator substituted for plan-specified iPhone 16 (iPhone 16 not installed locally; iPhone 17 available and satisfies the same iOS 26.3.1 SDK verification — mirrors Plan 02 substitution documented in 29.1-02-SUMMARY.md decision 3)"
  - "Compile-only verification adopted for test target (pre-existing ready_player_8Tests.swift async errors still block end-to-end xcodebuild test; explicitly out of scope per phase deferred-items.md and Plans 01/02 precedent)"

patterns-established:
  - "AUTH-GATE-NN sentinel-comment tagging convention: when a security-relevant surface is deliberately removed, leave a short comment block tagged `AUTH-GATE-NN (Phase X.Y):` + one-sentence rationale + RESEARCH.md pointer. Makes `grep AUTH-GATE` a reviewable security-audit surface."

requirements-completed: [AUTH-GATE-01]

# Metrics
duration: 3min
completed: 2026-04-21
---

# Phase 29.1 Plan 03: Remove UserProfileStore.login Password-Free Shim Summary

**Surgically deleted the dead `UserProfileStore.login(email:password:)` method at UserProfileNetwork.swift:80-88 (which ignored its password argument entirely and let any caller hijack a local profile with only an email) and replaced it with a 4-line AUTH-GATE-01 sentinel comment pointing at RESEARCH §Candidate 1 — closes AUTH-GATE-01 defense-in-depth half, Plan 02 already closed the server-side signOut composite half.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-21T07:35:47Z
- **Completed:** 2026-04-21T07:39:11Z
- **Tasks:** 1/1
- **Files modified:** 1
- **Files created:** 0

## Accomplishments

- `ready player 8/UserProfileNetwork.swift` — deleted the 9-line `func login(email: String, password: String) -> Bool` method (lines 80-88 pre-edit) and replaced it with a 4-line sentinel comment tagged `AUTH-GATE-01 (Phase 29.1)` pointing at `.planning/phases/29.1-fix-critical-auth-bug/29.1-RESEARCH.md §Candidate 1`. Net line delta: -5 (9 deleted, 4 inserted).
- Re-verified zero callers at execution time via `grep -rn "profileStore\.login\|UserProfileStore\.shared\.login\|\.login(email:" "ready player 8"` — no matches in production source. The two matches that exist in the codebase (`ready player 8Tests/AuthGateTests.swift:121,132`) are a MARK comment and a doc-comment inside Criterion E's `@Test` stub, neither is an actual call site.
- `createAccount(profile:)`, `updateProfile(_:)`, `logout()`, and all neighboring methods (properties, init, `sendConnectionRequest`, `acceptConnection`, `removeConnection`, `isConnected`, `isPending`, `updateSuggestions`) preserved byte-identically — grep count for each is exactly 1.
- iOS app target builds clean on iPhone 17 simulator (`** BUILD SUCCEEDED **`).
- Test target compile (`build-for-testing`) confirms `AuthGateTests.swift` itself has zero errors — the only file in the failed-commands list is pre-existing `ready_player_8Tests.swift`, identical blocker to Plans 01 and 02 (documented in phase `deferred-items.md`).

## Task Commits

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Delete UserProfileStore.login(email:password:) and insert sentinel comment | `fe778f6` | ready player 8/UserProfileNetwork.swift |

## Files Created/Modified

### ready player 8/UserProfileNetwork.swift

**Line range pre-edit:** lines 80-88 inclusive (9 lines, the full body of `func login(email:password:) -> Bool` including opening signature line, 7 lines of body, and closing brace).

**Deleted block (pre-edit):**
```swift
    func login(email: String, password: String) -> Bool {
        if let user = allUsers.first(where: { $0.email.lowercased() == email.lowercased() }) {
            currentUser = user
            saveJSON(profileKey, value: user)
            updateSuggestions(for: user)
            return true
        }
        return false
    }
```

**Inserted block (post-edit, lines 80-83):**
```swift
    // AUTH-GATE-01 (Phase 29.1): removed password-free local login shim.
    // Authentication MUST go through SupabaseService.signIn (accessToken is the
    // source of truth for `isAuthenticated`). Do NOT re-introduce a local-only
    // login method — see .planning/phases/29.1-fix-critical-auth-bug/29.1-RESEARCH.md §Candidate 1.
```

**Neighboring context preserved:**
- Line 78: closing `}` of `createAccount(profile:) -> Bool` body
- Line 79: blank line separator (preserved)
- Lines 80-83: new sentinel comment block
- Line 84: blank line separator (preserved)
- Line 85: `func updateProfile(_ profile: UserProfile) {` — opening of next method, unchanged

**Line-count delta:** -5 net (file was 335 lines pre-edit, 330 lines post-edit). The 9-line method body removed minus the 4-line sentinel block inserted.

## Decisions Made

1. **Single-task plan executed exactly as specified.** No Rule 1/2/3 deviations surfaced — RESEARCH §Assumption A2's grep-verified zero-caller claim held on the 2026-04-21 re-check at execution time (same grep invocation used pre-edit and in the post-edit regression guard).
2. **Sentinel placed in the deleted method's exact slot.** Preserves vertical file structure, makes `git blame` point at the right place, keeps the diff trivially reviewable (one hunk, 9 lines out / 4 lines in).
3. **iPhone 17 simulator substituted for plan-specified iPhone 16.** iPhone 16 is not installed locally (`xcrun simctl list devices available | grep iPhone 16` matches only iPhone 16e). iPhone 17 compiles against the same iOS 26.3.1 SDK and satisfies the plan's semantic verification criterion (iOS Simulator app build). Mirrors Plan 02's decision 3 — documented there in detail; re-applied here without re-litigation.
4. **Compile-only verification for test target.** Pre-existing async/concurrency errors in `ready player 8Tests/ready_player_8Tests.swift` (tracked in `.planning/phases/29.1-fix-critical-auth-bug/deferred-items.md` and originally logged in Phase 22 scope) block every `xcodebuild test` invocation regardless of `-only-testing:` filter. Plans 01 and 02 both adopted compile-only verification in identical circumstances; Plan 03 follows the same precedent. `build-for-testing` output confirms `AuthGateTests.swift` itself compiles clean and is not in the failed-commands list.

## Deviations from Plan

### Auto-fixed Issues

None required — no Rule 1/2/3 issues surfaced during execution. The deletion + sentinel-insertion landed exactly as specified by the plan's `<action>` block on the first attempt.

### Environment Substitutions

**1. [Environment] iPhone 17 simulator used instead of iPhone 16**
- **Reason:** Plan specified `platform=iOS Simulator,name=iPhone 16`, but that simulator variant is not installed locally. Available devices per `xcrun simctl list devices available`: iPhone 16e, iPhone 17, iPhone 17 Pro, iPhone 17 Pro Max, iPhone Air.
- **Impact:** Zero — both simulators target the same iOS SDK. Build output is `** BUILD SUCCEEDED **` on iPhone 17 just as it would be on iPhone 16.
- **Precedent:** Plan 02 made and documented the same substitution (29.1-02-SUMMARY.md decision 3).
- **Committed in:** fe778f6

### Scope-Boundary Tolerances

**2. [Rule 3 Scope Boundary] Pre-existing ready_player_8Tests.swift async errors not fixed**
- **Found during:** Post-edit `xcodebuild build-for-testing` verification (attempting to validate that AuthGateTests.swift compiles clean with the deletion applied).
- **Issue:** The test target still contains ~15 `'async' call in a function that does not support concurrency` errors in `ready player 8Tests/ready_player_8Tests.swift` (file last touched in commit `9805e17` "Phases 13–15", predates Phase 29.1 by months). These errors cascade to any `xcodebuild test` invocation regardless of `-only-testing:` scope.
- **Fix:** Did NOT auto-fix — explicit out-of-scope per `.planning/phases/29.1-fix-critical-auth-bug/deferred-items.md`, and GSD scope-boundary rule (only fix issues directly caused by the current task). These errors are owned by the Phase 30 NOTIF remediation cluster per Plan 01's deferred-items entry.
- **Verification workaround:** `xcodebuild build-for-testing 2>&1 | grep error: | awk -F':[0-9]+:' '{print $1}' | awk -F'/' '{print $NF}' | sort -u` returned exactly one file: `ready_player_8Tests.swift`. `AuthGateTests.swift` is NOT in the failed-commands list. All 7 structural grep acceptance criteria pass (enumerated in Verification Evidence below).
- **Files modified:** None (scope-boundary preservation).

---

**Total deviations:** 1 environment substitution (iPhone 16 → iPhone 17, mirrors Plan 02) + 1 scope-boundary tolerance (pre-existing test-target errors, mirrors Plans 01 and 02). Zero new bugs, zero new stubs, zero new missing critical functionality, zero architectural changes.

## Verification Evidence

### Structural acceptance (all 7 criteria PASS)

| Check | Expected | Actual |
|-------|----------|--------|
| `grep -c "func login(email: String, password: String) -> Bool" "ready player 8/UserProfileNetwork.swift"` | 0 | 0 |
| `grep -c "AUTH-GATE-01 (Phase 29.1): removed password-free local login shim" "ready player 8/UserProfileNetwork.swift"` | 1 | 1 |
| `grep -c "AUTH-GATE-01" "ready player 8/UserProfileNetwork.swift"` | 1 | 1 |
| `grep -c "func createAccount(profile: UserProfile) -> Bool" "ready player 8/UserProfileNetwork.swift"` | 1 | 1 |
| `grep -c "func updateProfile(_ profile: UserProfile)" "ready player 8/UserProfileNetwork.swift"` | 1 | 1 |
| `grep -c "func logout()" "ready player 8/UserProfileNetwork.swift"` | 1 | 1 |
| `grep -rn "profileStore\.login\|UserProfileStore\.shared\.login\|\.login(email:" "ready player 8"` | no matches | no matches |

### Build verification

```
$ xcodebuild build -scheme "ready player 8" -destination "platform=iOS Simulator,name=iPhone 17" 2>&1 | tail -3
... (signing + validation lines)
** BUILD SUCCEEDED **
```

### Test-target compile verification

```
$ xcodebuild build-for-testing -scheme "ready player 8" -destination "platform=iOS Simulator,name=iPhone 17" 2>&1 | grep "error:" | awk -F':[0-9]+:' '{print $1}' | awk -F'/' '{print $NF}' | sort -u
ready_player_8Tests.swift
# → Exactly 1 file; AuthGateTests.swift is NOT in the failed-commands list; pre-existing blocker since 9805e17
```

### Regression-guard grep output (zero production-source callers)

```
$ grep -rn "profileStore\.login\|UserProfileStore\.shared\.login\|\.login(email:" "ready player 8"
# (no output — zero call sites anywhere in the iOS source tree)

$ grep -rn "profileStore\.login\|UserProfileStore\.shared\.login\|\.login(email:" "ready player 8Tests"
ready player 8Tests/AuthGateTests.swift:121:    // MARK: - Criterion E: UserProfileStore.login(email:password:) removed
ready player 8Tests/AuthGateTests.swift:132:        // When UserProfileStore.login(email:password:) is gone, this line would fail
# → Both matches are inside comments (MARK and doc comment), NOT actual call sites. Intentional references in the Criterion E stub that documents this plan's acceptance criterion.
```

### Neighboring-method byte-identity evidence

Pre-edit `createAccount` body: lines 65-78 (14 lines including closing brace). Post-edit `createAccount` body: lines 65-78 (same range, byte-identical — confirmed by `git diff` hunk shows the only modified region is the 80-88 range).

Pre-edit `updateProfile` body: lines 90-98. Post-edit `updateProfile` body: lines 85-93 (same 9-line body, shifted up 5 lines due to the net -5 line delta from the deletion).

Pre-edit `logout` body: lines 129-132. Post-edit `logout` body: lines 124-127 (same 4-line body, shifted up 5 lines).

Line-count delta consistent with plan's ~5 line net shrink expectation: 9 removed − 4 added = 5 net removed.

### Git diff (task 1)

```
diff --git a/ready player 8/UserProfileNetwork.swift b/ready player 8/UserProfileNetwork.swift
@@ -77,15 +77,10 @@ final class UserProfileStore: ObservableObject {
         return true
     }
 
-    func login(email: String, password: String) -> Bool {
-        if let user = allUsers.first(where: { $0.email.lowercased() == email.lowercased() }) {
-            currentUser = user
-            saveJSON(profileKey, value: user)
-            updateSuggestions(for: user)
-            return true
-        }
-        return false
-    }
+    // AUTH-GATE-01 (Phase 29.1): removed password-free local login shim.
+    // Authentication MUST go through SupabaseService.signIn (accessToken is the
+    // source of truth for `isAuthenticated`). Do NOT re-introduce a local-only
+    // login method — see .planning/phases/29.1-fix-critical-auth-bug/29.1-RESEARCH.md §Candidate 1.
 
     func updateProfile(_ profile: UserProfile) {
         currentUser = profile
```

One hunk. 9 lines out, 4 lines in. Adjacent createAccount (closing `}` + `return true`) and updateProfile (opening declaration + body) unchanged in diff output.

## Issues Encountered

- **`xcodebuild test` end-to-end unrun** — identical blocker to Plans 01 and 02 (pre-existing async/concurrency errors in `ready_player_8Tests.swift` since Phase 13–15, tracked in phase `deferred-items.md` for Phase 30 remediation cluster). All 7 structural acceptance criteria + iOS app build + test-target compile-only verification + plan-level verification pass; Plan 03's correctness is fully demonstrated via grep evidence + `** BUILD SUCCEEDED **`.

## Threat Flags

None. Plan 03 only removes code (9 lines out, 4 lines of static comment in). No new network endpoints, auth paths, file access patterns, schema changes, or trust-boundary surface introduced. On the contrary, the removal SHRINKS the trust-boundary surface by eliminating an in-process Swift API that accepted and ignored a password argument — T-29.1-03-S01 (spoofing via password-free login) is MITIGATED exactly as the threat register specified. T-29.1-03-T01 (tampering with neighboring methods) MITIGATED via byte-identical preservation (grep-verified). T-29.1-03-R01 (repudiation of removal rationale) MITIGATED via AUTH-GATE-01 sentinel comment + RESEARCH.md pointer. T-29.1-03-E01 (future re-introduction) MITIGATED by sentinel's explicit "Do NOT re-introduce" warning — residual risk accepted (not compiler-enforced but code-review-detectable via `grep AUTH-GATE-01`).

## Known Stubs

None introduced by this plan. The `testProfileStoreLocalLoginRemoved` @Test stub in AuthGateTests.swift remains a trivially-passing `#expect(Bool(true), ...)` because the plan's design (RESEARCH §Candidate 1 + Plan 01 Wave 0 comment at AuthGateTests.swift:127-137) specifies that the real assertion for Criterion E lives in Plan 03's grep-based acceptance criteria, not in runtime test logic. A compile-time assertion would break the build rather than fail the test, which is the wrong failure mode. This plan's 7 grep acceptance criteria (all PASS) are the authoritative Criterion E verification.

## User Setup Required

None — no external services, env vars, dashboard config, or user actions introduced. The change is purely a source-level deletion + comment insertion.

## Next Phase Readiness

- **Plan 04 can land:** its gate-predicate rebinding can now safely assume that no local-only login path exists in the iOS codebase. The only way for `UserProfileStore.currentUser` to become non-nil after Plan 03 is via `createAccount(profile:)` (signup-for-new-email path, already gated by Supabase signIn/signUp flow elsewhere per Phase 29.1 RESEARCH) or `updateProfile(_:)` (which requires a pre-existing `currentUser` to overwrite). No email-only shortcut remains.
- **Plan 05 verification sweep:** has 3/3 must-haves for this plan's `files_modified` scope to confirm against — grep the same 3 patterns used in acceptance criteria, confirm iOS build green.
- **Open blocker for end-to-end test runs:** pre-existing `ready_player_8Tests.swift` async/concurrency errors still need resolution (Phase 30 NOTIF cluster or a dedicated quick task) before the full `xcodebuild test` suite can run green. Plan 03's contract is correct regardless, and the structural grep evidence anchors Criterion E of AuthGateTests.

## Self-Check: PASSED

- `ready player 8/UserProfileNetwork.swift` (modified) — FOUND
- Commit `fe778f6` (Task 1 — UserProfileStore.login deletion + sentinel) — FOUND
- Sentinel comment present (`grep AUTH-GATE-01` returns 1 match in UserProfileNetwork.swift) — CONFIRMED
- Method fully removed (`grep "func login(email: String, password: String) -> Bool"` returns 0) — CONFIRMED
- iOS build green on iPhone 17 simulator (`** BUILD SUCCEEDED **`) — CONFIRMED

---
*Phase: 29.1-fix-critical-auth-bug*
*Completed: 2026-04-21*
