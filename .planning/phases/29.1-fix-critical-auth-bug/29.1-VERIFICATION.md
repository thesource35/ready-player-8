# Phase 29.1 — Verification

**Phase**: 29.1 — fix-critical-auth-bug
**Verified**: 2026-04-21
**Status**: Partial — pending human UAT walkthrough (Task 3 checkpoint owns the flip to Satisfied on "pass")
**Requirements closed**: AUTH-GATE-01, AUTH-GATE-02, AUTH-GATE-03

All `grep`/`xcodebuild` commands below were run at execution time (2026-04-21) against the current working tree. Outputs are pasted verbatim. The `xcodebuild test` rows call out the pre-existing `ready_player_8Tests.swift` async/concurrency blocker (documented in `.planning/phases/29.1-fix-critical-auth-bug/deferred-items.md`, first logged in Phase 22, out of scope for 29.1 per Plans 01/02/03/04 precedent) — compile-only verification is substituted with a cross-reference, matching the posture adopted across the whole phase.

## Goal-Backward Verification

### Truth 1 — The iOS auth gate is driven by the Supabase session (AUTH-GATE-01)

| Evidence Type | Detail | Result |
|---|---|---|
| Code (grep) | `grep -n "if !supabase.isAuthenticated {" "ready player 8/ContentView.swift"` | `650:            if !supabase.isAuthenticated {` — gate predicate at L650 (research-baseline L638 drifted to L650 after Plan 04's +5/-2 edit) |
| Code (grep) | `grep -c "AUTH-GATE-01 (Phase 29.1): server session is the source of truth" "ready player 8/ContentView.swift"` | `1` |
| Code (grep) | `grep -rn "profileStore\.currentUser == nil" "ready player 8"` | `ready player 8/ContentView.swift:406:                if profileStore.currentUser == nil {` — ONLY match, inside `#if DEBUG` demo-button block (L402-419), anticipated by plan |
| Test (compile-only) | `xcodebuild test -only-testing:"ready player 8Tests/AuthGateTests/testGatePredicateTrustsSupabaseSession"` | Test target fails to link due to pre-existing `ready_player_8Tests.swift` async/concurrency errors (45+ errors, lines 103-150, 409-447). AuthGateTests.swift itself compiles clean (not in the failed-commands list). See `.planning/phases/29.1-fix-critical-auth-bug/deferred-items.md`. Compile-only verification per Plan 01/02/03/04 precedent. |
| Test (compile-only) | `xcodebuild test -only-testing:"ready player 8Tests/AuthGateTests/testGatePassesWhenTokenSetEvenWithoutProfile"` | Same pre-existing blocker. AuthGateTests.swift compiles clean. |
| Manual | UAT scenario 1 from VALIDATION.md §Manual-Only Verifications | fill after Task 3 checkpoint |

### Truth 2 — signOut clears both Supabase session AND local profile (AUTH-GATE-02)

| Evidence Type | Detail | Result |
|---|---|---|
| Code (grep) | `grep -c "func signOutEverywhere()" "ready player 8/SupabaseService.swift"` | `1` |
| Code (grep) | `grep -c "supabase\.signOutEverywhere()" "ready player 8/SettingsProfileView.swift"` | `1` |
| Code (grep) | `grep -c "supabase\.signOut()" "ready player 8/SettingsProfileView.swift"` | `0` (old call-site fully replaced) |
| Test (compile-only) | `xcodebuild test -only-testing:"ready player 8Tests/AuthGateTests/testSignOutClearsBothStores"` | Same pre-existing blocker. Test D was tightened in Plan 02 to anchor on the composite call (`svc.signOutEverywhere()`), 3 `#expect` invariants preserved. AuthGateTests.swift compiles clean. |
| Manual | UAT scenario 3 from VALIDATION.md | fill after Task 3 |

### Truth 3 — Signup is server-first; rollback on Supabase failure (AUTH-GATE-03)

| Evidence Type | Detail | Result |
|---|---|---|
| Code (grep) | `grep -c "Couldn't create your account: " "ready player 8/ContentView.swift"` | `1` — new retryable T4 copy per UI-SPEC §Copywriting Contract |
| Code (grep) | `grep -c "Account created locally but server signup failed" "ready player 8/ContentView.swift"` | `0` — old broken-invariant fallback string removed |
| Code (ordering) | `try await supabase.signUp` precedes `profileStore.createAccount(profile: profile)` inside the signup Task body | PASS — verified in Plan 04 Task 2 (commit `7503bbd`) per acceptance criterion; SUMMARY §Accomplishments confirms `supabase.signUp()` awaited BEFORE `profileStore.createAccount()` writes local state |
| Test (compile-only) | `xcodebuild test -only-testing:"ready player 8Tests/AuthGateTests/testSignupRollbackOnServerFailure"` | Same pre-existing blocker. Test C body was completed in Plan 04 Task 3 (commit `9c8679b`) — exercises `SupabaseError.notConfigured` path, asserts both stores clean post-throw. AuthGateTests.swift compiles clean. |
| Manual | UAT scenario 2 from VALIDATION.md | fill after Task 3 |

### Truth 4 — Password-free local login shim removed (AUTH-GATE-01 defense-in-depth)

| Evidence Type | Detail | Result |
|---|---|---|
| Code (grep) | `grep -c "func login(email: String, password: String) -> Bool" "ready player 8/UserProfileNetwork.swift"` | `0` — method deleted in Plan 03 (commit `fe778f6`) |
| Code (grep) | `grep -c "AUTH-GATE-01 (Phase 29.1): removed password-free local login shim" "ready player 8/UserProfileNetwork.swift"` | `1` — sentinel comment anchors the removal to RESEARCH §Candidate 1 |
| Code (grep) | `grep -rn "profileStore\.login\|UserProfileStore\.shared\.login\|\.login(email:" "ready player 8"` | (no matches in production source tree `ready player 8/`; the 2 matches in `ready player 8Tests/AuthGateTests.swift:157,168` are MARK + doc comments inside Criterion E's @Test stub, documented in Plan 03 SUMMARY Verification Evidence — NOT actual call sites) |
| Test (compile-only) | `xcodebuild test -only-testing:"ready player 8Tests/AuthGateTests/testProfileStoreLocalLoginRemoved"` | Same pre-existing blocker. Test E is grep-anchored (not compile-anchored) by design — Plan 03 SUMMARY §Known Stubs explains the trivially-passing `#expect(Bool(true))` rationale; the 7 grep acceptance criteria in Plan 03 are Criterion E's authoritative verification. |

### Truth 5 — Web platform unchanged

| Evidence Type | Detail | Result |
|---|---|---|
| Git | `git diff --stat 65a5fb8..HEAD -- "web/"` (65a5fb8 = parent of 371c260 = Phase 29.1 start commit) | (empty — zero files changed under web/ across the entire 14-commit phase 29.1 range) |

## Full Suite Gate

| Suite | Command | Result |
|---|---|---|
| AuthGateTests (scoped) | `xcodebuild test -only-testing:"ready player 8Tests/AuthGateTests" -scheme "ready player 8" -destination "platform=iOS Simulator,name=iPhone 17"` | FAILED TO RUN due to pre-existing `ready_player_8Tests.swift` async/concurrency errors (same file that has blocked Plans 01/02/03/04 since commit `9805e17` "Phases 13–15"). AuthGateTests.swift itself compiles clean and is NOT in the failed-commands list. Phase 30 NOTIF remediation cluster owns the fix. |
| Full iOS (build) | `xcodebuild build -scheme "ready player 8" -destination "platform=iOS Simulator,name=iPhone 17"` | `** BUILD SUCCEEDED **` (production app target green at phase close) |
| Full iOS (test) | `xcodebuild test -scheme "ready player 8" -destination "platform=iOS Simulator,name=iPhone 17"` | FAILED TO RUN — same pre-existing `ready_player_8Tests.swift` blocker. Compile-only verification substituted per phase-wide precedent. |
| Full web | `cd web && npm test` | Not run — `git diff --stat 65a5fb8..HEAD -- web/` is empty; zero files touched under web/ during Phase 29.1, so no regression surface. Web suite is unchanged from pre-phase. (iPhone 16 simulator substituted with iPhone 17 here for identical reason as Plans 02/03/04: iPhone 16 variant is not installed locally; iPhone 17 targets the same iOS 26.2 SDK — environment substitution only.) |

## UI-SPEC Preservation Audit

Per UI-SPEC §Preservation Checklist, the AuthGateView visual language is frozen except for the ONE T4 signup-failure error copy change (documented in Truth 3 above). Evidence:

| Frozen Element | grep command | Result |
|---|---|---|
| "Sign in to your account" heading | `grep -c "Sign in to your account" "ready player 8/ContentView.swift"` | `1` |
| "CREATE ACCOUNT" CTA | `grep -c 'Text("CREATE ACCOUNT").font' "ready player 8/ContentView.swift"` | `1` |
| "SIGN IN" CTA | `grep -c 'Text("SIGN IN").font' "ready player 8/ContentView.swift"` | `1` |
| "VERIFY" CTA | `grep -c 'Text("VERIFY")' "ready player 8/ContentView.swift"` | `1` |
| "Trusted by 142,891…" footer | `grep -c "Trusted by 142,891" "ready player 8/ContentView.swift"` | `1` |
| Trade list (12 items) | `grep -c '\["General", "Electrical", "Concrete"' "ready player 8/ContentView.swift"` | `1` |
| "Forgot password?" link | `grep -c "Forgot password" "ready player 8/ContentView.swift"` | `1` |
| Trust badges (256-bit / SOC 2 / GDPR) | `grep -c "256-bit encryption\|SOC 2 compliant\|GDPR ready" "ready player 8/ContentView.swift"` | `3` |
| Inline error visual | `grep -c 'Image(systemName: "exclamationmark.triangle.fill")' "ready player 8/ContentView.swift"` | `2` (>= 3 expected per plan — see note below) |

**Note on error-icon count (2 vs >=3):** The plan's acceptance expectation was `>= 3` based on the UI-SPEC documenting three inline-error sites (L149-150, L245-246, L356). The actual count at execution time is `2`. Investigation: Plan 04 Task 2 inverted the signup error-copy path — the old "Account created locally but server signup failed" fallback slot was consolidated into a single retryable inline row when the signup ordering was inverted. Two error-icon render sites remain (login L149-ish, 2FA L356-ish); the signup inline-error row now shares the login inline pattern via a single call site rather than appearing as a distinct third icon line. UI-SPEC §Preservation Checklist explicitly allows the T4 signup-copy change; the icon-render consolidation is a benign downstream of that inversion — not a visual regression (the signup form still renders the error row with the same `Theme.red` + exclamation triangle when populated, just via a shared code path). Documented here for reviewer transparency; no action needed.

## Deferred (Out of Scope for 29.1)

Per RESEARCH §Out of scope:

- JWT signature verification in `web/src/middleware.ts` (defense-in-depth; defer to performance-vs-security phase)
- `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly` on KeychainHelper (backup exposure; trivial but not urgent)
- `orgId = ""; canManage = true` hardcoded TODO in `web/src/app/projects/[id]/page.tsx:12-15` (authorization, not authentication; Phase 22 follow-up)
- `SupabaseService.signIn()` error distinction (invalid creds vs network) — usability polish
- Pre-existing `ready_player_8Tests.swift` + `ReportTests.swift` async/concurrency errors — blocks end-to-end `xcodebuild test`; tracked in phase `deferred-items.md`; Phase 30 NOTIF remediation cluster

## UAT Verdict

See Task 3 checkpoint for outcome. Record per scenario: PASS / FAIL / DEFERRED with rationale.
