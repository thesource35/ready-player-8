---
phase: 22
plan: 08
type: execute
wave: 3
depends_on: ["22-05", "22-06", "22-07"]
files_modified:
  - "ready player 8/Video/CamerasSection.swift"
  - "ready player 8/Video/AddCameraWizard.swift"
  - "ready player 8/Video/ClipUploadSheet.swift"
  - "ready player 8/Video/CameraCard.swift"
  - "ready player 8/Video/ClipCard.swift"
  - "ready player 8/ProjectsView.swift"
  - web/src/app/projects/[id]/cameras/CamerasSection.tsx
  - web/src/app/projects/[id]/cameras/CameraCard.tsx
  - web/src/app/projects/[id]/cameras/ClipCard.tsx
  - web/src/app/projects/[id]/cameras/AddCameraWizard.tsx
  - web/src/app/projects/[id]/cameras/ClipUploadCard.tsx
  - web/src/app/projects/[id]/cameras/SoftCapBanner.tsx
  - web/src/app/projects/[id]/page.tsx
autonomous: false
requirements:
  - VIDEO-01-D
  - VIDEO-01-G
must_haves:
  truths:
    - "ProjectDetail (iOS + web) renders a Cameras section alongside Overview/Documents/Team (D-20) — no top-level nav tab added"
    - "Add camera wizard: 2 steps (name+audio → RTMP URL+stream key), calls /api/video/mux/create-live-input, stream_key shown once then dropped"
    - "Audio toggle in wizard step 1 shows jurisdiction warning stripe + confirmation modal per D-35"
    - "Soft cap: at 16 cameras (80%) show warning banner; at 20 disable Add camera button (D-28)"
    - "Clip upload: file picker → pre-check (2GB/60min/container per D-31) → tus upload with progress → cs_video_assets row → polling UI until status=ready"
    - "Portal toggle badge on each ClipCard (only visible to owner/admin per D-39), disabled on drone source_type with tooltip (D-22)"
    - "Status transitions are visibly animated (200ms fade, no silent swaps) per UI-SPEC.md"
  artifacts:
    - path: "ready player 8/Video/CamerasSection.swift"
      provides: "iOS SwiftUI CamerasSection used inside ProjectDetail"
    - path: web/src/app/projects/[id]/cameras/CamerasSection.tsx
      provides: "React CamerasSection used inside project detail page"
    - path: web/src/app/projects/[id]/page.tsx
      provides: "Project detail page updated to render CamerasSection"
    - path: "ready player 8/ProjectsView.swift"
      provides: "ProjectDetail view updated to render CamerasSection"
  key_links:
    - from: Add camera wizard (iOS + web)
      to: POST /api/video/mux/create-live-input
      via: "fetch with project_id + org_id + name + audio_enabled"
      pattern: "create-live-input"
    - from: Clip upload UI
      to: VideoUploadClient (iOS) / tus-js-client (web) + /api/video/vod/upload-url
      via: "POST upload-url → tus upload to videos/{org}/{project}/{asset}/raw.ext"
      pattern: "upload-url"
    - from: ClipCard polling
      to: cs_video_assets row status watcher
      via: "2s poll via VideoSyncManager (iOS) / SWR revalidate (web)"
      pattern: "status.*ready"
---

<objective>
Wire the Cameras section into ProjectDetail on both platforms. Wizard, upload sheet, camera cards, clip cards, soft-cap banner, portal-visibility toggles, and status animations all land here.

Purpose: This is the user-facing surface of Phase 22. Every prior plan provides infrastructure; this plan is what users actually see.

Honors D-20 (ProjectDetail section, no nav tab), D-23 (2-step wizard with Mux create-live-input → RTMP+key copy), D-24 (file picker + 3x retry + polling pattern mirroring Phase 13), D-28 (soft cap warning at 16, disable at 20), D-35 (audio jurisdiction warning + confirmation modal), D-38 (name field with filename pre-fill), D-39 (governance-gated controls for owner/admin), all UI-SPEC.md copy and color rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-live-site-video-per-project-hls-camera-feeds-tied-to-project/22-CONTEXT.md
@.planning/phases/22-live-site-video-per-project-hls-camera-feeds-tied-to-project/22-UI-SPEC.md
@ready player 8/Video/VideoSyncManager.swift
@ready player 8/Video/VideoUploadClient.swift
@ready player 8/Video/LiveStreamView.swift
@ready player 8/Video/VideoClipPlayer.swift
@ready player 8/Field/FieldPhotoUpload.swift
@ready player 8/ProjectsView.swift
@web/src/app/projects/[id]/page.tsx
@web/src/app/projects/[id]/cameras/LiveStreamView.tsx
@web/src/app/projects/[id]/cameras/VideoClipPlayer.tsx
@web/src/app/projects/[id]/cameras/usePlaybackToken.ts

<interfaces>
iOS ProjectDetail composition: grep `ProjectDetail` in `ready player 8/ProjectsView.swift` (single hit per planner Grep). That view is the parent surface; add a new sub-section.

Web project detail: `web/src/app/projects/[id]/page.tsx` is the existing server component. Already hosts `DailyCrewSection.tsx` sibling. CamerasSection goes next to it.

22-UI-SPEC.md Copywriting contract:
- Empty state heading: `No cameras yet`
- Empty state body: `Register a jobsite camera to start streaming, or upload a recorded clip. Live streams use Mux; uploads transcode in the background.`
- Primary CTAs: `Add camera` and `Upload clip`
- Wizard step 1 footer: `Continue`, step 2 footer: `Finish`
- RTMP URL helper: `Paste this into your camera or encoder's streaming settings.`
- Stream key helper: `Keep this secret — it works like a password. You can copy it now, but it won't be shown again.`
- Audio toggle confirmation: `Enable audio capture?` with body `Recording audio may require two-party consent in your state or country. Only turn this on if everyone who may be recorded has given consent.` + accent `Enable audio` button
- Soft cap banner: `Camera limit reached (20). Archive an unused camera or contact support to raise the cap.` + Contact support CTA
- Clip name pre-fill: filename stripped of extension, optional `_YYYY-MM-DD` suffix if capture timestamp in metadata (D-38)
- Destructive delete modals: accent Cancel + red destructive button with exact object name

Status color vocabulary (UI-SPEC.md §Color):
- green = Live/Ready/Connected
- gold = Transcoding/Idle/In-progress
- red = Offline/Failed/Destructive
- purple = Portal visibility

Animation rule: 200ms ease-out fade-through on all status transitions (UI-SPEC.md Interaction Contracts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: iOS — CameraCard + ClipCard + AddCameraWizard + ClipUploadSheet + CamerasSection</name>
  <files>"ready player 8/Video/CameraCard.swift", "ready player 8/Video/ClipCard.swift", "ready player 8/Video/AddCameraWizard.swift", "ready player 8/Video/ClipUploadSheet.swift", "ready player 8/Video/CamerasSection.swift"</files>
  <read_first>
    "ready player 8/Video/VideoSyncManager.swift",
    "ready player 8/Video/VideoUploadClient.swift",
    "ready player 8/Video/LiveStreamView.swift",
    "ready player 8/Video/VideoClipPlayer.swift",
    "ready player 8/Video/VideoPlayerChrome.swift",
    "ready player 8/ThemeAndModels.swift",
    "ready player 8/Field/FieldPhotoUpload.swift",
    .planning/phases/22-live-site-video-per-project-hls-camera-feeds-tied-to-project/22-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: CameraCard renders thumbnail + status badge + name + location; tap opens sheet hosting LiveStreamView
    - Test 2: ClipCard renders poster + duration + status; tap opens VideoClipPlayer (status='ready' only, others show inline state)
    - Test 3: AddCameraWizard step 1 validates name length (1-128 D-UI-SPEC.md); audio toggle on shows jurisdiction warning stripe (red) + requires confirmation modal
    - Test 4: AddCameraWizard step 2 shows RTMP URL + stream key in monospace with Copy buttons (default "Copy" → "Copied" after press, reverts 2s); `Finish` button dismisses sheet and appends new source to VideoSyncManager
    - Test 5: ClipUploadSheet runs VideoUploadClient.probeFile → surfaces D-31 errors inline → on success starts upload with progress bar
    - Test 6: CamerasSection renders: soft-cap banner (at 16+ visible, at 20+ banner text says "Camera limit reached"), Live Cameras subsection with CameraCards, Recent Clips subsection with ClipCards, empty states per UI-SPEC.md
    - Test 7: Clip card status animations use `.animation(.easeOut(duration: 0.2))` on status transitions
  </behavior>
  <action>
    Create 5 new Swift files under ready player 8/Video/ (Xcode PBXFileSystemSynchronizedRootGroup auto-includes).

    CameraCard.swift: SwiftUI View — accepts `VideoSource` + onTap callback. Layout:
    - Top: 16:9 thumbnail placeholder (Theme.panel background; a `poster` URL field would be wired here if/when server exposes it — for v1 just a neutral panel)
    - Status badge overlay top-left using `LiveStatusBadge(isLive: source.status == .active, isOffline: source.status == .offline)` — during offline-within-grace show gold "Reconnecting…" per UI-SPEC.md (the source.status doesn't encode grace vs past-grace; read source.lastActiveAt and compute `source.status == .offline && Date().timeIntervalSince(lastActiveAt ?? distantPast) < 300` → show "Reconnecting…"; else the red "Offline · last seen {relative}" per UI-SPEC.md)
    - Bottom overlay: source.name (13px medium) + source.locationLabel in 10px muted
    - Theme.surface background, 14px corner radius (UI-SPEC.md)
    - `.premiumGlow(cornerRadius: 14, color: Theme.accent)` when source.status == .active

    ClipCard.swift: SwiftUI View — accepts `VideoAsset`. Layout:
    - 16:9 poster area (currently Theme.panel; a poster URL field would hydrate from Supabase Storage {asset}/poster.jpg via signed URL; v1 may leave as placeholder with asset duration overlay)
    - Status badge per UI-SPEC.md status vocabulary:
      - ready → green READY
      - transcoding → gold shimmer TRANSCODING
      - failed → red FAILED with inline `Retry transcode` button (only visible 24h from asset.createdAt per D-33; after that hide the button but keep the row)
      - uploading → gold `Uploading · {percent}%` (percent computed by sibling upload client writing to VideoSyncManager)
    - Purple `PORTAL` badge pill when asset.portalVisible == true
    - Cyan `DRONE` pill when asset.sourceType == .drone (informational — portal exposure separately blocked by D-22)
    - Context menu on tap-hold: Share with portal (only owner/admin per D-39), Remove from portal (owner/admin), Delete clip (owner/admin OR created_by self); Delete triggers confirmation modal per UI-SPEC.md destructive-actions table
    - `.animation(.easeOut(duration: 0.2), value: asset.status)` on root ZStack for 200ms transitions

    AddCameraWizard.swift: SwiftUI View presented as `.sheet`. 2-step state machine:
    - @State wizardStep: Int = 1
    - Step 1: form with Name TextField (required 1-128), Location Label TextField (optional 0-256), Toggle "Capture audio" (default off; when toggled ON shows red warning stripe at top with D-35 copy and requires confirmation modal with D-35 exact text + "Enable audio" accent button)
    - `Continue` button: validates → shows spinner → POST to `{apiBase}/api/video/mux/create-live-input` with `{project_id, org_id, name, location_label, audio_enabled}` → on 403 camera_limit_reached show inline error from AppError.cameraLimitReached → on 502/500 show AppError.muxIngestFailed → on 201 advance to step 2 storing the returned stream_key + rtmp_url + playback_id in local @State
    - Step 2: Display RTMP URL field (monospace, Copy button), Stream Key field (monospace, Copy button), helper copy verbatim from UI-SPEC.md. Copy button state default="Copy", active="Copied" (reverts after 2s using a Task.sleep).
    - `Finish` button: dismiss sheet; call VideoSyncManager.upsertSource() locally; parent triggers re-sync

    ClipUploadSheet.swift: SwiftUI View presented as `.sheet`. Uses PhotosPicker for Photos, fileImporter for Files:
    - On selection → call `VideoUploadClient.probeFile` → surface any D-31 AppError inline
    - Optional Name field pre-filled with filename stripped of extension (D-38); if asset metadata has capture timestamp, append `_YYYY-MM-DD`
    - On Upload tap → start VideoUploadClient.upload; progress bar updates via callback; on success dismiss + call VideoSyncManager.upsertAsset; on failure surface AppError
    - Use `Theme.gold` shimmer during transcode state (the sheet actually dismisses once upload completes — transcode shimmer is on the ClipCard itself, not here)

    CamerasSection.swift: Parent container rendered inside ProjectDetail. Accepts projectId + orgId:
    - `@StateObject private var sync = VideoSyncManager.shared`
    - `.task { await sync.syncProject(projectId, service: SupabaseService.shared) }`
    - Header: 16px/700 title "Cameras" + 2 buttons (Add camera primary, Upload clip secondary ghost)
    - Soft-cap banner above header when fixedCameraCount >= 16 (warning copy) / >= 20 (disabled Add camera button + "Camera limit reached (20)")
    - Live Cameras subsection: grid of CameraCards for sources.filter { $0.kind == .fixedCamera && $0.status != .archived }
    - Recent Clips subsection: list of ClipCards for assets.filter { $0.kind == .vod } ordered by startedAt desc
    - Empty states per UI-SPEC.md §Empty states (0 cameras+0 clips → `No cameras yet`; 0 live but has clips → `No live cameras`; has cameras but 0 clips → `No clips yet`; Supabase unconfigured → `Preview mode` with Big Buck Bunny mock source)
    - Outer 24px padding, 32px section gap to sibling sections (UI-SPEC.md §Spacing)

    Update ready player 8/ProjectsView.swift — grep for `ProjectDetail` (planner confirmed 1 hit). Find the body that renders Overview / Documents / Team (or the equivalent composition) and insert `CamerasSection(projectId: project.id, orgId: project.orgId)` alongside. Do NOT break any existing composition; additive only.
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8" && ls "ready player 8/Video/CameraCard.swift" "ready player 8/Video/ClipCard.swift" "ready player 8/Video/AddCameraWizard.swift" "ready player 8/Video/ClipUploadSheet.swift" "ready player 8/Video/CamerasSection.swift" && grep -c "struct CamerasSection" "ready player 8/Video/CamerasSection.swift" && grep -c "struct AddCameraWizard" "ready player 8/Video/AddCameraWizard.swift" && grep -c "create-live-input" "ready player 8/Video/AddCameraWizard.swift" && grep -c "PhotosPicker" "ready player 8/Video/ClipUploadSheet.swift" && grep -c "fileImporter\|UTType.movie" "ready player 8/Video/ClipUploadSheet.swift" && grep -c "VideoUploadClient" "ready player 8/Video/ClipUploadSheet.swift" && grep -c "CamerasSection" "ready player 8/ProjectsView.swift" && grep -c "No cameras yet\|No live cameras\|No clips yet" "ready player 8/Video/CamerasSection.swift" && grep -c "Camera limit reached" "ready player 8/Video/CamerasSection.swift" && grep -c "Capture audio\|audio_enabled\|audioEnabled" "ready player 8/Video/AddCameraWizard.swift" && xcodebuild -scheme "ready player 8" -destination 'platform=iOS Simulator,name=iPhone 15' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 5 new Swift files exist under ready player 8/Video/
    - grep `struct CameraCard`, `struct ClipCard`, `struct AddCameraWizard`, `struct ClipUploadSheet`, `struct CamerasSection` each return 1
    - grep `create-live-input` in AddCameraWizard.swift returns at least 1 (API call)
    - grep `PhotosPicker` in ClipUploadSheet.swift returns 1
    - grep `fileImporter` OR `UTType.movie`/`.mpeg4Movie`/`.quickTimeMovie` in ClipUploadSheet.swift returns at least 1
    - grep `VideoUploadClient` in ClipUploadSheet.swift returns at least 1
    - grep `CamerasSection` in ready player 8/ProjectsView.swift returns at least 1 (wired into parent view)
    - grep UI-SPEC.md empty-state headings in CamerasSection.swift — `No cameras yet` present (grep returns ≥1 of those strings)
    - grep `Camera limit reached` in CamerasSection.swift returns 1 (D-28)
    - grep `Capture audio` OR `audioEnabled` in AddCameraWizard.swift returns at least 1 (D-35 UI)
    - grep `Recording audio may require` in AddCameraWizard.swift returns 1 (D-35 consent copy)
    - grep `.easeOut(duration: 0.2)` OR `.animation` with duration 0.2 in ClipCard.swift OR CameraCard.swift returns at least 1 (UI-SPEC.md animation rule)
    - `xcodebuild build` exits 0
  </acceptance_criteria>
  <done>5 iOS Video UI files compile, wired into ProjectDetail via ProjectsView.swift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Web — CamerasSection + cards + wizard + upload card + soft-cap banner + wire into page.tsx</name>
  <files>web/src/app/projects/[id]/cameras/CamerasSection.tsx, web/src/app/projects/[id]/cameras/CameraCard.tsx, web/src/app/projects/[id]/cameras/ClipCard.tsx, web/src/app/projects/[id]/cameras/AddCameraWizard.tsx, web/src/app/projects/[id]/cameras/ClipUploadCard.tsx, web/src/app/projects/[id]/cameras/SoftCapBanner.tsx, web/src/app/projects/[id]/page.tsx, web/package.json</files>
  <read_first>
    web/src/app/projects/[id]/page.tsx,
    web/src/app/projects/[id]/DailyCrewSection.tsx,
    web/src/app/projects/[id]/cameras/LiveStreamView.tsx,
    web/src/app/projects/[id]/cameras/VideoClipPlayer.tsx,
    web/src/lib/video/types.ts,
    .planning/phases/22-live-site-video-per-project-hls-camera-feeds-tied-to-project/22-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: CamerasSection.tsx is a client component that fetches sources+assets on mount, renders soft-cap banner, live cameras grid, recent clips grid, empty states per UI-SPEC.md
    - Test 2: AddCameraWizard step 1 form posts to /api/video/mux/create-live-input; step 2 shows RTMP URL + stream key with Copy buttons
    - Test 3: ClipUploadCard uses tus-js-client (RESEARCH.md verified dep at ^4.3.1), chunks at 6MB, retries [0,3000,5000,10000,20000] per RESEARCH §Upload UX
    - Test 4: SoftCapBanner displays warning at count >= 16, disable-cta state at count >= 20 (D-28)
    - Test 5: page.tsx imports and renders CamerasSection below existing sections
  </behavior>
  <action>
    Add dep: `cd web && npm install tus-js-client@^4.3.1` (per RESEARCH.md verified version for Supabase resumable uploads).

    Create 6 React components:

    CamerasSection.tsx (client component — `'use client'`):
    - Accepts `{ projectId: string; orgId: string; canManage: boolean }` props where canManage = user has owner/admin role (determined by parent server component which knows the session)
    - useEffect fetches sources + assets via Supabase client (use existing SSR helper or direct `supabase.from('cs_video_sources').select('*').eq('project_id', projectId)` — RLS handles org scoping)
    - Polls every 5s while any asset has status='transcoding' or 'uploading' (cheap revalidation); otherwise idle
    - Renders: SoftCapBanner (if count >= 16), header (title + Add camera + Upload clip buttons), live cameras grid, recent clips list, empty states per UI-SPEC.md
    - 24px outer padding, 32px gap

    CameraCard.tsx (client component):
    - Accepts `{ source: VideoSource, canManage: boolean, onOpen: () => void, onDelete: () => void }`
    - Renders thumbnail area (16:9, var(--panel) background), status badge (green/gold/red per UI-SPEC.md), name + location label
    - Click opens player — parent state controls which source is expanded; expanded state renders `<LiveStreamView source={source} />` inline
    - Context menu (3-dot) visible only when canManage=true: Delete camera → confirmation modal with UI-SPEC.md copy → calls DELETE /api/video/mux/delete-live-input

    ClipCard.tsx (client component):
    - Accepts `{ asset: VideoAsset, canManage: boolean, onRetry: () => void, onToggleVisibility: (v: boolean) => void }`
    - Renders status-aware UI matching UI-SPEC.md:
      - `ready` → clickable poster area, play triggers inline `<VideoClipPlayer asset={asset} />`
      - `transcoding` → gold shimmer bar + `Transcoding · about {est} min left`
      - `uploading` → progress bar (wired to upload state from ClipUploadCard when in-progress)
      - `failed` → red X + `Transcode failed. Tap Retry, or re-upload the source file if the problem persists.` + Retry button; visible for 24h from asset.created_at per D-33
    - PORTAL purple pill when asset.portal_visible; DRONE cyan pill when asset.source_type === 'drone'
    - Toggle portal_visible via context menu (visible only when canManage; disabled with tooltip "Drone footage can't be shared via portal in this release." when source_type === 'drone' per D-22)

    AddCameraWizard.tsx (client component — sheet/modal overlay):
    - 2-step form: step 1 name + location + audio toggle (with jurisdiction warning stripe when audio on, D-35 confirmation modal); step 2 RTMP URL + stream key with Copy buttons
    - POST to /api/video/mux/create-live-input with auth
    - Handles 403 camera_limit_reached (show inline error), 500/502 mux_ingest_failed (inline error, can retry)
    - On success store response in state; step 2 displays stream_key ONCE — user is warned it won't be shown again
    - Finish button dismisses modal; parent refreshes camera list

    ClipUploadCard.tsx (client component):
    - Drag-drop zone + `<input type="file" accept="video/mp4,video/quicktime,video/x-m4v">`
    - On file select: client-side pre-check (D-31): size ≤ 2GB (MAX_UPLOAD_SIZE_BYTES from types.ts), extension in {mp4, mov}, duration probe via `<video>` metadata (create temp URL.createObjectURL, load, read .duration; if > 3600 reject)
    - Optional Name field pre-filled with `file.name` stripped of extension (D-38)
    - POST /api/video/vod/upload-url → receive { asset_id, upload_url, auth_token, object_name } → use tus-js-client:
      ```ts
      const upload = new tus.Upload(file, {
        endpoint: upload_url,
        chunkSize: 6 * 1024 * 1024,
        retryDelays: [0, 3000, 5000, 10000, 20000],
        headers: { authorization: `Bearer ${auth_token}`, 'x-upsert': 'false' },
        metadata: { bucketName: 'videos', objectName: object_name, contentType: file.type },
        onProgress: (sent, total) => setProgress(sent / total),
        onSuccess: () => { /* server row already exists; CamerasSection poll picks up status */ },
        onError: (err) => setError(err.message),
      })
      const prev = await upload.findPreviousUploads()
      if (prev.length > 0) upload.resumeFromPreviousUpload(prev[0])
      upload.start()
      ```
    - Error copy from UI-SPEC.md verbatim

    SoftCapBanner.tsx (client component):
    - Accepts `{ fixedCameraCount: number }`
    - At count >= 20: red text `Camera limit reached (20). Archive an unused camera or contact support to raise the cap.` + Contact support button
    - At count >= 16 (80%): gold warning banner with a softer copy (16 of 20 cameras used)
    - Hidden below 16

    Update web/src/app/projects/[id]/page.tsx — this is a server component. Import CamerasSection and render it alongside existing sections (look for where DailyCrewSection is rendered; add CamerasSection after it). Pass projectId, orgId (resolve from session + project row), and canManage (compute from user_orgs role lookup).

    Because page.tsx is server-rendered: CamerasSection starts empty and hydrates client-side. Use server-side prefetch of the initial sources/assets arrays if desired (optional optimization; if skipped, note that loading shimmer shows for ~300ms on first render).
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && jq -r '.dependencies["tus-js-client"]' package.json && ls 'src/app/projects/[id]/cameras/CamerasSection.tsx' 'src/app/projects/[id]/cameras/CameraCard.tsx' 'src/app/projects/[id]/cameras/ClipCard.tsx' 'src/app/projects/[id]/cameras/AddCameraWizard.tsx' 'src/app/projects/[id]/cameras/ClipUploadCard.tsx' 'src/app/projects/[id]/cameras/SoftCapBanner.tsx' && grep -c "'use client'" 'src/app/projects/[id]/cameras/CamerasSection.tsx' && grep -c 'tus-js-client\|import \* as tus' 'src/app/projects/[id]/cameras/ClipUploadCard.tsx' && grep -c 'chunkSize: 6' 'src/app/projects/[id]/cameras/ClipUploadCard.tsx' && grep -c 'create-live-input' 'src/app/projects/[id]/cameras/AddCameraWizard.tsx' && grep -c 'upload-url' 'src/app/projects/[id]/cameras/ClipUploadCard.tsx' && grep -c 'CamerasSection' 'src/app/projects/[id]/page.tsx' && grep -c 'No cameras yet' 'src/app/projects/[id]/cameras/CamerasSection.tsx' && grep -c 'Camera limit reached' 'src/app/projects/[id]/cameras/SoftCapBanner.tsx' && grep -c 'CAMERA_SOFT_CAP\|>= 20\|>= 16' 'src/app/projects/[id]/cameras/SoftCapBanner.tsx' && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `jq -r '.dependencies["tus-js-client"]' web/package.json` returns version >= 4.3.1
    - All 6 component files exist
    - CamerasSection.tsx has `'use client'` + imports of LiveStreamView, VideoClipPlayer, CameraCard, ClipCard, SoftCapBanner, AddCameraWizard, ClipUploadCard
    - ClipUploadCard.tsx: grep `tus-js-client` or `import * as tus` returns 1
    - grep `chunkSize: 6` OR `6 * 1024 * 1024` in ClipUploadCard.tsx returns 1 (6MB chunks per RESEARCH)
    - grep `retryDelays` in ClipUploadCard.tsx returns 1 (3x+ retry)
    - grep `/api/video/mux/create-live-input` in AddCameraWizard.tsx returns 1
    - grep `/api/video/vod/upload-url` in ClipUploadCard.tsx returns 1
    - grep `CamerasSection` in page.tsx returns at least 1 (rendered in parent server component)
    - grep `No cameras yet` in CamerasSection.tsx returns 1 (UI-SPEC empty state)
    - grep `Camera limit reached` in SoftCapBanner.tsx returns 1 (D-28 copy)
    - grep `CAMERA_SOFT_CAP` OR literal `20` with soft-cap intent in SoftCapBanner.tsx returns at least 1
    - grep `Capture audio\|audio_enabled` in AddCameraWizard.tsx returns at least 1 (D-35 UI)
    - grep `two-party consent\|Recording audio may require` in AddCameraWizard.tsx returns 1 (D-35 copy)
    - `cd web && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>6 web components exist, page.tsx renders CamerasSection, typecheck clean, tus upload wired with 6MB chunks + retries.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Cameras section renders on both platforms</name>
  <what-built>
    Full CamerasSection surface on iOS + web: Add camera wizard (both steps), Upload clip flow end-to-end (pre-check → tus → polling → ready → play), soft-cap banner, portal toggle UI, delete confirmations, status animations.
  </what-built>
  <how-to-verify>
    1. (Web) Run `cd web && npm run dev` → open `/projects/{some-project-id}` → confirm Cameras section appears with correct 24px padding, 32px gap to sibling sections.
    2. (Web) Empty state: no cameras + no clips → `No cameras yet` heading + body copy matches UI-SPEC.md.
    3. (Web) Click Add camera → step 1 shows name + location + audio toggle → toggle audio → confirm red jurisdiction stripe appears + confirmation modal with D-35 copy → confirm → Continue posts to /api/video/mux/create-live-input → step 2 shows RTMP URL + stream key in monospace + Copy buttons (state changes to "Copied" for 2s).
    4. (Web) Click Upload clip → drag-drop a small MP4 → validates size/duration/container → Upload → progress bar updates → status flips transcoding → polling detects ready → poster fades in → play works.
    5. (Web) Test a 3GB file → `File is too large…` error verbatim. Test a .mkv → `Unsupported file type…` error verbatim.
    6. (Web) Manually set cs_video_sources row count in org to 16 → refresh → gold warning banner appears. Set to 20 → Add camera button disabled + red banner with `Camera limit reached (20)`.
    7. (iOS) Build + run on iPhone 15 simulator. Open any project → ProjectDetail → scroll to Cameras section → repeat 2-6 on iOS (PhotosPicker + fileImporter both offered for upload).
    8. (iOS) Confirm Theme.green LIVE dot pulses during active live stream (requires a Mux-active camera or mock); Theme.gold for idle/transcoding; Theme.red for offline/failed.
    9. Animations: all status transitions use a 200ms fade — no instant badge-swap feel.
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 verification items pass, or describe any regression (e.g., "audio toggle confirmation missing; soft cap banner copy truncated") so we can fix.</resume-signal>
</task>

</tasks>

<verification>
- Both platforms render a Cameras section under ProjectDetail
- Wizard creates Mux live_input + DB row + shows credentials once per D-23
- Upload path runs end-to-end: pre-check → tus → transcoding → ready → playback
- Soft cap banner visible at 16, disabling at 20 per D-28
- Audio toggle shows jurisdiction warning + confirmation per D-35
- Drone-typed clips show DRONE cyan pill; portal toggle disabled per D-22
- Delete confirmations match UI-SPEC.md destructive-actions table (accent Cancel + red named destructive button)
- 200ms status transitions present on both platforms
</verification>

<success_criteria>
A user can register a camera (get a working RTMP URL + stream key), upload a clip (get back a playable HLS stream in < 2 min for small fixtures), delete a camera (with Mux rollback), and toggle portal visibility — all from ProjectDetail on both iOS and web, with every error path surfacing through AppError or inline UI copy matching UI-SPEC.md verbatim.
</success_criteria>

<output>
After completion, create `.planning/phases/22-live-site-video-per-project-hls-camera-feeds-tied-to-project/22-08-SUMMARY.md`
</output>
