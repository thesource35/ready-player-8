---
phase: 22
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - web/src/__tests__/video/mux-live-input.test.ts
  - web/src/__tests__/video/mux-jwt.test.ts
  - web/src/__tests__/video/mux-webhook.test.ts
  - web/src/__tests__/video/vod-playback.test.ts
  - web/src/__tests__/video/retention.test.ts
  - web/src/__tests__/video/portal-video-auth.test.ts
  - web/src/__tests__/video/cameras-section.test.tsx
  - web/src/__tests__/video/upload-validation.test.ts
  - web/src/__tests__/video/ratelimit.test.ts
  - web/src/__tests__/video/fixtures/mock-mux-webhook-active.json
  - web/src/__tests__/video/fixtures/mock-mux-webhook-disconnected.json
  - web/src/__tests__/video/fixtures/mock-mux-webhook-idle.json
  - web/src/__tests__/video/fixtures/mock-mux-webhook-asset-ready.json
  - web/src/__tests__/video/fixtures/tiny.mp4
  - "ready player 8Tests/VideoTests/VideoModelTests.swift"
  - "ready player 8Tests/VideoTests/LiveStreamViewTests.swift"
  - "ready player 8Tests/VideoTests/VideoClipPlayerTests.swift"
  - "ready player 8Tests/VideoTests/VideoAuthTests.swift"
  - worker/__tests__/transcode.smoke.test.ts
  - worker/package.json
  - worker/tsconfig.json
  - worker/vitest.config.ts
autonomous: true
requirements:
  - VIDEO-00
must_haves:
  truths:
    - Vitest test files exist for every /api/video/* route planned in later waves
    - iOS XCTest stubs exist for VideoSource/VideoAsset model, LiveStreamView, VideoClipPlayer, VideoAuth
    - Worker directory has package.json + vitest config + smoke test stub
    - Fixture files exist with realistic (captured or hand-crafted) Mux webhook JSON payloads + tiny test video
  artifacts:
    - path: web/src/__tests__/video/
      provides: "Test file scaffolding for all 9 video test suites"
    - path: "ready player 8Tests/VideoTests/"
      provides: "iOS XCTest stubs for 4 video test suites"
    - path: worker/
      provides: "ffmpeg worker repo skeleton with vitest"
    - path: web/src/__tests__/video/fixtures/tiny.mp4
      provides: "5-second copyright-safe MP4 fixture for upload+transcode tests"
  key_links:
    - from: Every downstream plan task (<automated> verify)
      to: Corresponding test file created here
      via: "npm run test -- {file}.test.ts or xcodebuild test -only-testing:VideoTests/{name}"
      pattern: "test\\(|describe\\(|func test"
---

<objective>
Create the Wave 0 test scaffolding that every downstream task's `<automated>` verify command depends on. This plan writes stub test files (with one pending/skipped test each) so the test commands referenced in later tasks actually resolve — preventing "MISSING" verify commands in Waves 1-4.

Purpose: Nyquist validation requires every task to have a runnable `<automated>` command. Without Wave 0 scaffolding, executors in Waves 1-4 would hit "no such file" errors and invent ad-hoc tests. This plan locks the test layout up front.
Output: 9 web test files, 4 iOS test files, 1 worker smoke test, 4 Mux webhook fixtures, 1 tiny.mp4 fixture, worker package.json/tsconfig/vitest config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-live-site-video-per-project-hls-camera-feeds-tied-to-project/22-CONTEXT.md
@.planning/phases/22-live-site-video-per-project-hls-camera-feeds-tied-to-project/22-RESEARCH.md
@.planning/phases/22-live-site-video-per-project-hls-camera-feeds-tied-to-project/22-VALIDATION.md
@web/vitest.config.ts
@web/package.json

<interfaces>
<!-- Established test patterns from earlier phases — executor should follow these shapes exactly -->

From web/vitest.config.ts alias: `@/*` → `web/src/*`. Test files live under `web/src/__tests__/` or sibling `.test.ts` files. `npm run test` runs vitest in the web directory (from CLAUDE.md scripts section: `npm run test`).

Existing test directory pattern — check `web/src/__tests__/` for existing suite structure used by Phase 13/20/21.

iOS test bundle: `ready player 8Tests/` with grouped subdirs per phase (e.g., `ScheduleTests/`). Files are XCTest subclasses with `func testX()` methods. Run via `xcodebuild test -scheme "ready player 8" -destination 'platform=iOS Simulator,name=iPhone 15' -only-testing:"ready player 8Tests/VideoTests"`.

Worker is a NEW subdirectory at repo root `/worker/` (per RESEARCH.md §Architecture project structure). Standalone package.json with its own node_modules. NOT part of web monorepo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create web test file scaffolding + Mux webhook fixtures</name>
  <files>
    web/src/__tests__/video/mux-live-input.test.ts,
    web/src/__tests__/video/mux-jwt.test.ts,
    web/src/__tests__/video/mux-webhook.test.ts,
    web/src/__tests__/video/vod-playback.test.ts,
    web/src/__tests__/video/retention.test.ts,
    web/src/__tests__/video/portal-video-auth.test.ts,
    web/src/__tests__/video/cameras-section.test.tsx,
    web/src/__tests__/video/upload-validation.test.ts,
    web/src/__tests__/video/ratelimit.test.ts,
    web/src/__tests__/video/fixtures/mock-mux-webhook-active.json,
    web/src/__tests__/video/fixtures/mock-mux-webhook-disconnected.json,
    web/src/__tests__/video/fixtures/mock-mux-webhook-idle.json,
    web/src/__tests__/video/fixtures/mock-mux-webhook-asset-ready.json,
    web/src/__tests__/video/fixtures/tiny.mp4
  </files>
  <read_first>
    web/vitest.config.ts,
    web/package.json,
    .planning/phases/22-live-site-video-per-project-hls-camera-feeds-tied-to-project/22-VALIDATION.md,
    .planning/phases/22-live-site-video-per-project-hls-camera-feeds-tied-to-project/22-RESEARCH.md
  </read_first>
  <action>
    Create 9 vitest stub test files. Each file must:
    1. Import vitest: `import { describe, it, expect } from 'vitest'`
    2. Have EXACTLY ONE placeholder test that is currently skipped: `it.skip('TODO: implemented in Wave N plan XX-YY', () => { expect(true).toBe(true) })`
    3. Include a top-of-file comment naming the downstream plan that owns it (e.g., `// Owner: 22-03-PLAN.md Wave 2 — Mux JWT minting (VIDEO-01-E)`).

    File-by-file stub content:
    - `mux-live-input.test.ts` — owner 22-03-PLAN, tests POST /api/video/mux/create-live-input schema (returns live_input_id, stream_key, playback_id, rtmp_url)
    - `mux-jwt.test.ts` — owner 22-03-PLAN, tests signPlaybackJWT claims (sub=playback_id, aud='v', exp<=now+300, kid set, RS256 alg)
    - `mux-webhook.test.ts` — owner 22-03-PLAN, tests Mux-Signature HMAC verify reject (wrong signature → 401) + dedupe via cs_video_webhook_events
    - `vod-playback.test.ts` — owner 22-04-PLAN, tests batch-sign manifest (manifest contains signed URLs for every .ts line, Content-Type: application/vnd.apple.mpegurl)
    - `retention.test.ts` — owner 22-10-PLAN, tests prune deletes rows where retention_expires_at < now() AND removes storage objects AND calls mux.video.assets.delete for archived live assets
    - `portal-video-auth.test.ts` — owner 22-09-PLAN, tests: drone source_type → 403, portal_visible=false → 403, live with show_cameras=false → 403, head-only for portal live (no DVR offset)
    - `cameras-section.test.tsx` — owner 22-08-PLAN, tests CamerasSection renders 0-camera empty state, live camera card, clip card, soft cap banner at 16+ cameras
    - `upload-validation.test.ts` — owner 22-08-PLAN, tests client-side guards: >2GB rejected with AppError.clipTooLarge, >60min rejected with AppError.clipTooLong, non-MP4/MOV rejected with AppError.unsupportedVideoFormat
    - `ratelimit.test.ts` — owner 22-03-PLAN, tests 30 req/min/IP limit on /api/video/mux/playback-token, /api/video/vod/playback-url, /api/portal/video/playback-token — 31st returns 429 with Retry-After header

    Create 4 Mux webhook fixture JSON files under `web/src/__tests__/video/fixtures/` following the Mux webhook payload envelope verified in 22-RESEARCH.md §"Webhook Events We Care About":

    ```json
    // mock-mux-webhook-active.json
    {
      "type": "video.live_stream.active",
      "object": { "type": "live_stream", "id": "test-live-input-id-active" },
      "id": "mux-event-test-active-001",
      "created_at": "2026-04-15T12:00:00.000Z",
      "data": {
        "id": "test-live-input-id-active",
        "status": "active",
        "stream_key": "REDACTED",
        "latency_mode": "low",
        "playback_ids": [{ "id": "test-playback-id-001", "policy": "signed" }]
      }
    }
    ```
    Same shape for `disconnected` (status=disconnected, type=video.live_stream.disconnected), `idle` (status=idle, type=video.live_stream.idle), and `asset-ready` (type=video.asset.ready with data: { id, live_stream_id, duration: 120.5, playback_ids: [...] }).

    Create `tiny.mp4` fixture: a 5-second 320x240 MP4 generated via `ffmpeg -f lavfi -i testsrc=duration=5:size=320x240:rate=30 -c:v libx264 -preset veryfast -pix_fmt yuv420p -movflags +faststart web/src/__tests__/video/fixtures/tiny.mp4 -y`. This is the lavfi testsrc pattern (copyright-safe, synthetic video). Target file size < 200KB. If ffmpeg is unavailable locally, commit a placeholder 0-byte file named tiny.mp4 and flag in task Done — Wave 1 worker plan will regenerate.
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8/web" && npm run test -- --run src/__tests__/video/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls web/src/__tests__/video/mux-live-input.test.ts web/src/__tests__/video/mux-jwt.test.ts web/src/__tests__/video/mux-webhook.test.ts web/src/__tests__/video/vod-playback.test.ts web/src/__tests__/video/retention.test.ts web/src/__tests__/video/portal-video-auth.test.ts web/src/__tests__/video/cameras-section.test.tsx web/src/__tests__/video/upload-validation.test.ts web/src/__tests__/video/ratelimit.test.ts` all exist
    - Each test file contains the literal string `it.skip(`
    - Each test file contains the literal string `// Owner: 22-`
    - `ls web/src/__tests__/video/fixtures/mock-mux-webhook-active.json web/src/__tests__/video/fixtures/mock-mux-webhook-disconnected.json web/src/__tests__/video/fixtures/mock-mux-webhook-idle.json web/src/__tests__/video/fixtures/mock-mux-webhook-asset-ready.json web/src/__tests__/video/fixtures/tiny.mp4` all exist
    - `jq -r '.type' web/src/__tests__/video/fixtures/mock-mux-webhook-active.json` returns `video.live_stream.active`
    - `jq -r '.type' web/src/__tests__/video/fixtures/mock-mux-webhook-disconnected.json` returns `video.live_stream.disconnected`
    - `jq -r '.type' web/src/__tests__/video/fixtures/mock-mux-webhook-asset-ready.json` returns `video.asset.ready`
    - `cd web && npm run test -- --run src/__tests__/video/ 2>&1 | grep -E "skipped|pending"` shows 9 skipped tests
    - `cd web && npm run test -- --run src/__tests__/video/ 2>&1 | grep -E "(FAIL|error)"` returns no unexpected errors (only skip is acceptable)
  </acceptance_criteria>
  <done>
    9 web test files + 4 Mux webhook fixtures + tiny.mp4 present. `npm run test -- --run src/__tests__/video/` exits 0 with 9 skipped tests and zero failures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create iOS XCTest scaffolding under ready player 8Tests/VideoTests/</name>
  <files>
    "ready player 8Tests/VideoTests/VideoModelTests.swift",
    "ready player 8Tests/VideoTests/LiveStreamViewTests.swift",
    "ready player 8Tests/VideoTests/VideoClipPlayerTests.swift",
    "ready player 8Tests/VideoTests/VideoAuthTests.swift"
  </files>
  <read_first>
    "ready player 8Tests/",
    .planning/phases/22-live-site-video-per-project-hls-camera-feeds-tied-to-project/22-VALIDATION.md
  </read_first>
  <action>
    Create 4 XCTest stub files under `ready player 8Tests/VideoTests/` (create the directory if it does not exist — Xcode's PBXFileSystemSynchronizedRootGroup will auto-include new Swift files per MEMORY.md).

    Each file must:
    1. `import XCTest` and `@testable import ready_player_8`
    2. Define `final class {FileBaseName}: XCTestCase`
    3. Contain EXACTLY ONE `func test_placeholder_TODO_WaveN_Plan2XYY() throws { throw XCTSkip("TODO: implemented in Wave N plan 22-YY-PLAN.md") }`
    4. Top-of-file comment naming the owner plan.

    File contents:
    - `VideoModelTests.swift` — owner 22-02-PLAN Wave 1, will test VideoSource/VideoAsset struct codable round-trip for `kind='fixed_camera'|'drone'|'upload'` and `source_type='fixed_camera'|'drone'|'upload'`
    - `LiveStreamViewTests.swift` — owner 22-06-PLAN Wave 3, will test SwiftUI LiveStreamView instantiates with a VideoSource struct and exposes portalToken param
    - `VideoClipPlayerTests.swift` — owner 22-06-PLAN Wave 3, will test VideoClipPlayer instantiates with VideoAsset and loads signed manifest URL
    - `VideoAuthTests.swift` — owner 22-05-PLAN Wave 2, will test SupabaseService.fetchVideoPlaybackToken throws AppError.permissionDenied on 403 and AppError.network on 500

    Example body:
    ```swift
    // Owner: 22-02-PLAN.md Wave 1 — VideoSource/VideoAsset model types (VIDEO-01-A)
    import XCTest
    @testable import ready_player_8

    final class VideoModelTests: XCTestCase {
        func test_placeholder_TODO_Wave1_Plan2202() throws {
            throw XCTSkip("TODO: implemented in Wave 1 plan 22-02-PLAN.md — covers VIDEO-01-A VideoSource/VideoAsset codable round-trip")
        }
    }
    ```

    NOTE on Xcode project file: Because project uses `PBXFileSystemSynchronizedRootGroup` (confirmed in MEMORY.md), adding Swift files under `ready player 8Tests/` auto-includes them in the test target. No `.pbxproj` edit required. Confirm this by running xcodebuild test and seeing the test discovered.
  </action>
  <verify>
    <automated>cd "/Users/beverlyhunter/Desktop/ready player 8" && xcodebuild -scheme "ready player 8" -destination 'platform=iOS Simulator,name=iPhone 15' -only-testing:"ready player 8Tests/VideoTests" test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `ls "ready player 8Tests/VideoTests/VideoModelTests.swift" "ready player 8Tests/VideoTests/LiveStreamViewTests.swift" "ready player 8Tests/VideoTests/VideoClipPlayerTests.swift" "ready player 8Tests/VideoTests/VideoAuthTests.swift"` all exist
    - Each file contains literal `throw XCTSkip(`
    - Each file contains literal `// Owner: 22-`
    - Each file contains `@testable import ready_player_8`
    - `xcodebuild test -only-testing:"ready player 8Tests/VideoTests"` output contains exactly 4 tests with result "Test Suite 'VideoTests' passed" and 4 "Test Case '-[... test_placeholder_TODO_...]' skipped"
  </acceptance_criteria>
  <done>
    4 XCTest files exist under VideoTests/, xcodebuild test discovers them, all 4 report skipped (not failed, not missing).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create worker/ subdirectory skeleton with Dockerfile + package.json + smoke test</name>
  <files>
    worker/package.json,
    worker/tsconfig.json,
    worker/vitest.config.ts,
    worker/Dockerfile,
    worker/.dockerignore,
    worker/.gitignore,
    worker/__tests__/transcode.smoke.test.ts,
    worker/README.md
  </files>
  <read_first>
    .planning/phases/22-live-site-video-per-project-hls-camera-feeds-tied-to-project/22-RESEARCH.md,
    web/package.json
  </read_first>
  <action>
    Create the ffmpeg worker subdirectory skeleton that Wave 2 plan 22-04 will fill in.

    `worker/package.json`:
    ```json
    {
      "name": "constructionos-video-worker",
      "version": "0.1.0",
      "private": true,
      "description": "ffmpeg transcode worker for ConstructionOS Phase 22 — deploys to Fly.io",
      "scripts": {
        "build": "tsc",
        "start": "node dist/server.js",
        "dev": "tsx src/server.ts",
        "test": "vitest --run"
      },
      "dependencies": {
        "@supabase/supabase-js": "^2.101.1",
        "hono": "^4.6.0"
      },
      "devDependencies": {
        "@types/node": "^20.0.0",
        "tsx": "^4.7.0",
        "typescript": "^5.5.0",
        "vitest": "^3.2.4"
      }
    }
    ```

    `worker/tsconfig.json`: standard Node 20 ESNext config with `outDir: "dist"`, `rootDir: "src"`, `strict: true`, `module: "NodeNext"`, `moduleResolution: "NodeNext"`.

    `worker/vitest.config.ts`:
    ```ts
    import { defineConfig } from 'vitest/config'
    export default defineConfig({ test: { environment: 'node', include: ['__tests__/**/*.test.ts'] } })
    ```

    `worker/Dockerfile` (stub — Wave 2 fleshes out):
    ```dockerfile
    FROM node:20-bookworm-slim
    RUN apt-get update && apt-get install -y --no-install-recommends ffmpeg ca-certificates && rm -rf /var/lib/apt/lists/*
    WORKDIR /app
    COPY package.json package-lock.json* ./
    RUN npm install --omit=dev
    COPY . .
    RUN npm run build
    ENV NODE_ENV=production
    EXPOSE 8080
    CMD ["node", "dist/server.js"]
    ```

    `worker/.dockerignore`: `node_modules`, `dist`, `__tests__`, `.env*`.
    `worker/.gitignore`: `node_modules`, `dist`, `.env*`.

    `worker/__tests__/transcode.smoke.test.ts`:
    ```ts
    // Owner: 22-04-PLAN.md Wave 2 — ffmpeg transcode worker (VIDEO-01-H)
    import { describe, it, expect } from 'vitest'
    describe('ffmpeg transcode smoke', () => {
      it.skip('TODO: implemented in 22-04-PLAN.md — ffmpeg produces valid m3u8 + segments + poster.jpg from tiny.mp4', () => {
        expect(true).toBe(true)
      })
    })
    ```

    `worker/README.md`: one page explaining the worker's role — receives POST /transcode from Supabase DB webhook, downloads raw.{ext} from Storage, runs ffmpeg HLS transcode, uploads to hls/ directory, updates cs_video_assets row. Mentions Fly.io target.

    Do NOT run `npm install` in worker/ (leave that for the executor of 22-04-PLAN in Wave 2 — they pick the exact working versions). Commit without node_modules.
  </action>
  <verify>
    <automated>ls -la "/Users/beverlyhunter/Desktop/ready player 8/worker/" && cat "/Users/beverlyhunter/Desktop/ready player 8/worker/package.json" | grep -c '"name": "constructionos-video-worker"'</automated>
  </verify>
  <acceptance_criteria>
    - `ls worker/package.json worker/tsconfig.json worker/vitest.config.ts worker/Dockerfile worker/.dockerignore worker/.gitignore worker/__tests__/transcode.smoke.test.ts worker/README.md` all exist
    - `jq -r '.name' worker/package.json` returns `constructionos-video-worker`
    - `jq -r '.scripts.test' worker/package.json` returns `vitest --run`
    - `grep -c 'ffmpeg' worker/Dockerfile` returns at least 1
    - `grep -c 'it.skip' worker/__tests__/transcode.smoke.test.ts` returns at least 1
    - `grep -c 'Owner: 22-04-PLAN' worker/__tests__/transcode.smoke.test.ts` returns 1
  </acceptance_criteria>
  <done>
    Worker skeleton exists. Wave 2 plan 22-04 can cd into worker/ and npm install without structural work.
  </done>
</task>

</tasks>

<verification>
- All 9 web test stubs exist and vitest runs them green (as skipped)
- All 4 iOS test stubs exist and xcodebuild test discovers them as skipped
- Worker skeleton has package.json, Dockerfile, test config
- 4 Mux webhook fixtures are valid JSON with correct `type` values
- tiny.mp4 exists (even if 0 bytes, gets regenerated in Wave 2)
- No test file is missing an owner comment
- No Wave 1-4 plan is referencing a test command that will resolve to "file not found"
</verification>

<success_criteria>
Every downstream plan (22-01 through 22-10) can cite a concrete `<automated>` command that targets a file already committed in Wave 0. Running `npm run test -- --run src/__tests__/video/` and `xcodebuild test -only-testing:VideoTests` both exit 0 with all tests skipped (not failing).
</success_criteria>

<output>
After completion, create `.planning/phases/22-live-site-video-per-project-hls-camera-feeds-tied-to-project/22-00-SUMMARY.md`
</output>
