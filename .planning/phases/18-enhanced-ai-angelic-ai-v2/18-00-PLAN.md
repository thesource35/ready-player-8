---
phase: 18-enhanced-ai-angelic-ai-v2
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - web/src/app/api/chat/__tests__/tools.test.ts
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-04

must_haves:
  truths:
    - "Test stubs exist for all four AI tool categories before implementation begins"
    - "Tests import from the tools module path that will be created in Plan 18-01"
  artifacts:
    - path: "web/src/app/api/chat/__tests__/tools.test.ts"
      provides: "Unit test stubs for get_projects, get_contracts, generate_rfi, draft_change_order, analyze_bid tools"
      min_lines: 80
  key_links:
    - from: "web/src/app/api/chat/__tests__/tools.test.ts"
      to: "web/src/app/api/chat/tools.ts"
      via: "import { createConstructionTools } from '../tools'"
      pattern: "createConstructionTools"
---

<objective>
Create test scaffolding for Phase 18 AI tools before implementation begins.

Purpose: Establish failing test stubs (Wave 0 Nyquist requirement) so that Plan 18-01 implementation has immediate feedback via `npx vitest run`.
Output: Test file with stubs covering AI-01 through AI-04 tool behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-enhanced-ai-angelic-ai-v2/18-RESEARCH.md
@.planning/phases/18-enhanced-ai-angelic-ai-v2/18-VALIDATION.md
@web/vitest.config.ts
@web/src/app/api/chat/__tests__/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create web tool test stubs for AI-01 through AI-04</name>
  <files>web/src/app/api/chat/__tests__/tools.test.ts</files>
  <read_first>
    - web/vitest.config.ts
    - web/src/app/api/chat/route.ts
    - web/src/lib/supabase/server.ts
  </read_first>
  <action>
Create `web/src/app/api/chat/__tests__/tools.test.ts` with test stubs that will fail until Plan 18-01 implements the tools module.

The test file must:

1. Import `createConstructionTools` from `../tools` (this file does not exist yet -- tests will fail to compile until 18-01 creates it).

2. Create a mock Supabase client factory:
```typescript
function mockSupabase(overrides: Record<string, unknown[]> = {}) {
  return {
    from: (table: string) => ({
      select: () => ({
        limit: () => ({
          eq: () => Promise.resolve({ data: overrides[table] ?? [], error: null }),
          then: (resolve: Function) => resolve({ data: overrides[table] ?? [], error: null }),
        }),
        eq: () => ({
          single: () => Promise.resolve({ data: (overrides[table] ?? [])[0] ?? null, error: null }),
        }),
        then: (resolve: Function) => resolve({ data: overrides[table] ?? [], error: null }),
      }),
    }),
  } as unknown as ReturnType<typeof import("@supabase/supabase-js").createClient>;
}
```

3. Test groups with `describe` blocks:

**AI-01 tests (get_projects, get_contracts):**
- `test("get_projects returns project data from Supabase")` -- call `tools.get_projects.execute({})` with mock returning `[{ id: "p1", name: "Test Project", status: "Active", progress: 50, budget: 1000000, client: "ACME" }]` for `cs_projects`. Assert result contains `projects` array with length 1.
- `test("get_contracts returns contract data from Supabase")` -- call `tools.get_contracts.execute({})` with mock returning `[{ id: "c1", title: "Bridge Contract", stage: "Proposal", budget: 500000 }]` for `cs_contracts`. Assert result contains `contracts` array.
- `test("get_projects returns error when not authenticated")` -- pass `null` as supabase. Assert result contains `{ error: "Not authenticated" }`.

**AI-02 tests (generate_rfi):**
- `test("generate_rfi returns structured RFI draft")` -- call with `{ subject: "Concrete spec", details: "Need clarification on mix design", priority: "HIGH" }`. Assert result has `type: "rfi_draft"`, `status: "DRAFT"`, `subject`, `details`, `priority`, `_action: "review_before_saving"`.

**AI-03 tests (draft_change_order):**
- `test("draft_change_order returns structured CO draft")` -- call with `{ description: "Added fire stops", amount: 18500, requested_by: "Owner" }`. Assert result has `type: "change_order_draft"`, `status: "DRAFT"`, `amount: 18500`, `_action: "review_before_saving"`.

**AI-04 tests (analyze_bid):**
- `test("analyze_bid fetches contract and market data")` -- call with `{ contract_id: "c1" }` and mock returning contract data for `cs_contracts` and market data for `cs_market_data`. Assert result has `contract` object and `market_context` array.
- `test("analyze_bid returns error for missing contract")` -- call with `{ contract_id: "nonexistent" }` and mock returning null. Assert result has `{ error: "Contract not found" }`.

Each test should call `createConstructionTools(mockSupabase(...))` to get the tools object, then call `tools.{name}.execute(input)`.

Use `import { describe, test, expect } from "vitest"` -- no vi.mock needed since we're passing the supabase client directly.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/app/api/chat/__tests__/tools.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at web/src/app/api/chat/__tests__/tools.test.ts
    - File contains `import { createConstructionTools } from "../tools"`
    - File contains `describe("AI-01` or `describe("get_projects`
    - File contains `describe("AI-02` or `describe("generate_rfi`
    - File contains `describe("AI-03` or `describe("draft_change_order`
    - File contains `describe("AI-04` or `describe("analyze_bid`
    - File contains at least 8 `test(` calls
    - Tests fail with module not found error (expected -- tools.ts not yet created)
  </acceptance_criteria>
  <done>Test file exists with stubs for all 4 requirement areas. Tests fail because tools.ts does not exist yet (expected Wave 0 state).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test scaffolding only -- no production code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-00-01 | N/A | test stubs | accept | No production code in this plan |
</threat_model>

<verification>
- Test file exists and is syntactically valid TypeScript
- Running vitest on the test file produces expected failure (import error for missing tools.ts)
</verification>

<success_criteria>
- web/src/app/api/chat/__tests__/tools.test.ts exists with 8+ test stubs
- Tests reference createConstructionTools from ../tools
- All 4 requirement IDs (AI-01 through AI-04) have corresponding test blocks
</success_criteria>

<output>
After completion, create `.planning/phases/18-enhanced-ai-angelic-ai-v2/18-00-SUMMARY.md`
</output>
