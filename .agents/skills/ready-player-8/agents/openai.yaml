interface:
  display_name: "Ready Player 8"
  short_description: "Repo-specific patterns and workflows for ready-player-8"
  default_prompt: "Use the ready-player-8 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true